# DEBT — the last-resort debt surface (hand-authored)

A present gap with no better file to carry its marker is stated here as
a column-0 `Owed: <stamp>: <reason>` line — marker form rule v2, like
every other text surface — so even the last resort enumerates into
`DEBT-AUTO.txt`, the only file ever read for present debt (PAR-0002).
One marker per file: a second simultaneous entry is standing pressure
to extend the marker grammar. Steady state: no entries.
