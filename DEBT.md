# DEBT — the last-resort debt surface (hand-authored)

A present gap with no better file to carry its marker is stated here as
a column-0 `Owed: <stamp>: <reason>` line — marker form rule v2, like
every other text surface — so even the last resort enumerates into
`DEBT-AUTO.md`, the only file ever read for present debt (PAR-0002).
One marker per file: a second simultaneous entry is standing pressure
to extend the marker grammar. Steady state: no entries.

Owed: 20260803T051541Z: the how-to index — the layer's generated read surface (PAR-0003 "The index": a pure function of the domain-folder walk, regenerated in the same commit as the change it reflects, with a mismatch test so staleness reds) — owed since the layer began governing with its first resident and no index; carried here because a generated file cannot be hand-created as its own placeholder; the entrypoint question (a `sieve.debt` sibling or one shared regen command) is named in PAR-0003's Consequences and decided when this is built
