<!-- Generated by scripts/doc_index.py. Do not edit; rerun the tool. -->

# SCAFFOLD — where things live

## `./`

- `.gitattributes`
- `.gitignore`
- `.python-version`
- `CLAUDE.md`
- `SIEVE.py` — Open SIEVE: `uv run SIEVE.py`.
- `pyproject.toml`
- `uv.lock`

## `.githooks/`

- `pre-commit`

## `docs/`

- `SCAFFOLD.md`

## `mockup/`

- `mockup.py` — The GUI as the tree has it, copied into one runnable file with mock data.

## `scripts/`

- `doc_index.py` — Regenerate `docs/SCAFFOLD.md` from the tree git is about to commit.

## `src/sieve/` — SIEVE isolates ethological events from video.

- `__main__.py` — `uv run sieve`, or `uv run python -m sieve`.

## `src/sieve/gui/` — The interactive surface: the frame, and what stands in its compartments.

- `palette.py` — The colours every surface draws with.

## `src/sieve/gui/frame/` — The frame: the window, its three compartments, and the chrome between them.

- `chrome.py` — What the window wears where no compartment covers it.
- `compartments.py` — The three rooms the window divides into, and nothing standing in them yet.
- `menu.py` — The bar across the top, and what the window itself can be asked to do.
- `window.py` — The window: a menu bar, three compartments, and the boundaries between them.
