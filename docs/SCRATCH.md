---
status: working
---

# Scratch

Running notes from a fast pass, held only until they have a home. **This file
owns nothing.** A note here is a debt: it means work was done and the record
has not been written yet, so the first job of the next session is to empty it,
not to add to it.

Where a note goes when it leaves: what was built goes to
`docs/completed-todo/` (by *moving* the item file — `tools/complete_item.py`),
what is measurably true goes to `docs/findings/`, what is still open goes into
the item's own `gated_on`.

---

The 2026-07-28 pass was drained 2026-07-28. Five items completed
(`headless-detection`, `the-decode-format-has-six-derivations`,
`budgets-attribute-cost-they-do-not-cap-it`, `the-todo-dag-is-prose`,
`machine-share-policy-is-above-its-consumers`), one finding written
(`2026.07.28-the-density-headroom-is-under-the-machines-own-variation.md`), and
the residue folded into the `gated_on` of the two items it belongs to
(`proxy-retention-policy`, `one-edit-is-three-regions-in-two-files`).

Nothing pending.
