<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# What already exists — take it, do not rebuild it

Every row is a `settled:` block in the `docs/completed-todo/` entry that
decided it; that entry is the home and this table is derived. The third
column is the part that costs a day if you re-derive it.

| What | Where | What you must not re-decide | Settled by |
|---|---|---|---|
| The gui-computes-nothing exception list shrinks when the adapter owns detector work | `.importlinter` | The stale direct GUI imports of sieve.detect and detector_worker's wavelet ops stay removed; adding them back is a reviewed widening of REWORK R1. | [2026-08-05](completed-todo/2026.08.05-detection-is-a-filter.md) |
| The placement contract and its shrink-only exception discipline | `.importlinter` | `allow_indirect_imports` stays True — after detection-is-a-filter, `gui -> pipeline -> filters -> core.wavelet` is the supported path, and forbidding indirect imports would fire on the architecture the contract exists to reach. `unmatched_ignore_imports_alerting` stays `error` — that is the shrink-only mechanism (deleting code and its exception are one edit), and adding an ignore entry is a reviewed widening of the rework, never a fix. | [2026-07-29](completed-todo/2026.07.29-the-exception-list-is-the-work-list.md) |
| gate/rebuild order in the work loop | CLAUDE.md work loop steps 7-8, noxfile.py docs docstring, tools/complete_item.py _ALWAYS | nox -s docs precedes nox -s checks, and docs is never made a prerequisite of checks - regenerating inside the gate makes tests/docs/test_doc_index.py compare fresh output against itself, deleting the only staleness check CI runs | [2026-08-05](completed-todo/2026.08.05-one-gate-run-per-item.md) |
| holding the preview for a write means pausing AND releasing the files, together | FilterTab._on_boundary_hold | that the two are one signal delivered synchronously; a queued connection or a split into two emits puts the unlink back in a race with the release, which is the bug where discard silently did nothing | [2026-08-05](completed-todo/2026.08.05-the-source-boundary-is-its-own-object.md) |
| A REWORK rule graduates through its Gate line, by hand | `docs/REWORK.md` | A rule leaves REWORK.md only when its Gate names something that exists and the shrink-only list behind it is empty; the sentence folds into the ARCHITECTURE.md rule it strengthens through that rule's own falsifier process. Nothing automates the fold — a tool that edits prose in two documents is a churn machine — and when the last rule graduates, REWORK.md flips to `record`. | [2026-07-29](completed-todo/2026.07.29-the-rework-enters-the-docs.md) |
| `src/sieve/docs/` never existed | `docs/SIEVE-HANDOFF.md` | The eight interface specs `SIEVE-HANDOFF.md` used to ask for are module docstrings plus their completed-todo entries, and the handoff now says so. | [2026-07-27](completed-todo/2026.07.27-doc-lifecycle-machinery.md) |
| `ARCHITECTURE-TREE.md` does not exist and no longer obviously should | `docs/findings/.index.md`, `docs/completed-todo/.index.md` | `docs/findings/` holds measurement-driven decisions one file at a time and `docs/completed-todo/` holds what was built. | [2026-07-27](completed-todo/2026.07.27-doc-lifecycle-machinery.md) |
| The crop belongs in the graph | `docs/findings/2026.07.25-the-crop-belongs-in-the-graph.md` | The per-replicate threshold-spread probe that would have measured one rack under one backlight is cancelled, not deferred. | [2026-07-27](completed-todo/2026.07.27-crop-tools.md) |
| `sieve detect --csv` is not the `Project.Sink` writer and does not relieve it | `docs/todo/sink-writers.md` | This writes the *detector's* output, computed outside the graph with a producer that already exists. `Sink` remains gated on a filter emitting a `TableSpec`; nothing does, so `cli/run_cmd.py` `_refuse_sinks` stands. | [2026-07-28](completed-todo/2026.07.28-detection-csv-export.md) |
| The gate's verdict line, and the stream it goes on | `noxfile.py` | The verdict is `print`ed to stdout, not logged through `session.log`, and that is the whole point: nox reports on stderr, and the reads that matter — `\| tail -n`, `Select-Object -Last n` — carry stdout alone. Routing it through the logger for consistency restores the exact silence this removed. It is last on the stdout stream and second-to-last on a merged one, behind nox's own concordant summary; that residue is not fixable from inside a session and is not a defect. | [2026-08-05](completed-todo/2026.08.05-the-gate-ends-with-a-verdict.md) |
| where the drift report is printed | noxfile.py `drift` session; `docs` prints `doc_drift.py --summary` only | `docs` runs after every completed item, so a report long enough to scroll away a piped verdict does not belong in it | [2026-08-05](completed-todo/2026.08.05-drift-is-an-audit-not-a-per-commit-check.md) |
| napari and pyqtgraph are out of the `gui` extra, and stay out | `pyproject.toml` | Closed 2026.07.26. The filter-tab plot family landed as QPainter widgets over `src/sieve/gui/band_plot.py`, and the last item owning a napari question — the three-way overlay — answered it by collapsing to two layers and one opacity slider. Re-adding either needs a new demand, not a revisit. | [2026-07-26](completed-todo/2026.07.26-plot-widgets.md) |
| Where the "what can this executor not call" enumeration lives | `src/sieve/backend/dispatch.py` | Beside the kernel protocols, not beside the executor that raises. Every refusal exists because no protocol in that module takes the shape, so the protocol and its refusal shrink together in one file on the day a filter needs one — `a-kernel-that-sees-a-span` deletes a branch there and nowhere else. The function is pure over the spec and returns a *clause*; the node id and the filter's id and version are prefixed by `_bind`, which is what keeps the answer machine-independent and the message actionable. It stays a separate question from `KernelRegistry.select`: whether any protocol could call this spec is the same on every machine, and whether a kernel for the plan's backend exists here is not. | [2026-08-04](completed-todo/2026.08.04-declarable-but-not-runnable.md) |
| Single-input windowed kernel shape | `src/sieve/backend/dispatch.py`, `src/sieve/pipeline/executor.py` | A windowed kernel is registered with @windowed_kernel, receives a FrameSpan, and must return the frame for FrameSpan.target; the executor sizes that span as refined warmup_frames plus the current frame. | [2026-08-05](completed-todo/2026.08.05-a-kernel-that-sees-a-span.md) |
| How exhaustiveness over a declaration enum is enforced | `src/sieve/backend/dispatch.py`, `tests/unit/test_declarable_shapes.py` | `if`/`elif`/`else` with `assert_never` in the `else`, not `match`. An exhaustive `match` needs a catch-all pattern and pyright strict reports that pattern as unreachable (`reportUnnecessaryComparison`), so the construct that reads better is the one that cannot carry the check — this was tried and reverted. What the idiom buys is that a third `Mode` or `StreamKind` member narrows to itself rather than to `Never` and fails pyright on the day it is written, which is a stronger and earlier instrument than the walk. The walk is the second one, and it is built from the enums by `itertools.product` rather than from a list of shapes, for the same reason. | [2026-08-04](completed-todo/2026.08.04-declarable-but-not-runnable.md) |
| Timings | `src/sieve/bench/metrics.py`, `src/sieve/gui/executor_adapter.py` | Qt-free bus; the adapter is the only place that knows both it and Qt. Do not invent a callback to report through. | [2026-07-26](completed-todo/2026.07.26-the-metric-bus.md) |
| A series node that cannot be labelled is refused, never renamed neutrally | `src/sieve/cli/detect_cmd.py` `_series_node` | Three refusals, each a different wrong artifact: a table emitter (no per-frame series), a node behind a rate change (`frame = start + offset` is wrong by the rate, silently), and a node whose elements have no meaning. Renaming the columns to `units_in_band` instead was the alternative and is worse than either — honest and unreadable, which is the trade rule 6 exists to refuse rather than to make. | [2026-07-28](completed-todo/2026.07.28-what-one-element-means.md) |
| The warmup split on the filter contract | `src/sieve/core/filter_base.py` | `FilterSpec.warmup_frames` is the *bound* over the legal parameter range; `ParamsBase.warmup_frames` is the *configured* need. `node_warmup_frames` picks. Declaring only the bound charges every run for the worst case (7199 frames, in the case that forced the split). | [2026-07-26](completed-todo/2026.07.26-per-block-temporal-baseline.md) |
| Structured filter presentation grows spec fields | src/sieve/core/filter_base.py CaptionPart FilterSpec.caption FilterSpec.param_value_labels | Guidance markdown remains the author's long-form prose surface; captions and option labels stay typed spec fields because controls need structured, validated values rather than parsed prose. | [2026-08-05](completed-todo/2026.08.05-presentation-is-a-channel-not-a-switch.md) |
| FilterSpec's fields are partitioned into three declared channels | src/sieve/core/filter_base.py Channel, SPEC_CHANNELS | that presentation is its own channel rather than part of the unhashed remainder, that cost/primary_params/summary are presentation, and that backend_agnostic is identity because it decides whether backend identity leaves the digest | [2026-08-05](completed-todo/2026.08.05-the-spec-has-three-channels.md) |
| emitted element names are identity contract data | src/sieve/core/filter_base.py ElementNames FilterSpec.element_names SPEC_CHANNELS | A concrete ElementKind must carry column-safe singular and plural names, relation declarations preserve or lose upstream names, and the field is identity rather than presentation because it names exported artifacts. | [2026-08-05](completed-todo/2026.08.05-a-filter-names-what-it-emits.md) |
| The declaration has no default, and an array emitter without one is refused | `src/sieve/core/filter_base.py` `FilterSpec.__post_init__` | Defaulting `element` to `PRESERVED` costs nothing today — every filter on the shelf that preserves would be right by accident — and converts the next element-redefining filter's omission into a CSV column with an invented noun, which is the failure this whole item closed. `None` means "emits rows", enforced against `emits` in both directions so it cannot also mean "forgot". The price is that every `@register_filter` in the suite states it; that is the enforcement, not a side effect of it. | [2026-07-28](completed-todo/2026.07.28-what-one-element-means.md) |
| Aggregation is kind-dependent, and its open default fails closed | `src/sieve/core/filter_base.py` `node_element` | `AGGREGATED` keeps `PIXEL` and refuses `BLOCK` — a mean of pixels is the scene sampled more coarsely, a mean of blocks is not a block. The branch names `PIXEL` and sends every other kind to `None`, which is a default over `ElementKind` in the opposite polarity to the one refused above: an `ElementKind` member added later is *refused* under aggregation until somebody decides what a mean of one means, rather than silently misnamed. Deliberate, not incidental. | [2026-07-28](completed-todo/2026.07.28-what-one-element-means.md) |
| Warmup along a path | `src/sieve/core/filter_base.py` `source_warmup_frames` | It does **not** sum. It walks sink to root converting `need` to `ceil(need / output_rate)`. A plain sum compiles, runs, and under- warms every temporal filter behind a decimator while rendering a plausible frame. | [2026-07-26](completed-todo/2026.07.26-per-block-temporal-baseline.md) |
| Windowed cache boundary | `src/sieve/core/filter_base.py`, `src/sieve/pipeline/cache_key.py` | Mode.WINDOWED specs are not cacheable under the existing node-key plus source-index store, because provisional lead-in outputs depend on the run frontier until founded spans enter the cache contract. | [2026-08-05](completed-todo/2026.08.05-a-kernel-that-sees-a-span.md) |
| Element meaning is a filter declaration resolved along the graph | `src/sieve/core/filter_base.py`, `src/sieve/pipeline/dag.py` | Not a field on `ArraySpec` and not a constant on every spec. `temporal_baseline` accepts any array and estimates per cell, so it emits blocks over `block_signal` and pixels over a raw frame — any constant it declared would be a lie in one of the two positions, which is why `ElementRelation.PRESERVED` exists and why the answer needs a walk. `node_element` is the one-node conversion and `Dag._elements` is the traversal that supplies its second argument, the same split `input_warmup_frames` and `plan._lead_in` have. | [2026-07-28](completed-todo/2026.07.28-what-one-element-means.md) |
| How a filter says which frames it keeps | `src/sieve/core/filter_base.py`, `src/sieve/pipeline/plan.py` | A selecting filter sets selecting=True and overrides ParamsBase.selected_frames(), returning a half-open range whose identity value is ALL_FRAMES; ExecutionPlan.build intersects every node's range with the requested span. Nothing enumerates which filters select and pipeline never names one, so do not re-add a span that reaches the executor without passing through the fold. | [2026-08-05](completed-todo/2026.08.05-the-span-is-a-filter.md) |
| the decorator keeps a typed, hand-written signature | src/sieve/core/filter_registry.py + tests/unit/test_filter_contract.py TestDecoratorMatchesSpec | do not rebuild register_filter around **kwargs or a generated signature — it would erase the per-keyword static types at the one place a filter author is checked; the drift is tested for instead | [2026-08-05](completed-todo/2026.08.05-register-filter-signature-matches-spec.md) |
| Filter discovery | `src/sieve/core/filter_registry.py`, `src/sieve/filters/__init__.py` | A `pkgutil` scan that names nothing. Never add a filter to an import list — a test AST-parses for exactly that. | [2026-07-25](completed-todo/2026.07.25-first-filter-and-discovery.md) |
| The saved artifact | `src/sieve/core/pipeline_model.py` | Schema v3. `Edge.port` names the input it feeds; `Project.detector` and the pin fields landed in v3; v1 and v2 documents still load. | [2026-07-25](completed-todo/2026.07.25-pipeline-artifact.md) |
| The detector in the artifact | `src/sieve/core/pipeline_model.py` `DetectorSettings` | Schema v3. Bands, count threshold, and D live on `Project.detector` with field-level pins in `Replicate.detector_overrides`; `solo_block` is looking, not tuning, and stays out. `None` means never tuned — do not resolve the fps-derived default into the field. | [2026-07-27](completed-todo/2026.07.27-replicates-remember-their-settings.md) |
| Settings memory | `src/sieve/core/pipeline_model.py` `edited_params` / `edited_detector` | The two-write edit: pin the diff on the replicate being looked at, move the baseline for everyone following. Submit only the fields touched — a whole resolved view drags a deviated arena's pins into the baseline. The GUI routes through `ReplicateDocument.edit_params`/`edit_detector`; the tab's `LiveChain` is the resolved *view*, never a second store. | [2026-07-27](completed-todo/2026.07.27-replicates-remember-their-settings.md) |
| The enforcement is static, and where the hole in it is | `src/sieve/core/types.py` | Mixing two quantities is refused by pyright, not at runtime, because a runtime guard would be `reportUnnecessaryIsInstance` under strict and returning `NotImplemented` would need a loose `other: object` annotation that gives up the static check entirely. Three of the four mixtures happen to raise `AttributeError`; `MediaTime + WallTime` does not, because both carry `.seconds` — it is caught where it is written and nowhere else. A `dict[str, Any]` splat also erases the check, which is how `tests/property/test_warmup.py` passed pyright and failed at run time. | [2026-08-04](completed-todo/2026.08.04-four-numbers-four-types.md) |
| The four quantity types, and which of them may be negative | `src/sieve/core/types.py` | Only `FrameCount` is a count and only it refuses a negative value; wall time, media time and work units keep their sign because a headroom below a limit is a real reading and clamping it would make "just made it" and "made it by a mile" the same number. `FrameCount` therefore has no `__radd__`, which is what makes `sum()` over per-node warmups — the plain-sum error `source_warmup_frames` exists to prevent — unwritable rather than merely wrong. `WorkUnits` deliberately carries no conversion to `WallTime` and no `.milliseconds`: the divisor belongs to one machine and one backend, and naming it is `work-units-have-one-anchor`'s job, not this type's. | [2026-08-04](completed-todo/2026.08.04-four-numbers-four-types.md) |
| Slide versus trim | `src/sieve/core/types.py` `ROI.placed_in` | The rule that a region keeps its exact extent and moves, used by both the stamp and “Set all”. `clamped_to` is the other rule and trims. Reaching for the wrong one at the frame edge silently makes a rack non-uniform while every number on screen says it worked. | [2026-07-27](completed-todo/2026.07.27-set-all.md) |
| Why media time is rational, stated as the arithmetic that fails | `src/sieve/core/types.py`, `tests/unit/test_quantities.py` | Not accumulated drift — a float frame duration added a million times is off by 1e-6 frames, and the "drifts over a two-hour recording" argument this item was written with is false. The failure is that every media time is eventually floored onto the frame grid: at 30000/1001, `floor(float(15 / fps) * float(fps))` is 14. First failure at frame 15, one whole frame, in the first second of footage. | [2026-08-04](completed-todo/2026.08.04-four-numbers-four-types.md) |
| The source's frame rate is the container's rational, probed with PyAV | `src/sieve/decode/reader.py` `container_rate`, `src/sieve/core/types.py` `VideoMetadata.fps` | OpenCV cannot answer it: `CAP_PROP_FPS` is a double and the denominator is gone by the time it returns. Recovering it with `Fraction(value).limit_denominator(...)` was the alternative and is right for the NTSC family by luck rather than by construction — silently wrong for any rate whose denominator exceeds the limit, which is the worst kind of number to found a published timestamp on. Zero stays the honest refusal for a container that declares no rate. The probe reads the header and never a frame, which is what keeps PyAV from becoming a second decoder identity (`pyproject.toml`). | [2026-08-05](completed-todo/2026.08.05-exact-source-frame-rate.md) |
| Decode | `src/sieve/decode/reader.py`, `src/sieve/decode/prefetch.py` | Threading buys 1.61x and stops. The remaining 6x is a 47.6 MB BGR array per frame, not a threading problem. Do not treat decode speed as open without reading both decode findings — and note that every route past it changes what a pixel is and needs a cache generation. | [2026-07-26](completed-todo/2026.07.26-read-frames-faster-than-one-at-a-time.md) |
| a detection leaves the process as two tables, not one | `src/sieve/detect/tables.py` | The series is what was *measured* and the intervals are what the current `DetectorSettings` *claims* from it; a re-tune moves the second and leaves the first untouched. One wide table would make a threshold change read as a new measurement. Both are long-form, keyed by `replicate` and `node`, so a batch is one file per kind and not one directory per arena. | [2026-07-28](completed-todo/2026.07.28-detection-csv-export.md) |
| A published second is computed in exact arithmetic, and refuses when the rate is absent | `src/sieve/detect/tables.py` `_at_frame`, `_seconds` | The CSV is what somebody takes into R and lines a video up against, so `frame / float(fps)` naming the wrong frame is the failure the whole item exists to close. `NA` rather than `0.000` from a source that declares no rate (rule 6), which also removes the `ZeroDivisionError` that path used to raise. Rounding is half-to-even on the `Fraction` and not on a double, because the ties are reachable: frame 15 at 30000/1001 is 0.5005 s exactly, and which side a double falls on is a property of the approximation rather than of the number. | [2026-08-05](completed-todo/2026.08.05-exact-source-frame-rate.md) |
| exported numbers are `repr`, not a fixed-width format | `src/sieve/detect/tables.py` `_number` | These tables exist to be diffed against a later run (`docs/todo/parity-comparison-finding.md`). A format that agrees to six decimal places is a comparison that cannot fail; `repr` round-trips and `float32` widens to `float64` exactly. | [2026-07-28](completed-todo/2026.07.28-detection-csv-export.md) |
| absence at the filesystem boundary is a missing file and an `NA`, never an empty one | `src/sieve/detect/tables.py` `write_tables`, `ABSENT` | Rule 6 reaching disk. A disarmed detector writes no `intervals.csv` at all, because a header-only one is indistinguishable from armed-and-found- nothing; an *armed* run with no intervals writes the empty file, and the choice keys on armed-ness rather than row count precisely because the two collapse for every input a first implementation is tried against. `gated` is `NA` when disarmed, not `FALSE` — the detector did not decide the frame was quiet, it did not look. A project with no detector is refused before decode, since `count` is derived through the value band and so there is no series either. | [2026-07-28](completed-todo/2026.07.28-detection-csv-export.md) |
| detection columns and count axes read emitted names | `src/sieve/detect/tables.py`, `src/sieve/gui/count_plot.py` | CSV headers, generated README text, CLI reports, and count plot labels use ElementNames so a filter-owned noun has one reader-visible path. | [2026-08-05](completed-todo/2026.08.05-a-filter-names-what-it-emits.md) |
| Stateful nodes | `src/sieve/filters/background_ema.py` | A stateful node is deliberately **uncacheable**, and not for the obvious reason. Read `docs/findings/2026.07.26-stateful-output-is-not- keyed-by-what-it-is.md` before assuming a key could carry it. | [2026-07-26](completed-todo/2026.07.26-a-kernel-that-can-remember.md) |
| Block signals | `src/sieve/filters/block_signal.py` | Emits `change_energy`, `flow_speed`, and `coherence`. The solve precedes block reduction; reduce the *tensor*, then eigendecompose. The spec's drafted coherence scalar was wrong — see `docs/findings/2026.07.26-the-specs-coherence-formula-fails-its-own- test.md`. | [2026-07-26](completed-todo/2026.07.26-block-signal-extraction-filter.md) |
| "No crop" is the unbounded region WHOLE_FRAME, never None and never a frame-sized pixel box | `src/sieve/filters/crop.py` | Do not respell the identity crop as None, a zero-extent sentinel, or a box sized to the frame. Project records no source dimensions, so the schema-v6 upgrade validator and a hand-typed YAML both write graphs without ever knowing them; an unbounded region intersected with the frame is the only full-frame value either can state. | [2026-08-05](completed-todo/2026.08.05-the-crop-is-a-filter.md) |
| A crop node's ROI is denominated in that node's input space, not in source pixels | `src/sieve/filters/crop.py`, `tests/integration/test_executor_run.py` | A crop node synthesized from Replicate.roi belongs at the root, where its input space is the source's. Reading ROI's source-pixel docstring as governing every crop node puts a downstream box somewhere else on the frame. | [2026-08-05](completed-todo/2026.08.05-the-crop-is-a-filter.md) |
| GUI and CSV whole-series detection use the detect filter adapter | `src/sieve/filters/detect.py`, `src/sieve/gui/detector_worker.py`, `src/sieve/cli/detect_cmd.py` | Do not route schema-v5 centered whole-record detection through the executor's trailing windowed kernel; use the filter-owned adapter until the graph migration removes Project.detector. | [2026-08-05](completed-todo/2026.08.05-detection-is-a-filter.md) |
| A span node belongs at a leaf | `src/sieve/filters/span.md`, `src/sieve/pipeline/cache_key.py` | Its parameters are hashed like any node's, so they enter the key of everything downstream; at a root, moving the bounds recomputes the chain for frames whose pixels did not change. This is a cache argument only — the counterpart to the crop's root placement, which is a coordinate-space one. | [2026-08-05](completed-todo/2026.08.05-the-span-is-a-filter.md) |
| Lead-in passes through a selecting node | `src/sieve/filters/span.py`, `src/sieve/pipeline/plan.py` | The span kernel is the identity and decode_range still widens span by the graph's lead-in, so frames below span.start reach every node and are cut once, at the yield. A span kernel that refused or dropped out- of-range frames would leave every stateful filter downstream of it unsettled for the whole answer. | [2026-08-05](completed-todo/2026.08.05-the-span-is-a-filter.md) |
| Threshold units | `src/sieve/filters/temporal_baseline.py` | Median and MAD over a trailing window, because the events are in the sample. The estimate lags a step change by about one window; a centred one needs `Mode.WINDOWED`, which is refused. | [2026-07-26](completed-todo/2026.07.26-per-block-temporal-baseline.md) |
| Plots | `src/sieve/gui/band_plot.py` and its specializations | QPainter, not pyqtgraph, not napari. | [2026-07-26](completed-todo/2026.07.26-plot-widgets.md) |
| One drag, one undo | `src/sieve/gui/commands.py` | `SetReplicateROI` merges on a per-press token, and `mergeWith` keeps the *first* command's displaced value. A test that drags with a single mouse-move cannot see any of this — the second event is a no-op `set_roi` and the count is 1 either way. | [2026-07-27](completed-todo/2026.07.27-crop-tools.md) |
| One action, many rows | `src/sieve/gui/commands.py` `SetReplicateROIs` | Merging cannot produce this. `mergeWith` only joins commands naming the *same* row, so a loop pushing one command per replicate is one undo entry per replicate whatever token it carries. A batch edit is one command holding many rows. | [2026-07-27](completed-todo/2026.07.27-set-all.md) |
| The heat layer draws one image, not a rect per cell | `src/sieve/gui/composite_view.py` `_paint_heat` | Do not draw a cell-resolution image scaled by Qt: it re-rounds the boundaries grid_edges owns and the heat layer drifts a pixel off the ring layer and off block_at. Expansion by np.repeat over the same edge widths is what makes the two unable to disagree. | [2026-08-05](completed-todo/2026.08.05-the-heat-layer-is-drawn-once.md) |
| The three filters' `fps` params stay `float`, and the two representations meet in one place | `src/sieve/gui/filter_tab.py` `_fps` | A params field is hashed into `canonical_json`, so retyping `block_signal`, `motion_history` and `temporal_baseline`'s `fps` would re-key every entry those filters ever cached — for kernels that multiply a window by a number and want nothing exact. `float(Fraction(30000, 1001))` is the same double `CAP_PROP_FPS` used to hand over, so the conversion moves no key; `tests/integration/test_decode.py` asserts that against OpenCV rather than assuming it. | [2026-08-05](completed-todo/2026.08.05-exact-source-frame-rate.md) |
| An edit's exits release the keyboard, not just the edit | `src/sieve/gui/keyboard_handback.py` | The handback is deferred by one turn of the event loop and applies to `QAbstractSpinBox` only. Clearing focus in place delivers a focus-out, which is a commit — Esc would then write the number it was pressed to discard. Text fields are out of scope because their Enter and Esc already belong to something else (the wizard's search hands Esc up to the wizard). | [2026-08-05](completed-todo/2026.08.05-enter-and-esc-hand-the-keyboard-back.md) |
| The window's length is a preference; its position is the project's | `src/sieve/gui/preferences.py`, `src/sieve/core/clip_window.py` | Stored in seconds under `session/`, silent, and outside `restore_defaults` — the next video need not share a frame rate, and every bracket drag writes it, so a `changed` here would rebuild the decode path mid-gesture. It feeds `default_window` as an argument and never becomes a `clip`: `Project.clip = None` has to stay reachable from a second launch or `plan.py` loses its whole-video fallback. The write-back reads `document.clip`, never `document.window`, because on a source whose container states no frame rate the fallback *is* the whole asset, and remembering that would follow the user to every later file. | [2026-08-05](completed-todo/2026.08.05-window-length-outlives-the-session.md) |
| The render thread | `src/sieve/gui/preview_runner.py` | Holds a `PreviewSession` on its own thread with one pending slot. Playhead refreshes are suppressed while a window render is outstanding so they cannot displace it. | [2026-07-26](completed-todo/2026.07.26-the-first-live-graph-tick.md) |
| the source boundary is a controller, not a region of the filter tab | src/sieve/gui/source_boundary.py | that it is constructed from the document and the card and reaches the tab only through render_hold/render_stale/status_message — an extraction that needs a fourth reach has the seam in the wrong place | [2026-08-05](completed-todo/2026.08.05-the-source-boundary-is-its-own-object.md) |
| Coalescing | `src/sieve/gui/transport/coalescer.py` | Two slots, rank rule, source stamp, Qt-free. It lives in `gui/` and `pipeline/` **cannot** inherit it — the layer contract puts `gui` above `pipeline`. | [2026-07-25](completed-todo/2026.07.25-qt-free-coalescer.md) |
| Time selection | `src/sieve/gui/transport/pacing.py`, `src/sieve/gui/timeline/geometry.py` | `ReplicateDocument.clip` is the chosen span and `SetClip` is its only writer; `window` is the derived view that falls back to ten seconds. Arithmetic is Qt-free. | [2026-07-26](completed-todo/2026.07.26-the-timeline-replaces-the-transport.md) |
| `gui/state.py` was not created | `src/sieve/gui/transport/player.py` | Scrub position and playing state live in `VideoPlayer`; a separate object would duplicate them. Create it when there is UI state with no natural owner (panel layout, zoom). | [2026-07-25](completed-todo/2026.07.25-window-state-and-layout.md) |
| Crop gestures | `src/sieve/gui/video_view.py` | Handles are hit-tested *before* containment, and only on the selected box. Drawing needs travel in both axes; adjusting needs it in either — a horizontal move under the both-axes rule releases as a click and navigates away. `view_rect` is the mapping, `content_rect` is only the floor it is clamped against. | [2026-07-27](completed-todo/2026.07.27-crop-tools.md) |
| Placement | `src/sieve/gui/video_view.py` `_placed` | A stamp or a move slides against the frame edge and never trims. `ROI.clamped_to` is the *other* rule and belongs to typed numbers, not to placements. | [2026-07-27](completed-todo/2026.07.27-stamp-as-the-default-gesture.md) |
| Cache keys | `src/sieve/pipeline/cache_key.py` | Ports bind each upstream key, so `a - b` and `b - a` are two computations. Sibling branches stay valid when one is edited. | [2026-07-25](completed-todo/2026.07.25-cache-key.md) |
| Graph validation and order | `src/sieve/pipeline/dag.py` | One topological order per document, not per traversal. Take `Dag.order`; do not sort a graph again. | [2026-07-25](completed-todo/2026.07.25-dag-validation.md) |
| DAG resolves element names beside element meanings | src/sieve/pipeline/dag.py Dag.element_names | Consumers that count node outputs must read the graph-resolved names, including source pixels and preserving filters, instead of deriving nouns from ElementKind or widgets. | [2026-08-05](completed-todo/2026.08.05-a-filter-names-what-it-emits.md) |
| One walk answers whether a graph is legal, in two consumption modes | `src/sieve/pipeline/dag.py` — `Dag.validate`, `Dag._walk`, `Diagnostic` | `validate` is a classmethod over a `Pipeline`, never a method on a built `Dag`: the graph worth validating is precisely the one no `Dag` exists for, and an instance method would be the `is_valid()` that makes the checked and unchecked cases one type. A `Diagnostic` carries the `GraphError` itself rather than a message assembled beside it, which is what makes the two modes unable to word one rejection differently — the drift that a second spelling in a widget produces by construction. The five rejections keep their fixed order and the first two still stop the walk: an unresolved filter leaves later checks with no `FilterSpec` to read and a cycle leaves them with no order to read in, so collecting past either would report verdicts derived from what is already missing. For the same reason a node with a wiring verdict gets no edge verdict, and a node trips at most one wiring verdict — an edge into `rigth` leaving `right` unfilled is one typo, not two faults. | [2026-08-05](completed-todo/2026.08.05-one-definition-of-edge-legality.md) |
| Execution | `src/sieve/pipeline/executor.py` | The one loop all three front ends call. `Mode.WINDOWED`, `rate_changing`, and `emits`-more-than-one-stream are still refused at run time. Multi-upstream is **not** — it landed. | [2026-07-25](completed-todo/2026.07.25-executor.md) |
| A selection is the run's, not a branch's | `src/sieve/pipeline/plan.py` | execute yields one FrameResult per frame carrying every node's output, so a graph has one frame set and selecting nodes intersect across the whole graph. Placement therefore cannot change the result. Do not re- decide this as a per-branch or per-path selection without first changing what a FrameResult is. | [2026-08-05](completed-todo/2026.08.05-the-span-is-a-filter.md) |
| Pre-run derivation | `src/sieve/pipeline/plan.py` | Resolved params, keys, and the source lead-in as a backward max over `Dag.order`. | [2026-07-25](completed-todo/2026.07.25-executor.md) |
| Re-render on edit | `src/sieve/pipeline/preview.py` | `PreviewSession` pays only for nodes below the edit: 3.3 ms warm against 1350 ms cold. The 3 s ceiling is met by the *store*, not by anything being fast. | [2026-07-26](completed-todo/2026.07.26-the-representative-clip-preview.md) |
| Every replicate pins its own crop region; the node's baseline is WHOLE_FRAME | `src/sieve/pipeline/upgrade.py` | Not the edited_params shape of baseline-plus-deviation. Under v5 no replicate's geometry followed any other's, so a later edit to a shared baseline would move an arena that never agreed to it. The unpinned baseline also means a newly drawn arena opens on the whole frame rather than on the last one's box. | [2026-08-05](completed-todo/2026.08.05-the-migration-transform-is-built-and-proven.md) |
| Synthesized node ids are derived from the document, never generated | `src/sieve/pipeline/upgrade.py` | crop-<root> and span-<leaf>. Node.node_id defaults to a fresh uuid, so an upgrade that let it default would give two machines upgrading one file two different documents. A derived id the document already uses is refused rather than disambiguated, so one file has one upgrade. | [2026-08-05](completed-todo/2026.08.05-the-migration-transform-is-built-and-proven.md) |
| The v6 upgrade lives in pipeline/, not as a model_validator on Project | `src/sieve/pipeline/upgrade.py` | Synthesizing a node names a filter — its id, its version, and the identity value of its parameters — and core/pipeline_model.py is deliberately not registry-aware. A validator there fails tests/unit/test_filter_id_spelling.py and costs three permanent entries on a shrink-only list. Do not move it back into core; the consequence is that the v6 reader also sits above sieve.filters, since a document must be upgraded before Project sees it. | [2026-08-05](completed-todo/2026.08.05-the-migration-transform-is-built-and-proven.md) |
| GUI/CLI parity is diffed on output, never on the resolved plan | `tests/gui/test_gui_cli_parity.py` | The comparison is the per-frame series and the intervals, taken from `PreviewRunner` + `SeriesCollector` + `detector_worker.derive` on one side and `sieve detect --csv` on the other. A plan diff is the cheaper thing to write and cannot make the claim: it passes while both sides compute the same wrong thing, and it fails on every field that is legitimately "where it lives and how fast it arrives" rather than "what a result is" — rule 7's line, which the plan straddles on purpose. | [2026-08-04](completed-todo/2026.08.04-gui-cli-execution-parity.md) |
| a parity fixture has to be able to disagree with itself, and needs its own footage | `tests/gui/test_gui_cli_parity.py` | Two arenas resolving alike make every parity assertion pass against a front end that drops replicate pins entirely, so the deviation must reach the *output* and a guard test must assert that it still does. The shared `synthetic_video` cannot serve this: it is a spatially uniform ramp, so change energy is constant, the in-band count saturates at the block total, and a pinned detection window changes nothing observable. That was the first draft, and it passed. `stirred_clip` and its measured `VALUE_FLOOR` / `COUNT_FLOOR` exist for that reason and are not decoration. | [2026-08-04](completed-todo/2026.08.04-gui-cli-execution-parity.md) |
| `hypothesis` is used by four modules under `tests/property/` | `tests/property/` | The test-selection rule in `CLAUDE.md` narrows what earns one going forward; the existing four stay. | [2026-07-25](completed-todo/2026.07.25-property-tests.md) |
| the presentation channel is asserted absent from the cache key | tests/unit/test_cache_key.py::TestIsolation::test_a_presentation_edit_moves_no_key | that the sweep is scoped to presentation and not to every unhashed field, because deterministic and stateful are execution and flipping either makes node_key raise rather than return an unchanged key | [2026-08-05](completed-todo/2026.08.05-presentation-edits-move-no-key.md) |
| the partition is checked in both directions, not just for missing rows | tests/unit/test_filter_contract.py::TestFilterSpec::test_every_field_is_in_exactly_one_channel | that a new FilterSpec field without a SPEC_CHANNELS row is a test failure at the moment it is written | [2026-08-05](completed-todo/2026.08.05-the-spec-has-three-channels.md) |
| How a filter's owning module is determined | `tests/unit/test_filter_id_spelling.py` | Asked of the registered params class via `inspect.getfile`, never derived from `filters/<filter_id>.py`. The convention holds for all seven filters and nothing enforces it, so assuming it would make the check silently wrong for the first filter that breaks it. | [2026-08-04](completed-todo/2026.08.04-a-filter-id-spelled-twice.md) |
| R4 column duplicate check includes emitted-name columns | tests/unit/test_filter_id_spelling.py | Element-dependent detection columns generated from source and filter element names are part of the duplicate package spelling guardrail; the check is no longer intentionally vacuous. | [2026-08-05](completed-todo/2026.08.05-a-filter-names-what-it-emits.md) |
| What the duplicate-literal check is aimed at | `tests/unit/test_filter_id_spelling.py` | Registered filter ids and declared `TableSpec` column names only — never every string across a layer boundary. A generic two-layer detector seeds an exception list the size of the codebase (`"in"`, `"array"`, every StrEnum value), which is enumeration rot re-encoded in Python. | [2026-08-04](completed-todo/2026.08.04-a-filter-id-spelled-twice.md) |
| Where a filter-id exception list may live | `tests/unit/test_filter_id_spelling.py` | In the test, not in `src/`, which is the one place `WITHOUT_PRODUCER`'s shape does not carry over: a list of filter ids in a module under `src/sieve/` would be eleven foreign spellings of its own and would trip the check it declares. | [2026-08-04](completed-todo/2026.08.04-a-filter-id-spelled-twice.md) |
| One-call completion of unplanned work | `tools/complete_item.py` | An omitted --summary or --settled still writes the TODO marker the gate fails on; filling them from arguments is a shortcut past imitation, never past the question. A slug with no item file is now refused unless --new says so, because the silent fallback scaffolded a second entry beside an untouched item whenever the slug was mistyped. Do not mint an item for work already finished: complete_item deletes it before anything commits it, so it leaves nothing but a wrong row in files.added. | [2026-08-05](completed-todo/2026.08.05-mint-and-complete-in-one-call.md) |
| A settled row is overturned by the entry that re-decides it, by name | `tools/doc_index.py` | `overturns:` on a completed entry matches a row's `what` exactly (a typo fails the suite) and moves it to SETTLED.md's Overturned section carrying both pointers. The original entry is a dated record and is never edited — and a row stands as law until the overturning entry actually lands, so rows the rework will eventually re-decide are not pre-struck. | [2026-07-29](completed-todo/2026.07.29-the-rework-enters-the-docs.md) |
| Absorbed items are superseded, never completed or deferred | `tools/doc_index.py` | `status: superseded` plus `superseded_by:` (slugs, resolved like `after:` and gate-checked) keeps the file so inbound references — including ones in `src/` — keep resolving, and drops the item from the open, deferred, frontier, and aspiration lists. Completing absorbed scope puts unfinished work in the Completed index (rule 6 arriving through the docs); deferring it misdescribes scope as timing. | [2026-07-29](completed-todo/2026.07.29-the-rework-enters-the-docs.md) |
| How a trigger and a gate are written, so a machine can read them | `tools/guardrail_refs.py` | The state lives inside the bold — `**Trigger: FIRED**`, never `**Trigger:** FIRED` — which is what lets the same file name the construct in prose without declaring one, and a declaration miswritten in the prose shape is an error rather than a skip. FIRED carries `→ docs/todo/<slug>.md`; NOT FIRED carries a `(YYYY.MM.DD)` date and nothing else is required. A gate declaration is paragraph-initial in REWORK.md. Every one of these lines wraps across two source lines, so the text is flattened before matching — a line-oriented parser sees none of them. | [2026-07-29](completed-todo/2026.07.29-a-claimed-gate-that-does-not-exist.md) |
| How a completed entry gets its commit hash | `tools/stamp_commit.py`, `tools/githooks/post-commit` | Additively, in a second commit, never by `--amend` — amending changes the hash being stamped, and rewrites history a push may already have shipped. Scoped to the entries in `git diff-tree HEAD`, so an older `pending` entry is left alone rather than stamped with a commit it does not describe. The recursion terminates on the pending check itself: the stamp commit touches `docs/completed-todo/` again and finds nothing pending. | [2026-08-05](completed-todo/2026.08.05-the-commit-hash-writes-itself.md) |

*90 rows from 52 entries; 63 entries settled nothing.*
