<!-- Machinery, not an ADR: the one home for group titles. A `position`'s
first pair must name a line here; an unused group is allowed and renders as
nothing. Lines are `NN — Title`, and everything else in the file is prose. -->

01 — The tool contract
02 — The saved artifact
03 — The repo itself
04 — The executor
05 — The GUI
