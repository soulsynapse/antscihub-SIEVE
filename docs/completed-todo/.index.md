<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Completed

One file per finished TODO item, newest first. Each records what moved, what was decided, and what was rejected along the way.

| Date | Item | Commit | Summary |
|---|---|---|---|
| 2026-07-27 | [The selected replicate is the one being tuned](2026.07.27-the-selected-replicate-is-the-one-being-tuned.md) | bb7238e | The filter tab now renders whichever replicate is selected instead of hard-coding row 0, the selection has exactly one owner (the document), and clicking a box on the replicate tab's video accepts it — selects, re-renders, and lands the user on the filter tab with that arena under them. |
| 2026-07-27 | [The rules revisited](2026.07.27-the-rules-revisited.md) | 75ee2a6 | The five non-negotiables were rewritten against the code that exists: one was vacuous, one was contradicted by the document that stated it, and the most load-bearing invariant in the system had no slot at all. |
| 2026-07-27 | [The graphs fill as the window renders](2026.07.27-the-graphs-fill-as-the-window-renders.md) | 7db406b | The detector now derives on a thread of its own and repaints the graphs from the prefix of the working window rendered so far, so the count, scalogram and density surfaces fill left to right while frames land instead of appearing all at once at render_finished — with a settled frontier that stops detections from ever being drawn and then withdrawn. |
| 2026-07-27 | [Set all](2026.07.27-set-all.md) | 83098c6 | The replicate tab can now give every box one size in a single action, held about each box's own centre and slid rather than trimmed at the frame edge, as one undo entry however many arenas it touched. |
| 2026-07-27 | [Rules carry their falsifiers](2026.07.27-rules-carry-their-falsifiers.md) | d6e7a46 | Every binding rule now states the objective it serves (O1-O4, defined at the top of ARCHITECTURE.md) and its falsifier — the pre-stated condition under which revising the rule, not obeying it, is correct; budgets gained perceptual anchors, a reference-workload scope clause, and a declared-debt mechanism the benchmark gate honors visibly and the runtime HUD never does. |
| 2026-07-27 | [Replicates remember their settings](2026.07.27-replicates-remember-their-settings.md) | 6250905 | Tuning is now per-replicate with lateral inheritance: every filter-tab edit pins its diff on the arena being looked at and moves the shared baseline, the detector gained a home in the artifact (schema v3), and a saved project reopens with each arena resolving to exactly the settings it was tuned with. |
| 2026-07-27 | [Migrate LATER.md into item files](2026.07.27-migrate-later-into-item-files.md) | pending | LATER.md's seventeen deferred sections became seventeen `docs/todo/` items with machine-readable triggers, so deferred work now lives in the same folder, index, and promotion path as open work, and `LATER.md` is gone. |
| 2026-07-27 | [Doc lifecycle machinery](2026.07.27-doc-lifecycle-machinery.md) | 0e7ca57 | Items are now one file each in docs/todo/ (open and deferred alike, trigger in frontmatter), completion is a literal move via complete_item.py, truth-claiming prose carries drift stamps that doc_drift.py reports on, and VISION/REFINED-VISION/SIEVE-HANDOFF/the parity plan are frozen as dated records. |
| 2026-07-27 | [Crop tools: the stamp, the drag, and the magnifier](2026.07.27-crop-tools.md) | 3efce56 | The replicate tab's boxes can now be placed rather than only drawn: scroll to magnify about the cursor with a hard floor at the natural fit, drag the selected box or its eight handles to move and resize it as one undo step, stamp a remembered size that survives placement exactly, and read or type any of it in a tools pane that also says what source the arenas were cut from. |
| 2026-07-27 | [Agent workflow tooling](2026.07.27-agent-workflow-tooling.md) | c5c3abb | Session wall-clock is now measured rather than guessed (transcript miner plus a finding), the completion ceremony's mechanical half is scaffolded by one command, bug bullets and unfinished entries are machine-checked, and a generated docs/.state.md orients a session in one read. |
| 2026-07-26 | [Wavelet and detection math](2026.07.26-wavelet-and-detection-math.md) | c447057 | The v1 Morlet transform and detection chain now exist as Qt-free, cv2-free pure functions in core/, so the live tab, a future whole-video pass, and the parity check can all compute the same numbers by calling the same code. |
| 2026-07-26 | [Three small GUI fixes](2026.07.26-three-small-gui-fixes.md) | b2108f9 | A rename the document refused now reads as refused in the table, a preference rewritten to the value it already held no longer re-signals on the launch after it was stored, and the display-proxy cache is named for what it holds rather than for what every cache holds. |
| 2026-07-26 | [The wizard](2026.07.26-the-wizard.md) | 4b15bcc | A seam click or a card's swap now opens the near-full-window inset helper: candidates judged against the seam with disable reasons, a provisional step that sits in the real chain as a dashed card and renders for real, hover frames through the single-frame path, per-model settings over guidance parsed from the filter's own markdown, and an Add/Cancel pair where Cancel restores the snapshot value exactly. |
| 2026-07-26 | [The timeline replaces the transport](2026.07.26-the-timeline-replaces-the-transport.md) | 18d765f | The window now has one anchor: a full-width band below the tabs that spans the whole asset, carries a working window that holds its length under every edit, and bounds the transport it drives. |
| 2026-07-26 | [The step composite](2026.07.26-the-step-composite.md) | c5b0300 | The filter tab now shows which pixels the selected step touched: clicking a card in the chain stack selects that step, and a composite pane draws the step's output alpha-blended over its input at one opacity — VISION step 4's three-way overlay collapsed to a single view. |
| 2026-07-26 | [The representative-clip preview](2026.07.26-the-representative-clip-preview.md) | c72b6c8 | A graph can now be re-run over the working window with one parameter changed and pay only for the nodes below the edit — 3.3 ms where the cold render was 1350 ms — and `sieve preview` renders it headless with both in-pipeline budgets reported and optionally enforced. |
| 2026-07-26 | [The metric bus](2026.07.26-the-metric-bus.md) | b9a0e6a | A timed interval now has somewhere to go and someone to hear it, so a budget can be missed by something other than a measurement taken by hand once. |
| 2026-07-26 | [The graph HUD](2026.07.26-the-graph-hud.md) | 62724d0 | The per-frame render cost is drawn: gui/graph_hud.py plots each frame of the working window's cost with the playhead as its cursor, and the bus's whole-render verdicts are painted somewhere a user actually looks — red on a miss — instead of reaching only a status bar and a test. |
| 2026-07-26 | [The first live graph tick](2026.07.26-the-first-live-graph-tick.md) | 066473b | The GUI computes a frame for the first time: a PreviewSession runs on a render thread, reports what each frame of the working window cost keyed by its source index, and publishes filter_to_first_tick — the last in-pipeline budget that had a ceiling and no producer other than the one gated on a parameter control. |
| 2026-07-26 | [Preprocessing filters](2026.07.26-rescale-and-normalize-filters.md) | b9a25a9 | The live tab's two spatial-prep steps exist as pipeline filters: rescale (v1's float linear scale, INTER_AREA) and normalize (off \| zscore to mean 128 / sd 32), each with wizard-facing guidance markdown. |
| 2026-07-26 | [Read frames faster than one at a time](2026.07.26-read-frames-faster-than-one-at-a-time.md) | 2bad973 | A span is now decoded on N threads instead of one, for a measured 1.61x — 24.50 ms/frame to 15.25 — with every frame byte-identical to what the sequential reader returns, and the thread count reaching a run as `--workers` rather than as anything in the project document. |
| 2026-07-26 | [Plot widgets (filter-tab parity item 5)](2026.07.26-plot-widgets.md) | 272cd7a | The live tab's four detection views exist as QPainter widgets — a shared band-plot base carrying the mockup-pinned gesture contract, the scalogram, the block-density plot, the windowed-count plot, and the block-heat panel — and pyqtgraph and napari are out of the gui extra. |
| 2026-07-26 | [Per-frame delivery and the single-frame path](2026.07.26-per-frame-delivery-and-single-frame-path.md) | a5824e8 | A caller of PreviewRunner can now receive every FrameResult on the render thread and ask for a single frame; SeriesCollector assembles one node's outputs into the (T, ny, nx) series the detector runs on, discarding superseded revisions by construction. |
| 2026-07-26 | [Per-block temporal baseline](2026.07.26-per-block-temporal-baseline.md) | 9bcb52d | A threshold can now be denominated in a block's own deviations rather than in the lighting rig's intensity units, and a filter whose lead-in is a parameter can charge for the lead-in it actually needs instead of the worst case over its whole legal range. |
| 2026-07-26 | [Open and save a project](2026.07.26-open-and-save-a-project.md) | a2a5ae3 | Replicates, the representative clip, and the graph now survive the window: the GUI reads and writes the `.sieve.yaml` artifact through File > Open Project / Save / Save As, tracks unsaved work off the undo stack, and offers the project filed beside a video when one is opened directly. |
| 2026-07-26 | [Multi-upstream kernels](2026.07.26-multi-upstream-kernels.md) | 17d2768 | A node may now take more than one upstream: edges name the input port they feed, a merging filter declares its ports on `accepts`, `MergingKernel` is the calling convention for them, and the executor hands such a node a frame per port — so the temporal chain's two-input discriminants are buildable. |
| 2026-07-26 | [Coherence as a third block signal](2026.07.26-coherence-as-a-third-block-signal.md) | 1314973 | block_signal now emits coherence — how much of a block's change one translation explains, in [0, 1] — so "high change_energy, low coherence" separates grooming from walking with no state and no time constants. |
| 2026-07-26 | [Chain model and the stack UI](2026.07.26-chain-stack-and-filter-tab.md) | 54a9b6d..e66a8ab | The Filter tab exists: the live chain draws as a card stack with stage headers, captions, hover swap/x, conflict repair, and seam affordances; the tab renders its runnable prefix through PreviewRunner, collects the block series, and derives the detector graphs with the two-tier drag discipline. |
| 2026-07-26 | [Block-signal extraction filter](2026.07.26-block-signal-extraction-filter.md) | ec4bf77 | The live tab's two per-block signals — change energy (Jtt) and Lucas-Kanade flow speed — exist as one stateful pipeline filter emitting (ny, nx) GRAY float32 grids, with block/grid resolution defined in exactly one place. |
| 2026-07-26 | [A kernel that can remember](2026.07.26-a-kernel-that-can-remember.md) | 6da53fd | A streaming kernel can now keep state across frames, and background_ema is the first filter that does — which gives the warmup arithmetic a consumer for the first time and makes the lead-in something other than decoded and discarded. |
| 2026-07-25 | [Window state and layout](2026.07.25-window-state-and-layout.md) | e2452d0 | The window now comes up maximized, reopens the video from the previous session when that file is still where it was, and splits its top half in two so the right side is available for the per-frame tools that do not exist yet. |
| 2026-07-25 | [Stale frame on reopen](2026.07.25-stale-frame-on-reopen.md) | 6456307 | A frame decoded against a video that has since been closed or replaced is now discarded on arrival instead of being painted into the new source's viewport and cached under its index. |
| 2026-07-25 | [Scrub budget and Preferences](2026.07.25-scrub-budget-and-preferences.md) | 4b2431a | The scrub budget miss is closed by measurement rather than by amendment: both remaining escape hatches were probed and shut, the ceiling moved to 100 ms, and it is now enforced by degrading to a coarse grid instead of by hoping. |
| 2026-07-25 | [Representative clip range](2026.07.25-representative-clip-range.md) | 7853ce7 | The user can mark the five-to-ten-second span they tune against — in and out points at the playhead, painted under the transport, undoable, and clamped to the source — so `Project.clip` finally has something in the GUI that sets it. |
| 2026-07-25 | [Replicate tab](2026.07.25-replicate-tab.md) | 6937776 | The repo can open a video, draw and edit named ROI boxes on it, scrub and play it, and undo any of that — with the layer boundaries that keep the GUI out of the science enforced by the quality gate. |
| 2026-07-25 | [Replicate equivalence groups](2026.07.25-replicate-equivalence-groups.md) | 9450b88 | The replicate table now carries a Group column saying which arenas run the same thing, derived from `resolved_params` on every read and never stored. |
| 2026-07-25 | [Qt-free coalescer](2026.07.25-qt-free-coalescer.md) | 825dd15 | The one-in-flight-one-pending decode discipline is now an object rather than fifty lines welded to `QImage` and `Signal`: `RequestCoalescer` owns the two slots, the rank rule, the display ordering, and the source stamp, so `pipeline/preview.py` inherits the behaviour the budget table pins instead of reimplementing it. |
| 2026-07-25 | [Pyright strict](2026.07.25-pyright-strict.md) | 3abdcfc | `nox -s typecheck` is clean at strict with no ignores and no per-directory relaxation, and one of the eight errors turned out to be a live bug in ChannelSpec. |
| 2026-07-25 | [Property tests or drop Hypothesis](2026.07.25-property-tests.md) | b7e4f5d | `tests/property/` now holds the four generated-input invariants that justify the Hypothesis dependency, and one of them found a real defect in default replicate naming. |
| 2026-07-25 | [Pipeline artifact](2026.07.25-pipeline-artifact.md) | 729c2be | `core/pipeline_model.py` holds the artifact a run is reproducible from — a source video, its replicates, a tuning clip, a filter DAG, its checkpoints, and its declared outputs — serialized to YAML, validated structurally, and deliberately unable to resolve a filter id. |
| 2026-07-25 | [Per-replicate parameter deviation](2026.07.25-per-replicate-parameter-deviation.md) | 4ff33c6 | A replicate can now pin any subset of a node's parameters and inherit the rest from a default that moves to the most recently configured values, so twelve arenas are configured once unless one of them needs to differ. |
| 2026-07-25 | [Per-node backend](2026.07.25-per-node-backend.md) | 3f1d95e | A plan now carries a backend per node rather than one for the whole graph, and derives each node's cache key from the backend that will actually produce it — so a chain containing one CPU-only filter can still run its other nodes on GPU. |
| 2026-07-25 | [GUI tests](2026.07.25-gui-tests.md) | e09b8bf | The Replicate tab's edit paths are under test — 85 pytest-qt tests covering undo/redo, model routing, letterboxed coordinate mapping and the editor guard, three of which were proven to fail by deliberate breakage. |
| 2026-07-25 | [First filter and discovery](2026.07.25-first-filter-and-discovery.md) | 45702d6..bbdc3df | A filter now exists, registers itself by being importable, and is found by a package scan with nothing listing it — so guardrail §3 is machine-checked rather than aspirational, and `sieve.filters` and `sieve.backend` are real layers instead of parenthesised ones. |
| 2026-07-25 | [Filter contract](2026.07.25-filter-contract.md) | da40c1c | `core/` now holds the filter contract as data — `FilterSpec`, `ParamsBase`, `ArraySpec`, `Mode`, and a registry keyed by `(filter_id, version)` — so a saved pipeline can be validated structurally with no filters installed and no codec present. |
| 2026-07-25 | [Filter contract: rate, stream kind, output size](2026.07.25-filter-contract-rate-stream-kind-output-size.md) | d5e9a6e | The three things VISION asks of filters that `FilterSpec` could not express are now declarable: a filter can say its output stops being indexed like its input, an edge can carry rows instead of frames, and a checkpoint's on-disk size can be predicted before it is written. |
| 2026-07-25 | [Executor](2026.07.25-executor.md) | e46ef92 | A validated graph can now be run: the repo decodes a span of a real video, crops it per replicate, walks the DAG applying kernels, caches every deterministic result, and hands back one frame per node per source frame — with the temporal warmup ahead of the span computed and discarded. |
| 2026-07-25 | [DAG validation](2026.07.25-dag-validation.md) | bf41114 | A saved pipeline can now be told whether it is runnable and in what order: `Dag.build` resolves every node against the registry, rejects cycles and edges whose declared types cannot chain, and produces the one topological order — and the one cache-key walk — the executor will take. |
| 2026-07-25 | [Cache key](2026.07.25-cache-key.md) | 893c010 | Two computations can now be asked whether they are the same computation: `node_key` derives a content hash from a node's resolved parameters, its ancestry, and the backend, and guardrail §5 falls out of the derivation rather than being enforced on top of it. |
| 2026-07-25 | [Build the CLI](2026.07.25-build-the-cli.md) | 8eadaa1 | `sieve inspect` and `sieve run` exist, so the executor now has a caller with no toolkit, no decode thread, and no display proxy — a front end that cannot produce a frame except through `pipeline/executor.py`. |
| 2026-07-25 | [Benchmark gate measures nothing](2026.07.25-benchmark-gate.md) | cee90cd | `nox -s benchmark` now measures two budgets against a running player instead of skipping the whole suite and exiting 0, and deleting the checks breaks the session rather than quietly greening it. |

*51 entries.*
