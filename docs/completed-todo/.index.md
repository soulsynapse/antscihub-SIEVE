<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Completed

One file per finished TODO item, newest first. Each records what moved, what was decided, and what was rejected along the way.

| Date | Item | Commit | Summary |
|---|---|---|---|
| 2026-07-25 | [Window state and layout](2026.07.25-window-state-and-layout.md) | e2452d0 | The window now comes up maximized, reopens the video from the previous session when that file is still where it was, and splits its top half in two so the right side is available for the per-frame tools that do not exist yet. |
| 2026-07-25 | [Stale frame on reopen](2026.07.25-stale-frame-on-reopen.md) | 6456307 | A frame decoded against a video that has since been closed or replaced is now discarded on arrival instead of being painted into the new source's viewport and cached under its index. |
| 2026-07-25 | [Scrub budget and Preferences](2026.07.25-scrub-budget-and-preferences.md) | 4b2431a | The scrub budget miss is closed by measurement rather than by amendment: both remaining escape hatches were probed and shut, the ceiling moved to 100 ms, and it is now enforced by degrading to a coarse grid instead of by hoping. |
| 2026-07-25 | [Replicate tab](2026.07.25-replicate-tab.md) | 6937776 | The repo can open a video, draw and edit named ROI boxes on it, scrub and play it, and undo any of that — with the layer boundaries that keep the GUI out of the science enforced by the quality gate. |
| 2026-07-25 | [Replicate equivalence groups](2026.07.25-replicate-equivalence-groups.md) | 9450b88 | The replicate table now carries a Group column saying which arenas run the same thing, derived from `resolved_params` on every read and never stored. |
| 2026-07-25 | [Qt-free coalescer](2026.07.25-qt-free-coalescer.md) | 825dd15 | The one-in-flight-one-pending decode discipline is now an object rather than fifty lines welded to `QImage` and `Signal`: `RequestCoalescer` owns the two slots, the rank rule, the display ordering, and the source stamp, so `pipeline/preview.py` inherits the behaviour the budget table pins instead of reimplementing it. |
| 2026-07-25 | [Pyright strict](2026.07.25-pyright-strict.md) | 3abdcfc | `nox -s typecheck` is clean at strict with no ignores and no per-directory relaxation, and one of the eight errors turned out to be a live bug in ChannelSpec. |
| 2026-07-25 | [Property tests or drop Hypothesis](2026.07.25-property-tests.md) | b7e4f5d | `tests/property/` now holds the four generated-input invariants that justify the Hypothesis dependency, and one of them found a real defect in default replicate naming. |
| 2026-07-25 | [Pipeline artifact](2026.07.25-pipeline-artifact.md) | 729c2be | `core/pipeline_model.py` holds the artifact a run is reproducible from — a source video, its replicates, a tuning clip, a filter DAG, its checkpoints, and its declared outputs — serialized to YAML, validated structurally, and deliberately unable to resolve a filter id. |
| 2026-07-25 | [Per-replicate parameter deviation](2026.07.25-per-replicate-parameter-deviation.md) | 4ff33c6 | A replicate can now pin any subset of a node's parameters and inherit the rest from a default that moves to the most recently configured values, so twelve arenas are configured once unless one of them needs to differ. |
| 2026-07-25 | [GUI tests](2026.07.25-gui-tests.md) | e09b8bf | The Replicate tab's edit paths are under test — 85 pytest-qt tests covering undo/redo, model routing, letterboxed coordinate mapping and the editor guard, three of which were proven to fail by deliberate breakage. |
| 2026-07-25 | [First filter and discovery](2026.07.25-first-filter-and-discovery.md) | 45702d6..bbdc3df | A filter now exists, registers itself by being importable, and is found by a package scan with nothing listing it — so guardrail §3 is machine-checked rather than aspirational, and `sieve.filters` and `sieve.backend` are real layers instead of parenthesised ones. |
| 2026-07-25 | [Filter contract](2026.07.25-filter-contract.md) | da40c1c | `core/` now holds the filter contract as data — `FilterSpec`, `ParamsBase`, `ArraySpec`, `Mode`, and a registry keyed by `(filter_id, version)` — so a saved pipeline can be validated structurally with no filters installed and no codec present. |
| 2026-07-25 | [Filter contract: rate, stream kind, output size](2026.07.25-filter-contract-rate-stream-kind-output-size.md) | d5e9a6e | The three things VISION asks of filters that `FilterSpec` could not express are now declarable: a filter can say its output stops being indexed like its input, an edge can carry rows instead of frames, and a checkpoint's on-disk size can be predicted before it is written. |
| 2026-07-25 | [Executor](2026.07.25-executor.md) | e46ef92 | A validated graph can now be run: the repo decodes a span of a real video, crops it per replicate, walks the DAG applying kernels, caches every deterministic result, and hands back one frame per node per source frame — with the temporal warmup ahead of the span computed and discarded. |
| 2026-07-25 | [DAG validation](2026.07.25-dag-validation.md) | bf41114 | A saved pipeline can now be told whether it is runnable and in what order: `Dag.build` resolves every node against the registry, rejects cycles and edges whose declared types cannot chain, and produces the one topological order — and the one cache-key walk — the executor will take. |
| 2026-07-25 | [Cache key](2026.07.25-cache-key.md) | 893c010 | Two computations can now be asked whether they are the same computation: `node_key` derives a content hash from a node's resolved parameters, its ancestry, and the backend, and guardrail §5 falls out of the derivation rather than being enforced on top of it. |
| 2026-07-25 | [Benchmark gate measures nothing](2026.07.25-benchmark-gate.md) | cee90cd | `nox -s benchmark` now measures two budgets against a running player instead of skipping the whole suite and exiting 0, and deleting the checks breaks the session rather than quietly greening it. |

*18 entries.*
