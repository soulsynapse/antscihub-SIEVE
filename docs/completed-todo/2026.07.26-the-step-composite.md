---
# ---- identity -------------------------------------------------------------
title: The step composite
date: 2026-07-26
commit: c5b0300
tags: [gui, in-pipeline, filter-tab, vision-step-4]

summary: >
  The filter tab now shows which pixels the selected step touched: clicking a
  card in the chain stack selects that step, and a composite pane draws the
  step's output alpha-blended over its input at one opacity — VISION step 4's
  three-way overlay collapsed to a single view.

# ---- what moved -----------------------------------------------------------
files:
  added:
    - src/sieve/gui/composite_view.py
    - tests/gui/test_step_composite.py
  changed:
    - src/sieve/gui/filter_tab.py
    - src/sieve/gui/chain_stack.py
    - src/sieve/gui/block_heat.py
    - docs/TODO.md
  removed: []

tests:
  added: 3
  total: 524
  gates: [lint, typecheck, imports, tests, docs]

# ---- the load-bearing part ------------------------------------------------
decisions:
  - what: the three modes collapsed to one composite with a selected step
    why: raw video is the first step's composite at full opacity, full state is
      the tail selected; only "contribution of this step" carries information
      the scalar plots cannot (argued in the TODO item, which was the design)
  - what: the target is the deepest node-backed ok step at or before the selection
    why: tab-side steps (morlet, windowed count) have no rendered frame; the
      caption says "(deepest rendered)" so the fallback is visible, not silent
  - what: the composite pair rides window renders and otherwise uses request_frame,
      suppressed while a window render is outstanding
    why: the runner holds one pending slot; a playhead refresh stream would
      displace the graphs' render from it and the series would never arrive
  - what: composite refreshes neither clear the HUD nor feed it costs
    why: a store-warm single frame is near-zero cost — begin() per playhead move
      would empty the window series and add_cost would overwrite real render
      costs with cache-hit times
  - what: block-grid overlays colorize with the heat panel's warm stop, scaled by
      the window series' 99.5th percentile, painted nearest-neighbour
    why: same colour = same quantity across panels; fixed scale keeps cell
      visibility meaning one thing at every playhead; smoothing would invent
      gradients between blocks the data does not contain
  - what: the first step's input is the player frame cropped to the replicate ROI
    why: the graph runs over the crop when a replicate exists, and an overlay on
      footage the graph never saw would misalign by construction

rejected:
  - what: napari (and any N-layer viewer) for the overlay
    why: the collapsed requirement is two layers and one slider — QPainter;
      closes the deferred napari question for good (Deferred decisions)
  - what: retaining per-frame node outputs over the window for playhead scrubs
    why: two full-resolution nodes over a 300-frame window is on the order of a
      gigabyte; a store-warm request_frame is milliseconds

# ---- consequences ---------------------------------------------------------
docs: [docs/TODO.md]
---

# The step composite

The demand the item named — two frames from *different nodes* at the same
source index — is met by indexing `FrameResult.outputs`, never by a second
render. The grabber is `filter_tab`'s wizard grabber pattern verbatim: a
consumer on the render thread drops `(input array, output array, is-grid)`
into a one-slot mailbox, and `_on_render_finished` converts on the GUI
thread. The display path never feeds the graph
(`docs/findings/2026.07.25-the-crop-belongs-in-the-graph.md` still holds).

**Selection is a stack concept now.** `StepCard` emits `select_clicked` on a
press and paints an accent bar when selected — emit-never-apply, like solo on
the heat panel; the tab owns the selection, keeps it sticky by id across
rebuilds, and defaults it to the tail so the first thing a user sees is the
full current state.

**What was checked by mutation.** Deleting the `_series_pending` guard in
`_refresh_composite` fails
`test_a_playhead_refresh_never_displaces_a_pending_window_render` (a playhead
move mid-render submits a frame request that would displace the graphs'
window render). The HUD test fails if either the `begin()` skip or the
`_on_frame_cost` drop for composite revisions is removed.

**Outside the item's scope.** `block_heat.py`'s `_HEAT` became public `HEAT`
so the composite could colour grids with the same warm stop rather than
importing a private name.

**Not exercised end-to-end here but covered by riding:** the real-render
composite path (grab → convert → `set_frames`) runs inside
`test_filter_tab.py`'s knob-burst test, which renders the synthetic fixture
through the real runner with the composite grabber attached to the same
consumer.
