---
title: Seeker upgrades
date: 2026-07-27
commit: pending
tags: [gui, timeline]

summary: >
  The working window is now dragged on the band itself — edge handles resize,
  the header band moves it whole, one undo entry per drag — and a hover bubble
  names the frame under the cursor in timecode and in frames.

files:
  added: []
  changed:
    - docs/TODO.md
    - src/sieve/gui/document.py
    - src/sieve/gui/timeline_bar.py
    - src/sieve/gui/timeline_model.py
    - tests/gui/qt_input.py
    - tests/gui/test_timeline.py
    - tests/unit/test_timeline_model.py
  removed:
    - mockups/seeker/README.md
    - mockups/seeker/renders/lanes-drag.png
    - mockups/seeker/renders/lanes-hover.png
    - mockups/seeker/renders/lanes-none.png
    - mockups/seeker/renders/split-drag.png
    - mockups/seeker/renders/split-hover.png
    - mockups/seeker/renders/split-none.png
    - mockups/seeker/seeker_bar.py

decisions:
  - what: >
      A handle drag is two new signals on the strip — `window_moved(origin)`
      and `window_resized(start, end)` — wired straight to two document verbs,
      rather than one signal carrying the resolved span.
    why: >
      A header drag holds the window's length and so *is* the edit
      `move_window_to` already performs; only a handle drag names both edges,
      and that is the one case needing the new `place_window`. Collapsing them
      would have made every window drag read "Resize Window" in the Edit menu,
      including the ones that resized nothing.
  - what: >
      The one-second floor lives in `timeline_model.started_at` /
      `ended_at_handle` and is enforced by the strip, not by the document.
    why: >
      `end_window_at` — the keyboard out-mark — is allowed to name a single
      frame, and always was. The floor is a property of a *drag*, where the
      cursor passes continuously through every shorter span on its way, not of
      the window. Putting it in the document would have silently changed what
      Mark Out means.
  - what: >
      A drag past the floor stops at the floor rather than being refused.
    why: >
      The item said "refused rather than committed", which is true of what
      reaches the document — nothing shorter than a second is ever written. A
      handle that went dead under a still-moving cursor would read as a broken
      widget; one that stops reads as a limit. This is also why the left handle
      uses `started_at` and not `ended_at`, whose release-the-origin rule is
      right for a keystroke that would otherwise do nothing and wrong for a
      grip the user is still holding.
  - what: >
      The bubble sits below the header band, not at the top of the track, and
      is not painted at all during a drag.
    why: >
      It follows the cursor, and the header is what the cursor is usually
      approaching; a readout that covers the handle it is guiding you to is
      worse than one a few pixels lower. During a drag the thing being dragged
      already answers "where am I".
  - what: >
      `STRIP_HEIGHT` stays at 44 px; the mockup's 64 is not adopted yet.
    why: >
      The 64 was sized for the detection ticks and coverage lanes, which have
      no producer (`docs/todo/coverage-and-detection-lanes.md`). The header
      band is nine pixels and fits inside 44 with room to spare, and twenty
      pixels of empty band is twenty pixels the graphs do not get.

rejected:
  - what: >
      Giving `SetClip` a `mergeWith`/`id()` so a per-move write collapses into
      one undo entry.
    why: >
      It makes the history correct only after the fact and makes every
      intermediate span a real document state — the player is bounded by the
      window, so each one would re-bound playback and re-render. The local
      draft is cheaper and is the two-tier rule the rest of this UI already
      follows for band drags.
  - what: >
      Re-classifying the grab on every mouse-move instead of once on press.
    why: >
      The window moves under the cursor during the drag, so a drag that
      re-asked what it was holding could change from a resize to a move
      halfway through, at the moment the opposite edge slid past the pointer.
---

# Seeker upgrades

The bracket half of parity item 8. The tick and coverage half is
`docs/todo/coverage-and-detection-lanes.md` and is unchanged by this.

## What the tests pin, and why each one

Five distinct failures, each with its own test rather than one drag test
asserting five things:

- **A press on an edge emits no `pressed`.** `pressed` *is* the seek, so a
  strip that classified after emitting would throw the playhead to the window's
  start on the way to resizing from the left.
- **An edge wins over the body it sits in.** A point on the edge is inside the
  window too; containment-first makes the handles unreachable and leaves the
  user resizing by typing. Same rule `video_view.py` settled for crop handles.
- **A press below the header is still a scrub**, which is the mirror claim —
  the window covers most of the band, and a window that swallowed presses would
  make the band unseekable wherever the user is working.
- **Four moves push nothing; the release pushes one.** `SetClip` has no
  `mergeWith`, so this is the whole of what stands between a window drag and an
  undo entry per pixel.
- **A drag to 5 px of the origin lands at 30 frames**, not at 5 — the floor is
  a duration at 30 fps, and the arithmetic that computes it is tested against
  numbers in `tests/unit/test_timeline_model.py` where it needs no widget.

Two existing tests moved from `y=10` to `y=30`: at ten pixels they now land in
the header band, which is a window move rather than a seek. They passed
unchanged (a header press-and-release at one point moves the window nowhere, so
the document sees nothing either way) — which is exactly why they had to move.
A test that passes because both readings agree is testing neither.

## Outside the item's scope

`docs/TODO.md`'s bottom-bar bullet said the loaded seeker "was never built";
it now says which half is, and where the other half went.

## Not committed with this

`src/sieve/filters/block_signal.py` carries an uncommitted edit from another
session — `AUTO_BLOCK_SOURCE_PX` 64 → 16 — which fails
`test_block_resolution_is_the_one_source_of_grid_truth` and
`test_captions_restate_the_values_the_chain_actually_holds`. It is untouched
and unstaged here. Every other test passes; the two failures are that constant
and nothing else.
