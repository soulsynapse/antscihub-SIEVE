---
title: The exception list is the work list
date: 2026-07-29T12:18:58-07:00
commit: "70b5f37"
tags: [rework, guardrails]

summary: >
  REWORK.md R1 binds through CI: the `gui-computes-nothing` forbidden contract
  landed in `.importlinter` with the seven real GUI-imports-computation edges
  as a shrink-only exception list, AUTO-GUARDRAILS §9 records it, and
  `.state.md` renders the list's size so the rework's burn-down is visible in
  the file every session reads.

settled:
  - what: The placement contract and its shrink-only exception discipline
    where: "`.importlinter`"
    do_not_redecide: >
      `allow_indirect_imports` stays True — after detection-is-a-filter,
      `gui -> pipeline -> filters -> core.wavelet` is the supported path, and
      forbidding indirect imports would fire on the architecture the contract
      exists to reach. `unmatched_ignore_imports_alerting` stays `error` —
      that is the shrink-only mechanism (deleting code and its exception are
      one edit), and adding an ignore entry is a reviewed widening of the
      rework, never a fix.

files:
  added: []
  changed:
    - .importlinter
    - docs/AUTO-GUARDRAILS.md
  removed: []
---
