---
title: A filter names what it emits
date: 2026-08-05T20:23:30-07:00
commit: "pending"
tags: []

summary: >
  Filters now declare column-safe names for concrete emitted elements, the
  DAG resolves those names beside element meaning, and detection tables plus
  count plots read the resolved declaration instead of local block or pixel
  vocabulary.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: emitted element names are identity contract data
    where: src/sieve/core/filter_base.py ElementNames FilterSpec.element_names SPEC_CHANNELS
    do_not_redecide: >
      A concrete ElementKind must carry column-safe singular and plural
      names, relation declarations preserve or lose upstream names, and the
      field is identity rather than presentation because it names exported
      artifacts.
  - what: DAG resolves element names beside element meanings
    where: src/sieve/pipeline/dag.py Dag.element_names
    do_not_redecide: >
      Consumers that count node outputs must read the graph-resolved names,
      including source pixels and preserving filters, instead of deriving
      nouns from ElementKind or widgets.
  - what: detection columns and count axes read emitted names
    where: "`src/sieve/detect/tables.py`, `src/sieve/gui/count_plot.py`"
    do_not_redecide: >
      CSV headers, generated README text, CLI reports, and count plot labels
      use ElementNames so a filter-owned noun has one reader-visible path.
  - what: R4 column duplicate check includes emitted-name columns
    where: tests/unit/test_filter_id_spelling.py
    do_not_redecide: >
      Element-dependent detection columns generated from source and filter
      element names are part of the duplicate package spelling guardrail;
      the check is no longer intentionally vacuous.

files:
  added: []
  changed:
    - docs/AUTO-GUARDRAILS.md
    - docs/REWORK.md
    - src/sieve/cli/detect_cmd.py
    - src/sieve/core/filter_base.py
    - src/sieve/core/filter_registry.py
    - src/sieve/detect/tables.py
    - src/sieve/filters/block_signal.py
    - src/sieve/gui/chain_model.py
    - src/sieve/gui/count_plot.py
    - src/sieve/gui/filter_tab.py
    - src/sieve/gui/wizard.py
    - src/sieve/pipeline/dag.py
    - tests/gui/test_detect_plots.py
    - tests/integration/test_cli_detect.py
    - tests/unit/test_dag.py
    - tests/unit/test_detect_tables.py
    - tests/unit/test_filter_contract.py
    - tests/unit/test_filter_id_spelling.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
