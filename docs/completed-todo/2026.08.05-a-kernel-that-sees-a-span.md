---
title: A kernel that sees a span — Mode.WINDOWED runs
date: 2026-08-05T21:02:47-07:00
commit: "420f596"
tags: []

summary: >
  Single-input Mode.WINDOWED filters now register span-taking kernels and
  execute over a bounded FrameSpan, while rate-changing, table, and windowed
  multi-input shapes remain refused by name.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: Single-input windowed kernel shape
    where: "`src/sieve/backend/dispatch.py`, `src/sieve/pipeline/executor.py`"
    do_not_redecide: >
      A windowed kernel is registered with @windowed_kernel, receives a
      FrameSpan, and must return the frame for FrameSpan.target; the
      executor sizes that span as refined warmup_frames plus the current
      frame.
  - what: Windowed cache boundary
    where: "`src/sieve/core/filter_base.py`, `src/sieve/pipeline/cache_key.py`"
    do_not_redecide: >
      Mode.WINDOWED specs are not cacheable under the existing node-key plus
      source-index store, because provisional lead-in outputs depend on the
      run frontier until founded spans enter the cache contract.

files:
  added: []
  changed:
    - src/sieve/backend/dispatch.py
    - src/sieve/core/__init__.py
    - src/sieve/core/filter_base.py
    - src/sieve/core/types.py
    - src/sieve/pipeline/cache_key.py
    - src/sieve/pipeline/executor.py
    - tests/unit/test_backend_dispatch.py
    - tests/unit/test_cache_key.py
    - tests/unit/test_declarable_shapes.py
    - tests/unit/test_executor.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
