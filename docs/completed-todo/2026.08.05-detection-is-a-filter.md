---
title: Detection is a filter emitting a per-frame channel
date: 2026-08-05T21:36:58-07:00
commit: "pending"
tags: []

summary: >
  Detection now has a filter-owned full-series adapter, and both the GUI
  derivation path and sieve detect --csv route through DetectParams without
  moving the existing centered-window or Morlet-edge semantics.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: GUI and CSV whole-series detection use the detect filter adapter
    where: "`src/sieve/filters/detect.py`, `src/sieve/gui/detector_worker.py`, `src/sieve/cli/detect_cmd.py`"
    do_not_redecide: >
      Do not route schema-v5 centered whole-record detection through the
      executor's trailing windowed kernel; use the filter-owned adapter
      until the graph migration removes Project.detector.
  - what: The gui-computes-nothing exception list shrinks when the adapter owns detector work
    where: "`.importlinter`"
    do_not_redecide: >
      The stale direct GUI imports of sieve.detect and detector_worker's
      wavelet ops stay removed; adding them back is a reviewed widening of
      REWORK R1.

files:
  added: []
  changed:
    - .importlinter
    - docs/AUTO-GUARDRAILS.md
    - docs/REWORK.md
    - src/sieve/cli/detect_cmd.py
    - src/sieve/filters/detect.md
    - src/sieve/filters/detect.py
    - src/sieve/gui/chain_model.py
    - src/sieve/gui/detector_worker.py
    - src/sieve/gui/filter_tab.py
    - tests/integration/test_cli_detect.py
    - tests/unit/test_chain_model.py
    - tests/unit/test_detect_filter.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
