---
title: The drift report is an audit, not a per-commit check
date: 2026-08-05T19:01:58-07:00
commit: "b6fe145"
tags: []

summary: >
  The forty-line drift report moved out of `nox -s docs` into its own `nox
  -s drift` session; `docs` now prints one line saying how many docs and
  findings want a revisit. doc_drift.py grew `--summary` over a single
  `report()` pass, so the count and the list it summarizes cannot disagree.
  Nothing about its report-never-gate behaviour changed.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: where the drift report is printed
    where: noxfile.py `drift` session; `docs` prints `doc_drift.py --summary` only
    do_not_redecide: >
      `docs` runs after every completed item, so a report long enough to
      scroll away a piped verdict does not belong in it

files:
  added:
    - tests/docs/test_doc_drift.py
  changed:
    - CLAUDE.md
    - README.md
    - noxfile.py
    - tools/doc_drift.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
