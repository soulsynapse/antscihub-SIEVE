---
title: Mint and complete in one call
date: 2026-08-05T18:55:36-07:00
commit: "pending"
tags: []

summary: >
  complete_item.py now writes a finished entry in one call: --new declares
  that no docs/todo/ item exists (and refuses when one does), and --summary
  / --settled fill the two fields that were previously filled by opening a
  neighbouring entry and imitating it.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: One-call completion of unplanned work
    where: "`tools/complete_item.py`"
    do_not_redecide: >
      An omitted --summary or --settled still writes the TODO marker the
      gate fails on; filling them from arguments is a shortcut past
      imitation, never past the question. A slug with no item file is now
      refused unless --new says so, because the silent fallback scaffolded a
      second entry beside an untouched item whenever the slug was mistyped.
      Do not mint an item for work already finished: complete_item deletes
      it before anything commits it, so it leaves nothing but a wrong row in
      files.added.

files:
  added:
    - tests/docs/test_complete_item.py
  changed:
    - CLAUDE.md
    - tools/complete_item.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
