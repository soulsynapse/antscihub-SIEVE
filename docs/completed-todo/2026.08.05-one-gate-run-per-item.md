---
title: One gate run per item
date: 2026-08-05T19:05:33-07:00
commit: "bd7ff2a"
tags: []

summary: >
  The work loop rebuilds the doc indexes before running the gate instead of
  after it, so ox -s checks runs once per item rather than twice;
  complete_item's printed checklist and the noxfile docstring carry the same
  order and the argument against folding docs into checks.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: gate/rebuild order in the work loop
    where: CLAUDE.md work loop steps 7-8, noxfile.py docs docstring, tools/complete_item.py _ALWAYS
    do_not_redecide: >
      nox -s docs precedes nox -s checks, and docs is never made a
      prerequisite of checks - regenerating inside the gate makes
      tests/docs/test_doc_index.py compare fresh output against itself,
      deleting the only staleness check CI runs

files:
  added: []
  changed:
    - CLAUDE.md
    - noxfile.py
    - tools/complete_item.py
    - tools/stamp_commit.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
