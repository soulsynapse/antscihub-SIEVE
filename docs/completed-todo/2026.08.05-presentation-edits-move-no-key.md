---
title: A presentation edit moves no cache key
date: 2026-08-05T19:29:39-07:00
commit: "pending"
tags: []

summary: >
  A test in tests/unit/test_cache_key.py substitutes a different legal value
  for every field SPEC_CHANNELS places in Channel.PRESENTATION and asserts
  node_key returns the same string, with the substitutes table checked
  against SPEC_CHANNELS so a fourth presentation field is covered by the row
  that declares it.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: the presentation channel is asserted absent from the cache key
    where: tests/unit/test_cache_key.py::TestIsolation::test_a_presentation_edit_moves_no_key
    do_not_redecide: >
      that the sweep is scoped to presentation and not to every unhashed
      field, because deterministic and stateful are execution and flipping
      either makes node_key raise rather than return an unchanged key

files:
  added: []
  changed:
    - tests/unit/test_cache_key.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
