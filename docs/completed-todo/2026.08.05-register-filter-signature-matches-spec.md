---
title: register_filter's signature is FilterSpec's field list
date: 2026-08-05T19:35:12-07:00
commit: "pending"
tags: []

summary: >
  register_filter's keyword list is pinned to FilterSpec's fields by set
  equality, and each keyword is probed with a non-default value to prove
  decorate's body forwards it. The three hand-maintained copies stay; the
  tests hold them in step.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: the decorator keeps a typed, hand-written signature
    where: src/sieve/core/filter_registry.py + tests/unit/test_filter_contract.py TestDecoratorMatchesSpec
    do_not_redecide: >
      do not rebuild register_filter around **kwargs or a generated
      signature — it would erase the per-keyword static types at the one
      place a filter author is checked; the drift is tested for instead

files:
  added: []
  changed:
    - tests/unit/test_filter_contract.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
