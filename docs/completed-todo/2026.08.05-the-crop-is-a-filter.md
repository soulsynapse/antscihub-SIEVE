---
title: The crop is a filter
date: 2026-08-05T22:08:19-07:00
commit: "9920b17"
tags: []

summary: >
  Crop is a discovered filter (spec, CPU kernel, guidance markdown)
  reachable through sieve run on a hand-built YAML graph, with the identity
  crop spelled as an unbounded ROI that clamps to whatever frame arrives.
  The schema does not move: the GUI keeps writing Replicate.roi, and an
  integration test pins the two paths to the same pixels.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: "\"No crop\" is the unbounded region WHOLE_FRAME, never None and never a frame-sized pixel box"
    where: "`src/sieve/filters/crop.py`"
    do_not_redecide: >
      Do not respell the identity crop as None, a zero-extent sentinel, or a
      box sized to the frame. Project records no source dimensions, so the
      schema-v6 upgrade validator and a hand-typed YAML both write graphs
      without ever knowing them; an unbounded region intersected with the
      frame is the only full-frame value either can state.
  - what: A crop node's ROI is denominated in that node's input space, not in source pixels
    where: "`src/sieve/filters/crop.py`, `tests/integration/test_executor_run.py`"
    do_not_redecide: >
      A crop node synthesized from Replicate.roi belongs at the root, where
      its input space is the source's. Reading ROI's source-pixel docstring
      as governing every crop node puts a downstream box somewhere else on
      the frame.

files:
  added:
    - src/sieve/filters/crop.md
    - src/sieve/filters/crop.py
    - tests/unit/test_crop.py
  changed:
    - docs/SCAFFOLD.md
    - tests/integration/test_cli_run.py
    - tests/integration/test_executor_run.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
