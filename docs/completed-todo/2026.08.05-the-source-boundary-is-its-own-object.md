---
title: The source boundary is its own object
date: 2026-08-05T19:17:39-07:00
commit: "5f4905b"
tags: []

summary: >
  The source boundary left filter_tab.py as gui/source_boundary.py: a
  QObject over the ReplicateDocument and the SourceCard, holding the four-
  state wording, the materialize gesture, the write-completion handlers, the
  discard, the MaterializeRunner and the seven document connections. 213
  lines out of a 2,388-line widget; the tab keeps a construct, three signal
  connections, a shutdown forward and the materializer property. The pause
  and the file release merged into one render_hold(bool) so the discard's
  ordering is guaranteed by statements inside a slot rather than by two
  connections the caller must sequence.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: the source boundary is a controller, not a region of the filter tab
    where: src/sieve/gui/source_boundary.py
    do_not_redecide: >
      that it is constructed from the document and the card and reaches the
      tab only through render_hold/render_stale/status_message — an
      extraction that needs a fourth reach has the seam in the wrong place
  - what: holding the preview for a write means pausing AND releasing the files, together
    where: FilterTab._on_boundary_hold
    do_not_redecide: >
      that the two are one signal delivered synchronously; a queued
      connection or a split into two emits puts the unlink back in a race
      with the release, which is the bug where discard silently did nothing

files:
  added:
    - src/sieve/gui/source_boundary.py
  changed:
    - docs/SCAFFOLD.md
    - docs/todo/filter-tab-is-eleven-jobs.md
    - src/sieve/gui/filter_tab.py
    - tests/gui/test_crop_boundary.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
