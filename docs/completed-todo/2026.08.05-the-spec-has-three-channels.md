---
title: The spec has three channels, and every field is in exactly one
date: 2026-08-05T19:26:54-07:00
commit: "8bacbeb"
tags: []

summary: >
  core/filter_base.py declares a Channel enum
  (identity/execution/presentation) and SPEC_CHANNELS placing all fifteen
  FilterSpec fields in exactly one, with a test asserting the partition is
  total in both directions so a field added without a row fails when it is
  written.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: FilterSpec's fields are partitioned into three declared channels
    where: src/sieve/core/filter_base.py Channel, SPEC_CHANNELS
    do_not_redecide: >
      that presentation is its own channel rather than part of the unhashed
      remainder, that cost/primary_params/summary are presentation, and that
      backend_agnostic is identity because it decides whether backend
      identity leaves the digest
  - what: the partition is checked in both directions, not just for missing rows
    where: tests/unit/test_filter_contract.py::TestFilterSpec::test_every_field_is_in_exactly_one_channel
    do_not_redecide: >
      that a new FilterSpec field without a SPEC_CHANNELS row is a test
      failure at the moment it is written

files:
  added: []
  changed:
    - src/sieve/core/filter_base.py
    - tests/unit/test_filter_contract.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
