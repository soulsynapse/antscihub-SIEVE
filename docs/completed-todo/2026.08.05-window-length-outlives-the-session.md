---
title: Window length outlives the session
date: 2026-08-05T18:18:57-07:00
commit: "pending"
tags: [gui, timeline, preferences]

summary: >
  The working window now opens at the length the user last worked in rather
  than at ten seconds: the length is remembered as a machine preference in
  seconds and fed to the fallback, while the clip itself stays unchosen.

settled:
  - what: The window's length is a preference; its position is the project's
    where: "`src/sieve/gui/preferences.py`, `src/sieve/core/clip_window.py`"
    do_not_redecide: >
      Stored in seconds under `session/`, silent, and outside
      `restore_defaults` — the next video need not share a frame rate, and
      every bracket drag writes it, so a `changed` here would rebuild the
      decode path mid-gesture. It feeds `default_window` as an argument and
      never becomes a `clip`: `Project.clip = None` has to stay reachable
      from a second launch or `plan.py` loses its whole-video fallback. The
      write-back reads `document.clip`, never `document.window`, because on a
      source whose container states no frame rate the fallback *is* the whole
      asset, and remembering that would follow the user to every later file.

files:
  added: []
  changed:
    - src/sieve/core/clip_window.py
    - src/sieve/gui/document.py
    - src/sieve/gui/main_window.py
    - src/sieve/gui/preferences.py
    - tests/gui/test_preferences.py
    - tests/gui/test_timeline.py
    - tests/unit/test_clip_window.py
  removed: []
---
