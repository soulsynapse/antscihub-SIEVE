---
title: Frame shrinks before BGR
date: 2026-08-06T04:44:17-07:00
commit: "d53e78f"
tags: []

summary: >
  The reference decode-lowering probe is recorded: FFmpeg crop/scale/gray
  plus current block_signal takes about 1.6 s versus v2's about 6.9 s, and a
  follow-on source-contract item is open because the route changes source
  identity.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: The FFmpeg-lowered route is worth implementing on the reference preview workload
    where: "`docs/findings/2026.08.06-working-frame-before-python-buys-the-cold-fill.md`"
    do_not_redecide: >
      The measured cold full-window cost is about 6.9 s today and about 1.6
      s with FFmpeg crop/scale/gray plus block_signal, so do not close the
      gap as too small without a newer measurement.
  - what: A lowered decode path is a new source identity, not a transparent VideoReader optimization
    where: "`docs/findings/2026.08.06-working-frame-before-python-buys-the-cold-fill.md`, `src/sieve/pipeline/cache_key.py`"
    do_not_redecide: >
      The pixel comparison changed nearly every sampled pixel and the odd
      origin probe changed 7808 of 15912 working pixels without exact=1;
      include the lowered route and prefix in the source ancestor key and
      keep FFmpeg crop exact=1.

files:
  added:
    - docs/findings/2026.08.06-working-frame-before-python-buys-the-cold-fill.md
    - docs/todo/ffmpeg-lowered-source-contract.md
  changed: []
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
