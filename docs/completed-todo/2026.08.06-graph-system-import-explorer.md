---
title: "graph-system: a scope-based browser navigator for the import graph"
date: 2026-08-06T19:13:48-07:00
commit: "pending"
tags: []

summary: >
  A standalone browser tool in graph-system/: extract.py (stdlib AST walk
  over src/sieve) writes data.js with the containment tree, module import
  edges carrying imported names, intra-module symbol edges, layer bands
  parsed live from .importlinter, SCAFFOLD annotations, and Projected
  ghosts; viewer.html (self-contained, vendored d3, opens from file://)
  gives every scope three views - Internals, Interior with outside collapsed
  to terminals, Boundary with independent upstream/downstream depth dials -
  plus hub-fade, edge-weight, layer-band, and __init__ noise cuts, with all
  view state in the URL hash. Only ruff sees the folder; pyright, import-
  linter, pytest, and the scaffold test do not.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled: none

files:
  added:
    - graph-system/
  changed: []
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
