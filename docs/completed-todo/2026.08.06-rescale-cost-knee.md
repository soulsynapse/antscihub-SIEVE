---
title: Rescale cost knee
date: 2026-08-06T04:05:39-07:00
commit: "8b1675d"
tags: []

summary: >
  The filter tab now fits rescale wall cost from completed preview renders,
  with one-scale observations held as provisional and the measured knee
  recorded in docs/findings.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: Rescale wall-time fits are runtime calibration
    where: >
      `src/sieve/gui/rescale_cost.py`; `src/sieve/gui/filter_tab.py`;
      `docs/findings/2026.08.06-rescale-cost-knee.md`
    do_not_redecide: >
      Treat observed F/M coefficients as per-machine, per-footage preview
      calibration; do not move them into CostEstimate or a reusable
      per-filter coefficient table.
  - what: One-scale rescale observations are provisional
    where: >
      `src/sieve/gui/rescale_cost.py` RescaleCostFit;
      `src/sieve/gui/filter_tab.py` _refresh_rescale_cost
    do_not_redecide: >
      Do not show milliseconds per frame or a knee until at least two
      distinct scales have completed in the same context.

files:
  added:
    - docs/findings/2026.08.06-rescale-cost-knee.md
    - src/sieve/gui/rescale_cost.py
    - tests/unit/test_rescale_cost.py
  changed:
    - src/sieve/gui/filter_tab.py
    - tests/gui/test_step_composite.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
