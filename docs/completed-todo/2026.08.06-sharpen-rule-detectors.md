---
title: Sharpen rule detectors
date: 2026-08-06T05:45:44-07:00
commit: "7061128"
tags: []

summary: >
  Rule 3 now reads as a detector for filter-owned surface outside the
  filter, Rule 6 now requires refused controls to name the reason, and the
  named-requirement obligation is already met by the wizard candidate reason
  path and tests rather than by new presentation debt.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: Rule summaries state diagnostics, not weaker promises
    where: docs/ARCHITECTURE.md rules 3 and 6; CLAUDE.md eight-rules summary
    do_not_redecide: >
      Rule 3 fires when adding a filter requires editing existing surface,
      and Rule 6 requires a refused control to name the refusal rather than
      merely appear disabled.

files:
  added: []
  changed:
    - CLAUDE.md
    - docs/ARCHITECTURE.md
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
