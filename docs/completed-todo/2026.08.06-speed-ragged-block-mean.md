---
title: Speed ragged block mean
date: 2026-08-06T03:39:49-07:00
commit: "11e08bc"
tags: []

summary: >
  The block_signal reducer now handles finite ragged planes with v1's core,
  bottom-strip, right-strip, and corner slab route, preserving partial-edge
  means and using the old NaN-padded nanmean route only when the input field
  carries NaN.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: Ragged block mean
    where: "`src/sieve/filters/block_signal.py`"
    do_not_redecide: >
      Finite fields reduce by reshape-view core plus bottom, right, and
      corner slabs. Do not restore whole-plane NaN padding for ordinary
      partial geometry; the NaN-padded nanmean route is only the fallback
      for input NaNs.

files:
  added:
    - docs/findings/2026.08.06-ragged-block-mean-costs-like-divisible.md
  changed:
    - src/sieve/filters/block_signal.py
    - tests/unit/test_block_signal.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
