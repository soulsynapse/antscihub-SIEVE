---
title: The overnight batchs gates are audited
date: 2026-08-06T17:59:13-07:00
commit: "b2187ea"
tags: []

summary: >
  Seven defects from the nine unaudited 2026-08-06 items are fixed: the
  settling gate compared a windowed run against itself on an all-NaN array,
  the work-unit anchor was denominated per frame while every declaration
  divides per megapixel, decode_start's documented clamp could raise, the
  cache-policy decision derived twice, the mutual layer was declared as not-
  yet-existing while a core docstring claimed a forbidden import, and the
  wizard's signal boundary was checked one way.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: The work unit is a megapixel copy
    where: src/sieve/core/types.py WORK_UNIT_ANCHOR
    do_not_redecide: >
      Do not restate the anchor as a full-frame copy or name a reference
      resolution; every CostEstimate divides per megapixel, so the anchor
      must be denominated there or calibration is off by the frame size.
  - what: A settling probe must have something to compare
    where: tests/property/test_filter_settling.py
    do_not_redecide: >
      Do not count an all-NaN output as agreement and do not clamp both
      origins to the same span; a filter the generic probe cannot compare
      enters WITHOUT_SETTLING_PRODUCER with its reason, never passes
      silently.
  - what: A refusal's reason is read off cache_policy
    where: src/sieve/pipeline/cache_key.py
    do_not_redecide: >
      Do not re-test FilterSpec fields to build the message; the three
      disqualifications are ordered, so message and decision must come from
      the one derivation.

files:
  added:
    - docs/findings/2026.08.06-the-gate-that-compares-a-run-to-itself.md
    - docs/todo/a-filter-declares-the-extents-it-emits.md
  changed:
    - .importlinter
    - src/sieve/core/ops/wavelet.py
    - src/sieve/core/types.py
    - src/sieve/filters/background_ema.md
    - src/sieve/filters/downsample.py
    - src/sieve/pipeline/cache_key.py
    - src/sieve/pipeline/plan.py
    - tests/gui/test_wizard.py
    - tests/property/test_filter_settling.py
    - tests/unit/test_plan.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
