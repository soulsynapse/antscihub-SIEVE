---
title: The wizard lifecycle leaves filter_tab
date: 2026-08-06T06:04:19-07:00
commit: "pending"
tags: []

summary: >
  Moved StepWizard session ownership out of FilterTab and into
  WizardLifecycle: the lifecycle now owns open/close, rollback state,
  provisional id, and the wizard frame mailbox, while FilterTab wires the
  named lifecycle signals for rendering, document writes, plot gestures, and
  detector controls. Added the wizard seam test that pins the crossing
  signal list and the no-back-reference condition.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: wizard lifecycle boundary
    where: src/sieve/gui/wizard_lifecycle.py
    do_not_redecide: >
      WizardLifecycle owns StepWizard lifetime, rollback state, provisional
      id, and the wizard frame mailbox; FilterTab wires the named lifecycle
      signals and keeps only tab-owned render/document/plot responses.

files:
  added:
    - src/sieve/gui/wizard_lifecycle.py
  changed:
    - docs/SCAFFOLD.md
    - src/sieve/gui/filter_tab.py
    - tests/gui/test_wizard.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
