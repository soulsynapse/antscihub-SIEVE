---
title: The project file is what the screen says, unless the user says otherwise
date: 2026-08-06T02:15:59-07:00
commit: "pending"
tags: []

summary: >
  Project write-through now defaults on through a Preferences checkbox; each
  undoable GUI edit quietly updates the project artifact on the history
  debounce, while disabling the preference preserves Ctrl+S as the commit
  point.

# Rows a later item must not re-decide, or the word `none`. Generated into
# docs/SETTLED.md; this is the only thing that writes it. Answering `none` is
# the common case and is a real answer — leaving the marker is not.
settled:
  - what: Project write-through defaults on as a GUI preference
    where: >
      `src/sieve/gui/preferences.py`,
      `src/sieve/gui/preferences_dialog.py`,
      `src/sieve/gui/main_window.py`
    do_not_redecide: >
      Do not add a second explicit commit concept for CLI or HPC here; users
      who need Ctrl+S as the project-file commit point disable the
      preference.
  - what: Automatic project writes share the history debounce but not explicit Save's UI
    where: >
      `src/sieve/gui/main_window.py` MainWindow._write_automatic_state,
      MainWindow._write_project_through
    do_not_redecide: >
      Do not route per-edit project writes through the modal/status explicit
      save branch.

files:
  added: []
  changed:
    - src/sieve/gui/main_window.py
    - src/sieve/gui/preferences.py
    - src/sieve/gui/preferences_dialog.py
    - tests/gui/test_preferences.py
    - tests/gui/test_project_io.py
  removed: []

# Uncomment only for a choice that would otherwise be re-argued.
# decisions:
#   - what:
#     why:
# rejected:
#   - what:
#     why:
---
