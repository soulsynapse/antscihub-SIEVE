<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Findings

One file per measurement that shaped a decision, newest first. `verdict` is the whole result — open the file for the method.

| Date | Finding | Status | Verdict |
|---|---|---|---|
| 2026-08-06 | [Working-size frames before Python buy the cold fill](2026.08.06-working-frame-before-python-buys-the-cold-fill.md) | open | On the reference project, v2's current luma path still spends about 6.9 s filling 1033 preview frames because it decodes 5312x2988 frames before cropping and downsampling; an FFmpeg crop=...:exact=1, area-scale, gray pipe over the same 1034 source frames plus the same block_signal work takes about 1.6 s. The route is worth buying: it saves about 5.3 s and moves the cold fill under the 3 s budget, but it is a source-contract change because the pixels differ and the FFmpeg process must declare its share of the machine. |
| 2026-08-06 | [The rest of the overnight batch, and the gate that compares a run to itself](2026.08.06-the-gate-that-compares-a-run-to-itself.md) | open | The nine overnight items not covered by `2026.08.06-tested-guards-that-nothing-reaches.md` were read against their own completed entries. The same defect class recurs and its worst instance is a test rather than a parameter: `tests/property/test_filter_settling.py` certified `detect` — the shelf's only WINDOWED filter — by comparing a run against itself, on an all-NaN array, under `equal_nan=True`. Two independent vacuities in one assertion, and `nox -s checks` could see neither. Seven defects are fixed; six questions that need a decision about what a module owns are recorded open below. |
| 2026-08-06 | [The overnight batch's defects are guards nothing reaches](2026.08.06-tested-guards-that-nothing-reaches.md) | open | Six overnight items were audited by reading the code they landed rather than by running it. All six built what their entries claim. The recurring defect is not broken code but *unreached* code: a guard with a test and no production caller (`lowering.protected_nodes`), a runtime sampler that does not reproduce the control its own probe ran under (rescale cost and cache reuse), and geometry duplicated out of the filters that own it with nothing tying the two copies together. The reuse defect is fixed; the other two are open. |
| 2026-08-06 | [Rescale cost is a local fit, not a filter table](2026.08.06-rescale-cost-knee.md) | closed | A completed-preview fit over one generated 960x540 fixture measured t(s) = F + M*s^2 with F = 0.233 ms/frame, M = 1.997 ms/frame, and a knee at scale 0.341; one-scale observations still cannot show a numeric price. |
| 2026-08-06 | [Ragged block mean costs like divisible geometry](2026.08.06-ragged-block-mean-costs-like-divisible.md) | closed | On this checkout, the old NaN-padded ragged `_block_mean` route measured 3.1979 ms on a 730x1300 float32 plane at block 16; the slab route measures 0.4161 ms, a 7.68x speedup. A comparable evenly divisible 736x1296 plane measures 0.4071 ms through the same new function, so ragged geometry is now 1.02x the divisible cost instead of 8.15x the old divisible baseline. The finite-field result is not bit-identical to the padded oracle because the mean is reassociated; max absolute delta in the timed sample is exactly 3.0517578125e-05. NaN-bearing fields fall back to the old `nanmean` route and remain bit-identical. |
| 2026-08-06 | [History orders filter_tab seams backwards](2026.08.06-history-orders-filter-tab-seams-backwards.md) | closed | The outline of `filter_tab.py` and the commit history rank its seams in opposite orders: the source boundary looked least like a job from the outline but shipped cleanly, while the step composite looked most like a job and has been struck as a non-seam. Current co-change counts still show `composite_view.py` tied to the tab, and a replay of blame just before the source-boundary extraction still separates the source boundary's confined commits from the composite's 66% commit. |
| 2026-08-06 | [Block-size retention does not pay for its share](2026.08.06-block-size-retention-does-not-pay.md) | closed | On the largest local ant clip SIEVE currently opens in this checkout (630x846, 30 fps), a hot block edit over the 300-frame default window costs 0.68-0.74 s and performs zero source reads; only the stateful `block_signal` node recomputes. Retaining the one pixel-resolution blurred `change_energy` plane would reduce that edit to 0.09-0.15 s, but the retained plane consumes 639,576,000 bytes for the 10 s window, leaving only 1.7 MB under a render-ring-sized honest share on this machine. Multi-product signals do not fit the same window. Do not build retention now. |
| 2026-08-05 | [What the deferred detector node keeps alive, and why it cannot be proven the way the crop was](2026.08.05-what-the-deferred-detector-node-keeps-alive.md) | open | Deferring the detect node is not one item slipping. It leaves detection as the one thing a project computes from outside the graph — rules 1 and 2's standing exception — and four ranked items plus three shrink-only `ignore_imports` entries sit behind it. The deeper cost is that the instrument that proved the crop and span migrations is unavailable here by construction: frame-for-frame equivalence cannot hold across a trailing/ centered change, so the detector migration must be *characterized* rather than *tested to zero difference*, and that characterization is buildable now and gated on nothing. |
| 2026-08-05 | [Three things the graph migration cannot do as it was written](2026.08.05-three-things-the-graph-migration-cannot-do-as-written.md) | open | The v6 flip cannot be a `model_validator(mode="before")` on `Project`, and it cannot synthesize a detect node. Synthesizing a node means naming a filter, which `core/pipeline_model.py` is deliberately blind to and `test_filter_id_spelling.py` gates; the detect filter's kernel is trailing and per-target while a saved `detector` field means centered whole-record semantics, so a synthesized detect node computes something else; and `DetectParams.fps` is a source fact no document records. The crop and the span have none of these problems and are carried, tested, in `pipeline/upgrade.py`. Revised 2026-08-05, same day, from re-reading what each obstruction licenses. Two of the three have answers and the third was drawn too wide: the reader is `pipeline/upgrade.py`'s own `load_project` and `Project.load` goes; the fps a synthesized detect node needs is probed off the container by that reader, not recovered from a sibling node; and *refusing a detector-bearing document* is not what "cannot synthesize a node" buys — it strands every project anyone has tuned. See the three `closed` rows below and `2026.08.05-what-the-deferred-detector-node-keeps-alive.md` for what the deferral costs over time. |
| 2026-08-05 | [The swap axis is built, and what stops it is identity rather than effort](2026.08.05-the-swap-axis-is-built-and-constrained-by-identity.md) | open | Interchangeable implementations chosen for the machine already exist here, at the kernel and not at the op: `@kernel(Params, Backend)` registers per `(filter_id, version, backend)`, `KernelRegistry.select` picks the first with both a kernel and a runtime, and `NoKernelError` separates "not written" from "not runnable here". What is deliberately absent is *adjudication at run time*: the executor asks `select` for the plan's backend as a one-element tuple, because backend identity is hashed for every filter that is not `backend_agnostic`. So the swap is legal exactly when two implementations produce the same numbers, `backend_agnostic` is the declaration that says so, and no filter in the tree can honestly make it. The axis is unexercised because of what it costs, not because nobody built it. |
| 2026-08-05 | [The composite grid overlay cost cells, not pixels](2026.08.05-the-grid-overlay-costs-cells-not-pixels.md) | closed | The step composite's grid overlay was priced per cell rather than per pixel, and the heat layer is the one that pays it on every cell: at B = 16,384 a repaint cost 34.2 ms with the heat slider up and 0.2 ms with it at zero, entirely independent of how many cells were detected. The pane repaints per frame and per hover crossing, so that was a 30 fps ceiling on the GUI thread bought with one slider. Indexing a 256-entry ramp table and expanding cells to pixels with `np.repeat` over the same `grid_edges` widths the ring layer reads makes it 2.2 ms, and flat in the block count: 8x the cells cost 1.2x the layer, because it now scales with the pane and not the grid. The ring/fill layer is still per cell and still scales with the *detected* count — 0.8 ms at none, 4.2 ms at 5%, 23.0 ms at 40% of B = 16,384 — which is affordable in the regime detection is tuned for and is the next thing to break if it ever is not. |
| 2026-08-05 | [The freeze during a render is the fold, not the detector](2026.08.05-render-fed-playback-folds-onto-one-frame.md) | open | Playback stalling while the graphs fill is render-fed playback's fold, not the detector: `feed_bounds` clamps the window to `frontier + 1`, so early in a render the span is one or two frames and the playhead runs at the render's frame rate instead of the wall clock. This is the fold working as designed and as tested — the defect is the judgment, not the code, so the test that pins it has to move with any fix. |
| 2026-07-28 | [The worker optimum moves with core class, and a core count cannot say so](2026.07.28-the-worker-optimum-moves-with-core-class.md) | closed | The prefetch optimum is not flat and it is not one number. Across worker counts the best reading varies 2.33x on performance cores and 2.11x on efficiency cores, so there is real gradient for a controller to act on — and the *location* of the optimum moves with core class: 2 workers on P-cores (7.96 ms/frame), 3 on E-cores (9.71) and 3 unpinned (9.04). More workers partly compensates for slower cores, which is why the P/E gap is 1.67x at one worker and only 1.22x at each side's own optimum. `resolve_workers` is denominated in `available_cpus()`, a count in which a P-core and an E-core are the same unit, so no value of `LUMA_WORKER_CAP` is right on both. |
| 2026-07-28 | [The session has no floor, it has a window](2026.07.28-the-session-floor-is-the-window.md) | open | There is no machine-independent RSS floor to measure. An idle session with a large working window climbed 3.39 -> 4.72 GB over ~95 s and then released; the same session idle with a shrunk window sat flat at 1.61-1.71 GB for over 330 s. Session memory is a function of window extent, which is user-chosen and unbounded, while memory_reserve() scales its guess with total RAM. The formula models the wrong variable, and it fails hardest on small machines, where the quantity it reserves for does not shrink. |
| 2026-07-28 | [The luma path has almost nothing left to thread](2026.07.28-the-luma-path-has-almost-nothing-left-to-thread.md) | closed | The four-worker optimum does not survive the luma path, and it moves the wrong way: two workers, 6.52 ms/frame against a sequential 8.49 — 1.30x, where the colour path measured on the same machine in the same session gets 1.44x — and four workers is 7.88 ms, 21% worse than two. Declining the convert removes most of what the interleave was overlapping, so the reasoning that a 3x smaller buffer would admit more workers is backwards: there is less parallel work, not more room for it. INFERRED_WORKER_CAP was wrong on this path in the harmful direction and LUMA_WORKER_CAP = 2 replaces it. |
| 2026-07-28 | [The density rebuild is linear in block-frames, and buys one octave below auto](2026.07.28-the-density-rebuild-buys-one-octave-below-auto.md) | closed | `DensityPlot.set_series` costs 5.1 us per thousand (frame, block) pairs and is linear across two decades of B, so the 100 ms `density_rebuild` ceiling lands at B = 16,384 over a 600-frame window (84 ms measured) — confirming the provisionally-stated bound rather than moving it. What that bound *buys* is narrower than it looks: on the full reference frame the auto grid is 3,901 blocks and the smallest admitted block edge is 32, so a user gets exactly one halving below auto before the control refuses. The refusal itself costs 4.5 ms at B = 32,768 (one `max()` pass) against the seconds the rebuild would have. |
| 2026-07-28 | [The density headroom is under the machine's own variation](2026.07.28-the-density-headroom-is-under-the-machines-own-variation.md) | open | The `density_rebuild` miss was not collection state and not a regression: fresh processes on the reference workstation read 89.3-99.7 ms at B = 16,384 against a 100 ms ceiling, where the finding that set the bound measured 84.1 ms the same morning. The headroom is smaller than the machine's own state-to-state variation, so no statistic and no retry policy can adjudicate it — the ceiling itself is the open question. |
| 2026-07-28 | [The decode format is free to derive](2026.07.28-the-decode-format-is-free-to-derive.md) | closed | `graph_needs_chroma` over a three-node chain costs a median of 8.9 us, p95 13.2 us, max 77.5 us. The render thread called it twice per render, inside `full_preview_render` — 18 us against a span measured in seconds. The cost half of docs/todo/the-decode-format-has-six-derivations.md is not a reason to do anything, and the item said to find that out before assuming otherwise. |
| 2026-07-28 | [The crop artifact is FFV1, and the qp0 route serves wrong pixels](2026.07.28-the-crop-artifact-is-ffv1.md) | open | FFV1 wins the replicate-crop codec on every axis at once: byte-identical through the unchanged VideoReader in both formats, 0.09 ms/frame sequential (107x the source's luma decode), 3.9 ms median seek (21x the source's 82 ms), the smallest files (48.4 MB gray / 72.8 MB BGR for the 77 s reference crop), and sub-2 s encodes. The pre-stated favourite, lossless H.264 -qp 0, is refuted twice: at default GOP its seeks cost 66 ms — barely better than the source — and its gray variant comes back wrong on every frame, because OpenCV's luma path hands a right-shaped wrong-pixeled array for H.264/yuvj420p that the reader's shape guard accepts. The encoder is blameless (ffmpeg round-trips it byte-exact); the failure is decode-side and silent, which is why the writer's read-back verification pass is a correctness gate, not belt-and-braces. |
| 2026-07-28 | [Of the free block measures, only two are not already on screen](2026.07.28-four-free-block-measures-two-survive.md) | closed | Seven candidates read off the tensor arrays block_signal already forms; five are explained by the existing three. trace (R2rank 0.87-0.88) and temporal_texture (0.94-0.98) are change_energy wearing a hat, texture (0.73-0.83) and spatial_coherency (0.32-0.39) are partly explained and separate nothing (d = +0.5 and +0.1). flow_agreement (R2rank 0.08-0.09, d = +1.6) and flow_direction (R2rank 0.002-0.006) survive, and the pre-stated prediction was wrong about both: agreement was called the likely casualty against coherence and correlates with it at 0.09-0.17. |
| 2026-07-28 | [Capacity beats policy in the render ring](2026.07.28-capacity-beats-policy-in-the-render-ring.md) | closed | At the ring's operating capacity the proposed distance-from-playhead rule buys 0.69 percentage points of hit rate over the plain ring (33.39% vs 32.70%), while raising capacity from 280 to 720 frames buys 42 points on the plain ring alone. LRU and the ring are indistinguishable everywhere. The second eviction rule is not worth building; the share is worth raising, and it saturates at ~720 frames. |
| 2026-07-27 | [The Morlet pad was one e-folding short, and the record wrapped onto itself](2026.07.27-the-morlet-pad-was-one-e-folding-short.md) | closed | morlet_power padded by one e-folding time, leaving a ~56-frame zero gap against a 0.5 Hz wavelet needing over twice that, so a burst against one end of the record wrapped circularly onto the other at 2.4% of full band-power scale; three e-foldings drops it to 3e-7. Separately, a truncated record's trailing values are only final two e-foldings back from the cut, not one. |
| 2026-07-27 | [The density histogram was a scatter, and it ran on every drag tick](2026.07.27-the-density-histogram-was-a-scatter.md) | closed | `DensityPlot.set_series` binned `(T, B)` with one `np.add.at`, which is the unbuffered scalar-at-a-time ufunc path and needed a frame index per element to do it. On the stress workload that was a 1,010 MB `intp` array and a 505 MB `int32` allocated per call, and 4,840 ms of scatter, on the GUI thread, inside a 50 ms budget. A `np.bincount` per frame produces the same histogram in 263 ms with no index array at all. The larger win is that it need not run: the cheap tier hands back the identical `band_power` object, so an identity check takes a value-band drag from ~5.5 s to a repaint. |
| 2026-07-27 | [Session time is generation, not tools](2026.07.27-session-time-is-generation-not-tools.md) | closed | Of 22.7 h of active agent time across 80 sessions, tool execution is 3.3 h and the full gate only 0.44 h (median 11.5 s per run) — the cost is model generation, so a scoped/incremental gate is dead and the levers are less ceremony to generate (docs 13%) and less re-orientation (11%). |
| 2026-07-27 | [scipy.fft's workers argument does nothing in this build](2026.07.27-scipy-fft-workers-does-nothing-here.md) | closed | `scipy.fft`'s `workers` is inert on this install — 1, 2, 8, 16, and 32 time identically on the strided `axis=0` call `morlet_band_power` makes *and* on a fully contiguous batch, and `get_workers()` reports 1. So the whole `(T, B)` Morlet pass ran single-threaded while `gui/concurrency.py` budgeted threads for it, on a 32-core machine. Running the existing block-chunk loop on a `ThreadPoolExecutor` with the inner FFTs pinned to one worker gives 4.2x at eight threads and 4.6x at the stdlib default, bit-identical output. |
| 2026-07-27 | [Dilation creates activity and diffusion conserves it](2026.07.27-dilation-creates-activity-and-diffusion-conserves-it.md) | closed | `motion_history`'s two coupling modes are not two settings of one operator. Under a continuously driven block, `dilate` holds the peak at the input's value at every reach and grows the field's total by 5.8x, 20.7x and 73.3x as reach goes 1, 2, 4 blocks — it *creates* activity. `diffuse` holds the total at exactly 1.000 at every reach and pays for the width out of the peak: 0.388, 0.160, 0.066 of the input. So a threshold tuned with coupling off survives turning `dilate` on and is broken by turning `diffuse` on, and the breakage is proportional to a parameter the user thinks is about *width*. REFINED-VISION C predicted `dilate` would win; this is why, and it is stronger than "expect". |
| 2026-07-27 | [Decode is a bandwidth wall, so a faster render starves the player harder](2026.07.27-decode-is-a-bandwidth-wall-shared-by-two-consumers.md) | open | The player and the preview decode the same window twice and contend on memory bandwidth, not cores: 32 sit idle. Playback of the reference source is 0.74x real time alone and 0.40x while a render fills. Switching the pipeline to the luma plane makes the render 1.88x faster (21.4 -> 11.4 ms/frame) and makes playback *worse* (23.8 -> 19.6 fps), because a consumer that finishes sooner draws the shared wall harder per unit time. Only removing a consumer helps: decoding the viewport as luma too takes playback-during-render to 52.3 fps, 2.7x the colour pane. A shorter window changes none of it — render wall time and watch time are both linear in the frame count, so the ratio is invariant. |
| 2026-07-26 | [Threading the reads buys 1.6x and stops](2026.07.26-threading-the-reads-buys-1.6x-and-stops.md) | closed | N readers on N threads take sequential reads from 24.50 to 15.25 ms/frame — 1.61x — peaking at four workers and degrading past six, to 28.49 ms at twelve on a 32-core machine. Cores are not the limit: every read allocates and frees a fresh 47.6 MB BGR array, so four workers already saturate memory bandwidth and more add page-fault churn the kernel serialises. The estimate of 9x in the previous finding was wrong because it assumed the only wall was the single-threaded convert. The remaining 6x requires not materialising a full-resolution BGR frame at all. |
| 2026-07-26 | [The spec's coherence formula fails its own test](2026.07.26-the-specs-coherence-formula-fails-its-own-test.md) | closed | ((λ1-λ2)/(λ1+λ2))² over the block tensor's top two eigenvalues scores a translating texture 0.16, not ~1; the pair that satisfies the item's own acceptance test is the smaller two — Haussecker & Spies' spatial coherency ((λ2-λ3)/(λ2+λ3))², which scores translation 0.92, flicker 0.06, and opposing motion 0.01. |
| 2026-07-26 | [The preview budgets are decode budgets until the store is warm](2026.07.26-the-preview-budget-is-a-decode-budget.md) | superseded | WRONG CONCLUSION, RIGHT NUMBERS — see the note above. A cold preview render costs ~22 ms per frame of `decode_range` and the filters are noise beside it, so `full_preview_render`'s 3 s ceiling is a limit of ~137 frames of footage — unmeetable on a 5-10 s window of the reference source by 2.2x to 4.4x. Warm, the same window re-renders after a parameter edit in 3.3 ms and the same budget is met with a 900x margin. The budgets therefore describe a warm preview, and what makes them reachable at all is the store surviving the edit, not any filter being fast. |
| 2026-07-26 | [The convert is single-threaded, not expensive](2026.07.26-the-convert-is-single-threaded-not-expensive.md) | open | ffmpeg performs the identical work — decode plus full-frame YUV420 to BGR24 — in 2.95 ms per frame where `VideoReader.read` takes 26.0 ms, because ffmpeg spreads sws_scale over ~17 cores (utime 15.5 s in rtime 0.88 s) and OpenCV's `retrieve()` converts single-threaded on the calling thread. Decode itself is 1.2-1.7 ms. The reference footage is not expensive to decode, this decode path is expensive to call. This verdict ended "and roughly 9x is available without changing a pixel", which was an estimate and was wrong — threading the reads was then built and measured at 1.61x, because a second wall sits behind the first. See `2026.07.26-threading-the-reads-buys-1.6x-and-stops.md`. |
| 2026-07-26 | [A stateful filter's output is not keyed by what it is](2026.07.26-stateful-output-is-not-keyed-by-what-it-is.md) | closed | A stateful filter with a correct `warmup_frames` *is* span-independent to within its own declared epsilon — so the reason it may not be cached is not that its output varies, but that nothing can check the declaration the invariance rests on, and the key carries neither the declaration nor the run's origin. |
| 2026-07-26 | [A sampled window is cheapest when it is longest](2026.07.26-a-sampled-window-is-cheapest-when-it-is-longest.md) | closed | `temporal_baseline`'s per-frame cost rises with the window up to the point the ring can still hold it whole and falls by 4x immediately after — 0.69 ms at a 15-frame window, 7.96 ms at 255, 2.03 ms at 258, 1.14 ms at 900. The peak is not the longest window but the longest one at stride 1, because the stride is what decides how often the two medians run at all. Any per-cell order statistic over a sampled trailing window has this shape; a cost model that assumes monotone-in-window is wrong by 4x at the discontinuity. |
| 2026-07-25 | [The seek is irreducible](2026.07.25-the-seek-is-irreducible.md) | closed | set(POS_FRAMES) is 46.7 ms of a 67.8 ms round trip and has no tunable knob; hardware acceleration does not engage in this build and keyframe alignment buys nothing, so the budget is met by asking for less rather than decoding faster. |
| 2026-07-25 | [The filter contract cannot type what VISION asks filters to do](2026.07.25-the-filter-contract-cannot-type-vision.md) | closed | Three things VISION requires of filters are inexpressible in `FilterSpec`: tabular output, a change of frame rate, and output size. The rate gap is the expensive one — it makes ARCHITECTURE's warmup arithmetic silently wrong by the decimation factor across any decimating node. |
| 2026-07-25 | [The replicate crop is a spatial warmup, and nothing declares it](2026.07.25-the-crop-is-a-spatial-warmup.md) | superseded | WRONG IN ITS PREMISE — see `superseded_by`. It assumed the filter graph would run on uncropped parent frames until the crop materialized. It runs on the crop from the first frame; the parent is only where that crop is read from. The analysis of the two filter classes below is still correct and is what made the premise worth checking, but the consequence it drew does not follow. |
| 2026-07-25 | [The crop belongs in the graph, so materializing it cannot change a result](2026.07.25-the-crop-belongs-in-the-graph.md) | closed | The filter graph consumes the replicate's ROI crop from its first frame; the background crop is a faster source for the same pixels, never a switch in what is computed. That makes it a checkpoint, and `Project.checkpoints` is already contractually never hashed — so the whole-frame-statistic hazard is closed by an invariant the artifact had rather than by a new declaration. |
| 2026-07-25 | [Open to first frame has headroom](2026.07.25-open-to-first-frame-has-headroom.md) | closed | 213 ms against a 500 ms budget through the full GUI path — 25 ms of container metadata and 188 ms of first decode — so open is the one pre-pipeline budget with room to spare. |
| 2026-07-25 | [Decode cost is colour conversion](2026.07.25-decode-cost-is-colour-conversion.md) | closed | grab() is 1.3 ms and retrieve() is 29 ms, so almost all per-frame cost is the YUV to BGR convert — short forward jumps should grab rather than seek, and real-time playback of 59.94 fps 5.3K footage is not achievable at 34 fps decode. |
| 2026-07-25 | [Coalescing bounds scrub lag](2026.07.25-coalescing-bounds-scrub-lag.md) | closed | A burst of 40 seeks settles on the final target in 172 ms — roughly two decodes, not forty — so scrub latency is bounded by one decode regardless of drag speed; queued, the same burst would take ~3.2 s. |

*40 entries.*
