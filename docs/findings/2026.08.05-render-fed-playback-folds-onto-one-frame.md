---
title: The freeze during a render is the fold, not the detector
date: 2026-08-05
status: open
tags: [gui, transport, playback, render-ring]

verdict: >
  Playback stalling while the graphs fill is render-fed playback's fold, not
  the detector: `feed_bounds` clamps the window to `frontier + 1`, so early in
  a render the span is one or two frames and the playhead runs at the render's
  frame rate instead of the wall clock. This is the fold working as designed
  and as tested — the defect is the judgment, not the code, so the test that
  pins it has to move with any fix.

question: >
  Reported as "the video freezes during the phase where the detection is
  happening — it only starts playing once the filters are done." Is the
  detector blocking the GUI thread?

source:
  method: code reading against the reported symptom; no timing was taken
  build: working tree at ef69282
  machine: reference workstation

measurements:
  - probe: none — this is a diagnosis, not a measurement
    result: >
      The numbers that would close it are named under open_questions; until
      one of them exists this finding is an argument from the code, and its
      status stays `open` for that reason and not only because nothing acted
      on it.

closed:
  - what: the detector blocking the GUI thread
    why: >
      detection runs on `gui/detector_worker.py`'s thread, and the freeze
      lifts on `window_render_changed(False)` — the render's edge, not the
      detector's
  - what: per-partial plot repaints as the sole cause
    why: >
      `FilterTab._apply` does repaint every plot on the GUI thread once per
      partial pass, so it remains a live second contributor — but it cannot
      explain a picture that resumes exactly when the render ends while the
      plots keep their rate

open_questions:
  - >
    The one-click confirmation, not yet run. Uncheck "Show the graph render's
    frames during playback" in Preferences and re-run the same tune. Picture
    moves during the render, it is the fold. Picture still freezes — and
    especially if the sliders and graphs are sluggish too, not just the video
    — the repaint path is carrying more of it than this finding allows.
  - >
    What the fold is worth when it works. It exists to stop playback decoding
    frames the render has not produced, and nobody has measured the decode
    bandwidth it saves against the stall it costs. Deleting it is the
    recommendation below; that recommendation is unpriced on one side.

files:
  - src/sieve/gui/transport/pacing.py
  - src/sieve/gui/transport/player.py
  - src/sieve/gui/main_window.py
  - tests/unit/test_pacing.py
supersedes: []
---

# The freeze during a render is the fold, not the detector

The chain is three connections. `main_window.py:367` wires
`PreviewRunner.window_render_changed` to `VideoPlayer.set_render_filling`, and
`filter_tab.py:409` wires the same signal to the auto-gray toggle, which turns
`_viewport_luma` on and so opens the third of `_feed_ring`'s three gates. Both
conditions at `player.py:426` therefore become true on the same emission: the
instant a window render starts, `timerEvent` replaces the playback bounds with
`feed_bounds(bounds, ring.frontier)`.

`feed_bounds` returns `ClipRange(start=window.start, end=min(window.end,
frontier + 1))`. The frontier is "the last frame the render has produced", reset
when a window render starts, so for the whole first stretch of a render it sits
at or just past `window.start`. The span is then one or two frames wide and
`playback_step` loops inside it — the playhead advances at whatever rate the
chain emits frames, which on an expensive chain is a stall or a two-frame
shuffle. When the render ends, `window_render_changed(False)` clears
`_render_filling`, the fold lifts, and playback snaps back to the wall clock.
That is the reported symptom in full, including why it ends when the graphs
finish.

**This is not an unguarded edge case, and reading it as one wastes the fix.**
The first pass at this diagnosis said the single-frame fold was the case
`feed_bounds` forgot, since its own docstring rejects a fold that "pinned
playback to a single frame … in the name of keeping it moving." It is the
opposite: `tests/unit/test_pacing.py`'s
`test_a_frontier_at_the_window_start_is_a_one_frame_loop` asserts exactly
`ClipRange(start=100, end=101)` and explains it — "the very start of a render:
one frame exists and it is shown." The docstring's rejected case is folding to
a span with *nothing* in it, or to a foreign one; showing the one frame that
does exist was the deliberate choice.

So there is no bug to find in `pacing.py`. What is wrong is the judgment the
fold encodes — that watching the rendered prefix beats watching the source at
real time — and it is wrong in exactly the regime the tuning loop lives in,
where the chain emits a few frames a second and the prefix is therefore a
stutter. Any fix moves that test rather than satisfying it, which is the
practical reason this distinction is worth the paragraph: a session sent to
"add the missing guard" would find the case already covered and conclude the
diagnosis was wrong.

## Why the recommendation is to delete rather than tune

The obvious repair is a minimum lead — fold only once the frontier covers, say,
a second of frames. It buys back most of the bandwidth for most of a render and
costs a tuning constant, which is the shape this repo refuses elsewhere and
which would need a producer under rule 4 to be a real number rather than a
guess.

The alternative is to drop `set_render_filling`, the
`window_render_changed` → player connection, and `feed_bounds` entirely, and
let the ring be purely a cache: a hit for frames the render has reached, an
ordinary decode for the rest. The fold's purpose — not decoding frames the
render has not produced — is a decode-bandwidth argument, and the symptom is
the evidence that the trade is backwards at the moment it matters most, which
is while somebody is watching to decide whether the tune is right. The
preference stays as the lever for anyone who wants the bandwidth back.

What would overturn this: the confirmation gesture above coming back with a
picture that still freezes with the preference off. Then the fold is at most
half of it and `FilterTab._apply`'s per-partial repaint is the other half,
which is a different fix in a different file.
