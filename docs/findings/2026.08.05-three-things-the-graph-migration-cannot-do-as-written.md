---
title: Three things the graph migration cannot do as it was written
date: 2026-08-05
status: open
tags: [rework, schema, migration, detector, layers]

verdict: >
  The v6 flip cannot be a `model_validator(mode="before")` on `Project`, and
  it cannot synthesize a detect node. Synthesizing a node means naming a
  filter, which `core/pipeline_model.py` is deliberately blind to and
  `test_filter_id_spelling.py` gates; the detect filter's kernel is trailing
  and per-target while a saved `detector` field means centered whole-record
  semantics, so a synthesized detect node computes something else; and
  `DetectParams.fps` is a source fact no document records. The crop and the
  span have none of these problems and are carried, tested, in
  `pipeline/upgrade.py`.

  Revised 2026-08-05, same day, from re-reading what each obstruction
  licenses. Two of the three have answers and the third was drawn too wide:
  the reader is `pipeline/upgrade.py`'s own `load_project` and `Project.load`
  goes; the fps a synthesized detect node needs is probed off the container by
  that reader, not recovered from a sibling node; and *refusing a
  detector-bearing document* is not what "cannot synthesize a node" buys —
  it strands every project anyone has tuned. See the three `closed` rows below
  and `2026.08.05-what-the-deferred-detector-node-keeps-alive.md` for what the
  deferral costs over time.

question: >
  `docs/todo/the-graph-carries-the-crop-the-span-and-the-detector.md`
  specifies one commit: a before-validator on `Project` that synthesizes crop,
  span, and detect nodes for documents below version 6 and then drops the four
  fields. Does that mechanism build?

source:
  method: >
    Building the transform. The three obstructions below are each what stopped
    a specific line from being written, not a survey — the layer one is a test
    that fails, the detector ones are contracts the repo already states.
  build: working tree at f4aa194
  machine: n/a — none of this is a timing

measurements:
  - probe: none — this is a design result, not a measurement
    result: >
      What would close it is the flip landing, which is the follow-up item
      `the-graph-carries-the-crop-and-the-span`. The detector half has no
      close short of a centered windowed kernel.

closed:
  - what: "the mechanism the item names: a `model_validator` on `Project`"
    why: >
      It would spell `crop`, `span`, and `detect` — and their versions, and
      the identity value of their parameters — inside `core/`, which the
      module's own docstring forbids ("Deliberately not registry-aware") and
      which `tests/unit/test_filter_id_spelling.py` fails on, three new
      entries on a list REWORK R4 says only shrinks. `pipeline/upgrade.py`
      sits above `sieve.filters` and takes every id and version off the
      registered spec, so it types none of them.
  - what: "synthesizing a detect node from `Project.detector` in this commit"
    why: >
      `detect_cpu` is a `Mode.WINDOWED` kernel over a trailing span with one
      target frame; `Project.detector` means the centered, non-causal
      whole-record derivation `filters/detect.py`'s own adapter exists to
      preserve, and `2026.08.05-detection-is-a-filter.md` settles that the two
      must not be substituted for one another. The equivalence test the item
      demands would therefore fail by construction, which is the correct
      outcome and not a bug in the test.
  - what: "`carry_into_graph` refusing a document *because* it carries a detector"
    why: >
      Drawn too wide, and the cost is not small: the flip leaves
      `Project.detector` and `Replicate.detector_overrides` on the model, and
      `carry_into_graph` copies every key but `clip`, so the field would ride
      through untouched and keep being computed by the same v5 adapter as
      today. Refusing therefore protects nothing — a v6 document can still be
      written with a tuned detector, so the later migration must handle one
      regardless — while making every saved project anyone has actually tuned,
      and every snapshot in its history directory, unopenable from the flip
      until an item gated on an unwritten kernel protocol lands. Rule 6 is
      satisfied by carrying a field unchanged: a v5 detector still means what
      it meant. What must stay refused is the *synthesis*, and nothing
      synthesizes one.
  - what: "where the v6 reader lives"
    why: >
      `pipeline/upgrade.py` itself, as `load_project(path) -> Project`, and
      `Project.load` is deleted. `core/history.py` only ever writes projects,
      so `core/` never parses an upgradable document and the door can move
      whole. Not a separate `pipeline/open_project.py`: version dispatch and
      the transform change in the same commit every time the schema moves,
      which is the split CLAUDE.md warns leaves two files edited in lockstep.
      `from_yaml` stays as the raw, no-upgrade parse. Enforcement is already
      half-built — `extra="forbid"` means a v5 file reaching `model_validate`
      directly dies on the `clip:` and `roi:` keys `to_yaml` always dumps —
      but it dies in pydantic's words, so `_readable` also refuses
      `value < 6` naming the reader, which costs `core/` nothing because a
      module path in a message is not an import.
  - what: "where a synthesized detect node's fps comes from"
    why: >
      The container, probed by the reader. The choice this finding first
      posed — a sibling node's params, or the field default — is a false pair,
      because it assumes the upgrade is document-to-document. The reader has
      the path, the document has a `SourceRef`, and `pipeline/` sits above
      `decode/`, so it asks `decode/reader.py` `container_rate` the same
      question `gui/filter_tab.py` `_fps` asks when it writes fps into a node
      today. Construction, not luck, and it reuses the settled answer instead
      of inventing a recovery rule. A source that has moved refuses *that
      document* by name, which is a message about a missing video.

open_questions:
  - >
    Whether a reader that probes the container is acceptable at all — it makes
    opening a project touch the video file, which today it does not. Scoped to
    documents that carry a detector it would bite only there, and only until
    the detector leaves the model.
  - >
    Whether `DetectParams.fps` should be a params field at all in the long
    run. `the-detector-node-is-centered` calls removing it "probably the right
    answer"; it collides with `block_signal.py`'s own argument that `scale`
    and `fps` are explicit parameters *because the kernel must stay pure* and
    with the 2026-08-05 settled row pinning those three floats to avoid
    re-keying every cached entry. Arguable either way, and the migration no
    longer needs it decided.

files:
  - src/sieve/pipeline/upgrade.py
  - src/sieve/core/pipeline_model.py
  - src/sieve/core/history.py
  - src/sieve/decode/reader.py
  - src/sieve/filters/detect.py
  - src/sieve/gui/main_window.py
  - tests/unit/test_filter_id_spelling.py
supersedes: []
---

# Three things the graph migration cannot do as it was written

## The layer obstruction is the one that changes the design

The item asks for `model_validator(mode="before")` on `Project`, and the
reasoning is sound as far as it goes: an upgrade belongs where the document is
parsed, and pydantic gives a before-validator the raw mapping. What it misses
is what the upgrade has to *write*. A synthesized node carries a `filter_id`, a
`version`, and parameters at values only the filter knows — `crop.WHOLE_FRAME`
and `SpanParams()`'s defaults, both of which exist precisely because a writer
that has not opened the video needs a way to say "the whole of it".

Every one of those is a fact about `sieve.filters`, and `core/pipeline_model.py`
is the one module in the repo whose docstring makes a point of not knowing them:
"Deliberately not registry-aware … The split is what lets a project open on a
machine where a filter is missing." Writing the validator there would trade that
property for the convenience of a hook, and the trade is machine-checked against
you — `test_no_filter_id_is_spelled_outside_its_module_undeclared` fails, and
the fix is three permanent entries on a shrink-only list whose entries are
supposed to be the rework's work list.

So the upgrade is `src/sieve/pipeline/upgrade.py`, above `sieve.filters` in the
layer contract, and it types no filter id: the ids and versions come off
`CropParams.spec()` and `SpanParams.spec()`, and the identity values come from
the modules that own them. The consequence the flip inherits is not small: if the upgrade cannot be a
validator, `Project.load` cannot be the v6 entry point either.

The reader is therefore `load_project` in `upgrade.py` beside the transform,
and `Project.load` goes. What makes that a door rather than a convention is
that `core/history.py` only ever *writes* projects, so after the move nothing
below `pipeline/` parses a document at all; `from_yaml` remains for the raw
parse, `_readable` refuses anything below 6 with the reader named, and the
`test_filter_id_spelling.py` shape — this symbol appears here and nowhere
else — is what keeps a second reader from growing. Two things the flip must
not forget: `main_window._read_project` catches
`(OSError, YAMLError, ValidationError)` and `UnupgradableDocumentError` is a
`ValueError`, so it escapes; and every snapshot beside a v5 project is itself
a v5 document, so restore goes through the same door.

## The detector cannot be carried, on semantics before scope

`filters/detect.py` registers `Mode.WINDOWED` with a kernel that takes a
`FrameSpan` and returns the gate for `span.target` — trailing, causal,
per-frame. `Project.detector` is read by `detect_series`, which runs the Morlet
transform over the *whole collected series* and gates it with `centered=True`
by default. The completed entry for that filter settles the point in as many
words: do not route schema-v5 centered whole-record detection through the
executor's trailing windowed kernel.

A detect node synthesized from `Project.detector` would therefore be a node
that computes something else. It would produce a plausible gate channel of
exactly the right length, which is the migration's named failure mode arriving
through the one field the parity instrument was not built to watch — the parity
test diffs claimed intervals, and both sides would be claiming from the same
new semantics after the flip.

`carry_into_graph` refuses such a document by name rather than dropping the
field — and that is one step further than the argument reaches. The refusal
would be the honest form of this finding if the flip dropped `detector` from
the model, and the flip does not: the field stays, `carry_into_graph` copies
every key but `clip`, and a v5 detector arriving in a v6 document is still
computed by `detect_series` exactly as it is today. What the semantics
argument licenses is refusing to *synthesize*, which costs nothing because
nothing synthesizes one. What the refusal actually does is make every project
with a tuned detector — which is every project that has been used for the
thing SIEVE is for — unopenable on the day the flip lands, together with its
history, with no bounded date for reopening, since
`the-detector-node-is-centered` is gated on a look-ahead kernel protocol that
has no item. So the flip drops `_refuse_a_detector` and the refusal moves to
the item that drops the field, where it becomes a synthesis instead.

## The fps problem is smaller and worth stating anyway

`DetectParams.fps` has no home in the artifact. `Project` records a source
*path*, and `2026.08.05-exact-source-frame-rate.md` settles that the rate is the
container's rational, probed off the stream — a fact about the file, not about
the document. A document-to-document transform cannot know it.

It is recoverable in practice, because a v5 project with a tuned detector has a
`block_signal` node upstream and that node's params carry `fps` as a float. But
recovering it that way is correct by luck: it depends on the detection suffix
always sitting above a filter that happens to declare the rate, and a project
that carries a detector and no such node would fall back to the field default of
30.0 — which is wrong for 30000/1001 in the direction that moves the Morlet
bank and therefore what is claimed as an event. That is rule 6's failure exactly:
a number that looks better founded than it is.

Both options are wrong because both accept the premise. A *document-to-document*
transform cannot know the rate; the *reader* can, because it is holding the
path. `pipeline/` imports `decode/` legally, `container_rate` is the settled
answer to exactly this question, and `float()` of that rational is the same
double `filter_tab._fps` already writes into `block_signal`'s params — so the
synthesized node gets the rate from the same place a hand-built one does, and
no cache key moves. The cost is that opening a project would touch the video,
which today it does not; scoping the probe to documents that carry a detector
confines it to the case that needs it and to the years the field survives.
A document whose source has moved refuses by name, which is rule 6 satisfied
rather than dodged.
