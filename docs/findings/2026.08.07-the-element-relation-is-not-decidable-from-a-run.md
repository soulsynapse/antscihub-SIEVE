---
title: The element relation is not decidable from a run
date: 2026-08-07
status: open
tags: [tools, declarations, element]

verdict: >
  A run exposes how many values a tool emitted, never what one of them is a
  value of, and crop separates the two — it emits fewer elements than it
  consumed and every one of them is still a pixel. So PRESERVED and AGGREGATED
  are indistinguishable through the executor, while the concrete ElementKinds
  are decidable and are now gated.

question: >
  Can ToolSpec.element be checked by running the tool, the way
  adr/cache-admission-is-bounded-warmup.md's BOUNDED claim is?

source:
  build: the shelf as `sieve.tools.discover()` returns it
  probe: every tool as a single-node root graph through `pipeline.executor`

measurements:
  - probe: element counts in and out, at each tool's default parameters
    result: >
      crop 16384 -> 16384 (PRESERVED), downsample 16384 -> 4096 (AGGREGATED),
      rescale 16384 -> 16384 (AGGREGATED, because its default scale is 1.0),
      block_signal 16384 -> 4 (BLOCK), detect 16384 -> 1 (FRAME).
  - probe: the mutants `crop element=PRESERVED ==> AGGREGATED` and the reverse
    result: >
      SURVIVED against every count-based rule that is true of the shelf.

consequences:
  - >
    `tests/unit/test_declarations_run.py` asserts the FRAME equivalence in both
    directions, BLOCK's strict coarsening, and AGGREGATED's one safe
    inequality, and states in its docstring that the relation half is not
    covered rather than covering it with something false.

closed:
  - what: that the whole of `element` could be run-checked generically
    why: >
      crop reduces the element *count* while preserving the element *meaning*,
      so no rule over counts separates PRESERVED from AGGREGATED; and rescale's
      default scale of 1.0 makes even "an aggregating tool emits fewer" false
      of a configuration the shelf actually ships.

open_questions:
  - >
    Whether the relation half has any checkable consumer before Phase 7. The
    resolved kind is what `node_element` hands a count, so a two-node graph
    below `block_signal` distinguishes PRESERVED (BLOCK) from AGGREGATED (None)
    — but that reads the conversion, not the run, and what refuses a `None`
    element is not yet built.

files:
  - tests/unit/test_declarations_run.py
  - src/sieve/core/tool_base.py
---

# The element relation is not decidable from a run

The prediction that would have made this positive: an aggregating tool emits
strictly fewer values than it consumed, and a preserving one emits exactly as
many. Both halves fail against tools already on the shelf, and they fail in
opposite directions, which is why no threshold rescues the rule.

`rescale` kills the first half. It declares `AGGREGATED` and its `scale`
defaults to `1.0`, so the shipped default configuration emits exactly what it
consumed — the declaration is a claim over the legal parameter range and the
run only ever sees one point of it. Sweeping the range would fix that one tool
and not the rule: the configuration a run has to pick would then be derived
from parameter bounds, which is a second thing that can be wrong.

`crop` kills the second half, and kills it as a matter of meaning rather than of
configuration. Cropping decides *which* values survive and never what one of
them is a value of — a count over a crop of `block_signal`'s output is still a
count of blocks — so `crop` is `PRESERVED` while emitting a fraction of the
elements it was handed. A rule reading "preserving means the count is
unchanged" would convict it, and the tool is right.

What survives is the part where the count *is* the claim. `ElementKind.FRAME`
says one value describes the source frame as a whole, and "the run handed back
one value for an input of many" is exactly that sentence; the gate asserts the
equivalence in both directions, so a tool that quietly stopped reducing to a
scalar fails even though its declaration still parses. `ElementKind.BLOCK` says
the frame was divided into a grid, which is decidable as strictly coarser than
the input and strictly finer than a scalar — necessary but not sufficient,
since an aggregating relation looks identical from here.

---

## Amendment, 2026-08-07 (review)

The `crop` half of the argument above is right and was not measured. Every
number in `measurements` is at default parameters, and `crop`'s default
`region` is 1048576x1048576 — the whole frame — so the row that stands under
the sentence "`crop` is `PRESERVED` while emitting a fraction of the elements
it was handed" reads `crop 16384 -> 16384`. A reader comparing the prose to the
table cannot tell which of the two is wrong.

The missing probe, run at review:

- probe: crop as a single-node root graph at `region=(0, 0, 32, 32)`, the
  128x128 footage of the case above
  result: 16384 -> 1024, `element` still `PRESERVED`

That is the configuration the argument needs, and with it the argument holds as
written. The consequence for the gate is unchanged — the mutant
`crop element=PRESERVED ==> AGGREGATED` was re-run independently at review and
SURVIVED, as recorded.

What the defaults row does establish, and the finding did not say: the rule
"`PRESERVED` implies the count is unchanged" is *green today* across the whole
shelf, all six preserving tools emitting 16384 -> 16384. So it is not a rule a
run would have caught being false — it is one that would have passed until the
first pipeline cropped anything, and then convicted a correctly declared tool.
A rule green on every shipped default is the weaker evidence, not the stronger.
