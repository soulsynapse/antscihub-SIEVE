---
title: Two crops of one name and span write one file, and backs still says yes
date: 2026-08-07
status: closed
tags: [materialize, phase-5, artifacts, port-fidelity]

verdict: >
  `artifact_filename` is a pure function of name, format and span — not of
  region — so two records that `identity()` calls distinct resolve to the same
  path, the second write clobbers the first, and the first record's `backs()`
  returns True over a file holding a different region's pixels.

question: >
  05.1's docstrings say the artifact name is "convenience, never identity" and
  that `backs` fails "in the direction that recomputes rather than the direction
  that serves the wrong pixels". Do both hold on the same path?

source:
  tree: v3 at 7d89dd2
  footage: tests/conftest.py's synthetic fixture, 160x120
  build: the repo's locked environment; no library version touches the result
  machine: n/a — the result is structural, reproduced in one pytest run

measurements:
  - probe: >
      `materialize_crop` twice against the fixture with `name="Arena 1"`,
      `span=SourceSpan(10, 16)`, `luma=True`, and regions `ROI(17, 9, 64, 48)`
      then `ROI(0, 0, 32, 24)`
    result: >
      both records carry `path = synthetic.crops/arena-1-luma-10-16.mkv`;
      `identity()` differs between them (region is one of its four components);
      one file survives in the folder; reading the first record's path back
      through `VideoReader` gives 32x24 where the record says 64x48; and
      `record_a.backs(ROI(17, 9, 64, 48), source=..., luma=True, ...)` is True.
  - probe: >
      the same shape in v2 — `git show main:src/sieve/pipeline/materialize.py`
    result: >
      identical. `artifact_filename(replicate.name, fmt, span)` with no
      uniquification anywhere in `materialize_crop`, and the same docstring
      sentence. The defect is inherited verbatim, not introduced by the port.

consequences:
  - >
    `docs/todo/two-crops-of-one-name-and-span-write-the-same-file.md` minted
    high, gating `the-materialize-command-derives-what-v2-was-handed.md`:
    nothing calls `materialize_crop` yet, so the corruption is unreachable
    until the command lands, and it must not still be there when it does.
  - >
    The `artifact_filename` docstring's "the name is disambiguated on the way
    out rather than trusted on the way in" describes a shape neither v2 nor v3
    has. Disambiguating a *lookup* by region (`backs`) is not the same as
    disambiguating a *write*, and the sentence conflates them.

closed:
  - what: >
      that v3 is more exposed here than v2 only in theory
    why: >
      v3's `name` is free-form display text a caller passes ("Never read back"),
      where v2's was `replicate.name`. Whatever constrained replicate names in
      v2 does not reach this parameter in v3, so the collision is strictly
      easier to reach.

open_questions: []

files:
  - src/sieve/pipeline/materialize.py
  - src/sieve/core/pipeline_model.py
---

# Two crops of one name and span write one file, and backs still says yes

The whole point of 05.1 is that an artifact which is not what it claims to be
must not be registered, and the verification pass it ports is a good guard
against the failure v2 measured — an encoder that lies. It is no guard at all
against this one, because at the moment `_verify` runs there is nothing wrong:
the file it reads back genuinely is the crop that was just fed to it. The lie
arrives later, and it arrives in the *first* record, which nothing re-checks.

What would have refuted the concern: `identity()` including the path, or
`artifact_filename` taking the region. Neither is true — `identity()`'s
docstring explicitly excludes the path ("writing the same cut twice to two names
is one cut recorded twice"), which is the right rule for the opposite direction
and leaves this one open. Two distinct cuts sharing one name is not the same
question as one cut under two names, and only the second is handled.

The reason this is a finding rather than a line in the item is the second
measurement. It reads like a v3 porting slip and it is not: v2 shipped it, ran
long enough to be the evidence base, and never hit it — which is itself the
useful datum, because it means the collision is rare rather than impossible and
a reviewer of the eventual fix should not expect a v2 regression test to exist.

---

## 2026-08-08 — the region went in the stem, and the suffix loop was the wrong shape

The open question is settled: `artifact_filename` takes the region and spells it
into the stem as `<x>x<y>-<w>x<h>`. What decided it was not readability but
`CropRecord.identity`, which this file already quotes in the other direction. A
uniquifying suffix counted off the folder makes the *second* write of the same
cut land beside the first instead of replacing it, which is the accumulation that
docstring exists to forbid — so the suffix shape would have closed this finding
by opening its mirror image. A stem that is a pure function of the record closes
both: distinct cuts cannot share a path, and one cut cannot occupy two.

The cost is smaller than the item expected, because a literal box beats a hash:
`arena-1-luma-10-16-17x9-64x48.mkv` is longer than the old stem and no less
readable. `ROI` refuses negative origins and non-positive extents, so no
component can carry a sign or an empty field into a file name.

`cut_from` is still not in the stem, and the folder is what stands in for it:
artifacts live under the parent video's stem. Two parents with the same stem
under one project directory therefore still share a crops folder — out of this
finding's scope, and unreached while nothing calls `materialize_crop`.
