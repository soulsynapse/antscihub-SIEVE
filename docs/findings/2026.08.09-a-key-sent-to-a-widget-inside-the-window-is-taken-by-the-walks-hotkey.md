---
title: A key sent to a widget inside the window is taken by the walk's hotkey
date: 2026-08-09
status: closed
commit: ""
tags: [gui, testing, hotkeys]

verdict: >
  Qt.Key_Down delivered to a combo inside a MainWindow runs window.go_down and
  never reaches the combo, so a case driving the popup that way asserts the
  walk's redraw and not the gesture it named.

question: >
  Why was a case that drove a generated combo's popup red before and after the
  defect it was written for was fixed?

source:
  footage: tests/conftest.py synthetic_video (160x120, 40 frames, mp4v)
  build: PySide6 6.x, QT_QPA_PLATFORM=offscreen
  machine: Windows 11, the repo's uv environment

measurements:
  - probe: >
      driving.key(combo, Key_Down) on a _ChoiceCombo inside an open MainWindow,
      with a listener on ParamForm.edited and on TuningLoop.refilled
    result: >
      neither signal fired and GraphPanel.is_stale went True — the combo's
      keyPressEvent never ran, and the mark came from _redraw's refill_graph
  - probe: the same gesture with Key_PageDown, which no shortcut binds
    result: >
      the popup opened, Return committed through the combo, and the panel's
      mark answered the write alone

consequences:
  - >
    tests/gui/test_app.py drives the popup with PageDown and moves the highlight
    with End, and says why in a comment beside the keys.

closed:
  - what: >
      that driving.key delivers to the widget it names, which the module's own
      docstring implies by saying focus is not arranged
    why: >
      QApplication.sendEvent goes through notify, and a WindowShortcut on the
      window is matched there, before the event reaches the child

open_questions:
  - >
    whether driving.key should say this itself — a helper whose contract is
    "one key press on `widget`" is where the exception belongs, but stating it
    there is a change to a shared module that no case currently needs.

files:
  - tests/gui/test_app.py
  - tests/gui/driving.py
  - src/sieve/gui/hotkeys.py
supersedes: []
---

# A key sent to a widget inside the window is taken by the walk's hotkey

`bind_navigation_hotkeys` puts four `QShortcut`s on the window — Left, Right, Up,
Down — and a `QShortcut`'s default context is `Qt.WindowShortcut`, matched inside
`QApplication::notify` before the event is dispatched to a child. Every case in
`tests/gui/test_param_generator.py` that opens a combo's popup with Down works
because there is no window over it; the same two lines inside a `MainWindow`
drive the walk instead, and the walk's `_redraw` calls `refill_graph`, which
marks the graph stale.

That is what makes it worth recording rather than merely fixing: the contaminated
gesture is red *for the shape the case was written to catch*. A case asserting
"the panel is not stale after a no-op edit" fails identically whether the defect
is present or the key was stolen, so the proof of red does not distinguish them
and the fix appears not to work. What separated them here was listening to
`ParamForm.edited`: the walk's mark arrives with that signal silent, and the
defect's arrives with it emitted.

`_ChoiceCombo` treats PageDown, PageUp, Home and End as navigation keys for its
own reason — each steps a closed combo and emits `activated` on the way past —
so any of the four opens the popup, and none of them is bound on the window.
