---
title: A shown window in one added case aborts the gui run at exit
date: 2026-08-09
status: open
tags: [gui, tests, qt]

verdict: >
  A new case that showed and activated a MainWindow ended tests/gui in a native
  abort (0xC0000409) after all 204 cases reported passed; the same case alone
  and the directory without it both exit 0, and the cause is unattributed.

question: >
  Is show() plus activateWindow() safe to reach for in a new tests/gui case, the
  way test_pinned_slot.py's fixture already does?

source:
  build: PySide6 as locked, QT_QPA_PLATFORM=offscreen, pytest as the gate runs it
  machine: Windows 11, the working tree mid-way through the folder-source item
  footage: none — the case builds a document naming no video and never decodes

measurements:
  - probe: >
      tests/gui with a new case that called window.show(), window.activateWindow()
      and driving.pump() on a MainWindow over a project naming no footage, then
      window.close().
    result: >
      204 passed in 9.11s, then exit -1073740791 (0xC0000409, stack buffer
      overrun). No traceback and no failing case.
  - probe: The same file run alone.
    result: 1 passed, exit 0.
  - probe: The same directory with that file ignored.
    result: 203 passed, exit 0.
  - probe: >
      The case rewritten to set activation with QApplication.setActiveWindow and
      deliver a synthetic ActivationChange, never showing the window.
    result: 204 passed, exit 0. The whole suite is 1206 passed, exit 0.

consequences:
  - >
    driving.activate sets activation directly rather than showing, and cites
    this file for why.

closed:
  - what: a Python exception leaking out of a Qt override — CheckpointFile was
      missing the ToolSource method the walk had just gained, which was live for
      the first observation
    why: >
      the abort reproduced after that was fixed and every case passed, so the
      leaked exception is not what aborts.

open_questions:
  - >
    What differs from test_pinned_slot.py's fixture, which shows and activates
    once per case and does not abort. Candidates not separated: that this
    window is over a project with no SourceRef and so has no player thread,
    that the pump follows the activation immediately, and that the activation
    lands on a window whose own changeEvent handler then re-reads the document.
  - >
    Whether the abort is reachable without the handler this item added at all —
    no case had ever both shown a window and had a reason to activate one that
    re-reads on activation, so the pairing is new rather than the platform
    under it.

files:
  - tests/gui/driving.py
  - tests/gui/test_source_resolution.py
  - tests/gui/test_pinned_slot.py
---

# A shown window in one added case aborts the gui run at exit

Recorded because the failure mode is the worst shape a test failure has: every
case reports passed, the summary line is green, and the process exit code is the
only thing that says otherwise. A session that read the summary and not the exit
code would have called this clean.

What would refute the reading that showing is involved: the abort surviving the
rewrite to `setActiveWindow`. It did not — the same case driving the same
handler through the same event, with no shown window, exits 0 over the whole
suite. That is enough to route around it and not enough to say what it is, and
the difference from `test_pinned_slot.py` is what an attribution would have to
explain. The candidates above are unseparated because separating them costs a
whole-directory run per candidate against a failure that appears in no smaller
run.
