---
title: The shelf reparses every project per arrow key, and its scroll does not follow the accent
date: 2026-08-09
status: open
commit: "7078a28"
tags: [gui, performance]

verdict: >
  Moving the project selection rebuilds the whole pane, which reparses every
  document in the library — about 72 ms per keystroke over forty projects — and
  resets the scroll area to the top, so the accent leaves the visible region on
  the way down and never comes back.

question: >
  09.5 holds which project wears the accent in the window and redraws the pane
  on every move, as the walk's other panes are redrawn. The walk's panes redraw
  off an in-memory graph; this one redraws off the filesystem. What does a
  keystroke cost, and what does the rebuild throw away?

source:
  footage: none — forty generated three-node projects, each over a SourceRef that does not exist
  build: PySide6 as pinned in the lockfile, CPython 3.13, offscreen platform
  machine: Windows 11, the v3 worktree at 7078a28

measurements:
  - probe: "listings() over a forty-project library, once, outside any widget"
    result: 40.2 ms — about 1 ms per document, all of it read and YAML parse
  - probe: >
      ten MainWindow.go_down calls at the project position over the same
      library, the pane shown
    result: >
      717.4 ms, 71.7 ms per keystroke. The rebuild is unconditional, so a held
      key at either end of the list pays the same.
  - probe: >
      the scroll area's vertical bar after those ten moves, with the selected
      card at index 10
    result: value 0 of maximum 1926 — the accent is below the fold and the view did not move

consequences:
  - >
    Owned by todo/the-shelf-is-rebuilt-per-keystroke-and-pays-for-it-twice.md,
    which also carries the second half — chain_stack.PipelinePane is rebuilt the
    same way on every move of the walk and loses its scroll position identically.
  - >
    MainWindow.select_project's docstring claims arrowing down a library is not
    the expensive keystroke it would be if selecting opened a document. The
    redraw it exempts is where the cost is; the sentence goes with whatever fix
    lands.

open_questions:
  - >
    Whether the remedy is a cheaper rebuild (cache the listings against mtime,
    or repaint the accent in place) or a stack that carries state across a
    rebuild. The scroll half needs the second and the parse half is satisfied by
    either, which is why the item holds one decision rather than two.

files:
  - src/sieve/gui/app.py
  - src/sieve/gui/project_select.py
  - src/sieve/gui/chain_stack.py
---

# The shelf reparses every project per arrow key

The number that matters is not 72 ms on its own — it is that 72 ms buys nothing.
Nothing a card says depends on which card is selected, so the parse is repeated
work by construction: the same forty documents, unchanged since the last
keystroke, read and decoded again to produce byte-identical strings. Key
autorepeat is roughly thirty a second, and a library four times this size would
put a keystroke's cost above the frame budget the whole architecture is
organised around.

What would have refuted "it is the parse": had `listings` been cheap and the
717 ms been widget construction, the fix would be the opposite one — build the
cards once and repaint. `listings` alone is 40 ms of a 72 ms keystroke and the
rest is forty `ChainCard`s with their layouts, so both halves are real and the
parse is the half that is avoidable without changing what is on screen.

The scroll result is not a performance finding and is filed here because the
same rebuild causes it. It is the more user-visible of the two: a library long
enough to scroll cannot be navigated by arrow key at all, because the thing the
arrow key moves is not on screen.
