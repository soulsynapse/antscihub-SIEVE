---
title: Anchoring puts the project directory into the node key
date: 2026-08-10
status: closed
commit: "d62948e"
tags: [cache-key, resolve-source, adr, portability]

verdict: >
  `resolve_source.anchored` rewrites a relative path param to an absolute one
  before `Dag.build`, and `node_key` hashes the resolved params, so the project's
  own directory is now inside every key below a source node. Two projects naming
  one file by the same relative pattern key differently, which is the failure
  `a-users-file-wires-in-like-any-other-input.md` forbids by name; the commit and
  `anchored`'s docstring both claim the pattern never enters a key.

question: >
  Does the anchoring d62948e landed leave the ADR clause it says it leaves
  untouched — resolution policy stays out of the key?

source:
  footage: not applicable — the keys are derived, nothing is decoded
  build: v3 at d62948e, `pick` 1.0.0
  machine: not a timing

measurements:
  - probe: >
      one graph, one `pick` node with pattern `plate_bg.png`, keyed three ways
      through `Dag.build(...).node_keys(source="SRC", picked={"s": "IDENT"})` —
      as the document holds it, anchored on `C:/one/proj`, anchored on
      `C:/two/elsewhere`. The picked identity is held equal across all three, so
      the only thing varying is the project directory.
    result: >
      three different keys. As held `36c10034...`, anchored on the first
      `68498c46...`, anchored on the second `f5a8feff...`. Same file identity,
      different project folder, different key.
  - probe: "which positions `cache_key.node_key` digests"
    result: >
      `upstream`, `tool_id`, `version`, and `params.canonical_json()` of the
      resolved params. A path param is a param, so the anchored absolute string
      is in the fourth position. `picked_key`'s docstring already says so —
      "The pattern is still a parameter and still reaches the node's own key
      through `params`" — which is the sentence the commit's claim contradicts.

consequences:
  - >
    Folded into `docs/todo/the-field-that-names-footage-leaves-the-schema.md`,
    the item that owns the anchoring clause. Its `done_when` does not cover it —
    none of the four legs derives a key.

closed:
  - what: >
      that the anchoring is free in keys because resolution precedes `Dag.build`
    why: >
      preceding the build is what puts the product *into* the build's input. The
      ordering the ADR asks for is satisfied and the exclusion it asks for is
      not; they are two clauses and only one of them was checked.

open_questions:
  - >
    Whether the fix is to exclude `ToolSpec.path_params` from `node_key`'s
    digest for a source-tool node — sound on the face of it, since the file that
    param resolved to is already in the key as the `picked_key` on the node's
    `upstream` pair — or to accept the cost and say so in a successor ADR.
    Nothing in the tree pins key stability across a project's location either
    way, which is what makes this re-measurable rather than red.

files:
  - src/sieve/pipeline/resolve_source.py
  - src/sieve/pipeline/cache_key.py
  - docs/adr/a-users-file-wires-in-like-any-other-input.md
  - docs/todo/the-field-that-names-footage-leaves-the-schema.md
supersedes: []
---

# Anchoring puts the project directory into the node key

The ADR clause is one sentence and it names both halves of this failure:
"Resolution policy stays out of the key. What is hashed is the resolved file's
identity, never the rule that found it — neither 'this exact path' nor 'the
folder of this name beside the project'. A rule in the key makes two projects
naming one file disagree about it." The anchoring is the second of the two
forbidden rules, applied and then hashed.

What makes it worth a file rather than a line in the item is that the direction
of travel is towards universality, not away from it.
`adr/a-document-names-footage-only-through-a-tool.md` — the ADR d62948e is
implementing — requires the document to store the path *relative to the project
file*. So relative is not an edge spelling that a few hand-written projects
happen to use; it is what every project will hold once the migration lands. Today
most params on disk are absolute, because that is what a file picker and
`crop_serving` write, and for those the key was already location-bearing before
this commit. The relative spelling was the one that was location-independent, and
it is the one the migration makes the norm and this commit makes location-bearing.

The user-visible cost is a cache that empties when a project folder moves.
Relocating is not hypothetical: `Project.relocated` exists for it and
`tests/unit/test_pipeline_model.py::test_relocating_rebases_every_stored_path`
says why in its own comment — "Moving a project folder is how footage reaches a
cluster". After a move the document is correct, every file resolves, and every
node below the source recomputes, with no symptom that says why.

The probe, verbatim, because nothing in the tree holds it:

```python
from pathlib import Path
from sieve.core.pipeline_model import Node, Pipeline
from sieve.pipeline.dag import Dag
from sieve.pipeline.resolve_source import anchored
from sieve.tools import discover

discover()
p = Pipeline(nodes=(Node(node_id="s", tool_id="pick", version="1.0.0",
                        params={"pattern": "plate_bg.png"}),))

def keys(pl):
    return Dag.build(pl).node_keys(source="SRC", picked={"s": "IDENT-SAME-FILE"})

print(keys(p))
print(keys(anchored(p, Path("C:/one/proj"))))
print(keys(anchored(p, Path("C:/two/elsewhere"))))
```

A negative result would have been three equal keys, or two equal anchored ones —
either would have meant the path param does not reach the digest and the commit's
claim held. All three differ.

## Amendment 2026-08-10 — the exclusion branch was taken, and it is half a channel

`node_key` now drops `spec.path_params` from the params position, which is the
first of the two branches the `open_questions` above named, taken without a new
ruling for the reason stated there: the file the parameter resolved to is
already in the key as the identity on the source root's `upstream` pair. The
probe below is the one above with the branch built —
`tests/unit/test_cache_key.py::TestPortability` holds it — and the three keys
that differed are now one.

The re-measurement is what makes the *bound* on it worth recording, because the
item that owns this ("What a user sees after moving a project folder is a
document that opens, every file resolving, and every node below the source
recomputing") reads as though either branch would answer for that sentence, and
neither does. The project's directory reaches a key by two independent channels
and the anchoring is only one of them:

  - probe: >
      one `pick` root, `pattern: plate_bg.png`, keyed through the real walk
      (`anchored` → `Dag.build` → `source_files` → `picked_identities` →
      `node_keys`) with the project directory *and its file* created at
      `<tmp>/one/proj` and then at `<tmp>/two/elsewhere`
    result: >
      two different keys, with the path parameter excluded. The identity strings
      differ in their first field, which is the resolved absolute path.
  - probe: >
      `cache_key.source_identity` of one file, before and after renaming the
      directory holding it — a rename rather than a copy, so size and mtime are
      preserved exactly and the path is the only fact that moves
    result: >
      the identity moves; its second and third fields are byte-identical. So the
      path channel is live on its own, not an artifact of a copy's fresh mtime.

That second channel is `source_identity`'s own settled design — "The path is
included because size and mtime alone are not distinctive enough" — and its
docstring already prices the consequence as "Footage copied to another machine,
or restored from a backup, gets a new identity and recomputes. Wasteful,
correct, and the common case." A project folder moved with its footage inside it
is that case. So what the exclusion discharges is exactly the ADR clause's first
half, *two projects naming one file agree about it*, and its second half, *one
project agrees with itself after the folder is reorganized underneath it*, is
not the anchoring's to give back and was already gone before d62948e. Anyone
reading the closure as "a cache now survives a move" has the wrong claim.

## What the criterion could not have caught

`done_when`'s four legs name a refusal, a rewrite, a CLI message, a library line
and a version. None of them derives a key, so a green criterion here would have
said nothing about this either. The clause that was discharged was checked by the
one case the run wrote, and that case asserts the resolved *file* — which is
right, and is a different assertion from the key the resolution moved.
