---
title: A role-named queue entry re-derives its path and never its role
date: 2026-08-07
status: open
tags: [loop, queue, doc_index, specify, review]

verdict: >
  A `sieve-v3-work` entry was served against a head that `--next` labelled
  `specify`, and then, fifty seconds into the same session, against one it
  labelled `review`. The saved prompt re-runs `--next` and so always gets the
  right path, but which prompt starts was decided when the entry was queued and
  nothing re-checks it — so the role rots exactly the way a tailored prompt's
  premise does, and the work prompt has no clause that notices.

question: >
  Does queueing entries by the role `--next` names actually keep the
  specify/work/review split, as
  `2026.08.07-a-tailored-prompt-works-an-item-the-selector-would-have-sent-to-specify.md`
  concluded it does for role-queued entries?

source:
  build: v3 at 02ec96d, worktree clean at session start
  machine: >-
    orchestrator run 2026.08.07-223812-sieve-v3-work, started 22:38:12, queued
    by the review at 2026.08.07-221048 ("queued sieve-v3-review, behind the
    sieve-v3-work the previous run left waiting")

measurements:
  - probe: the first doc_index.py --next of the session
    result: >-
      `specify docs/todo/a-frame-count-does-not-enforce-its-own-int.md`, with
      `doc_index: 39 open items have no done_when` on stderr. The run carrying
      it was `sieve-v3-work`.
  - probe: the same command two minutes later, unchanged tree on this session's part
    result: >-
      `review docs/todo/a-run-commits-what-it-wrote.md`. Commit `4b59cf4`
      (22:39:02) landed fifty seconds after the session started and moved an
      item to `awaiting-review`, which outranks the whole open queue.
  - probe: what the work prompt says to do when the role is not its own
    result: >-
      nothing. Its only stop condition is "if it says nothing is takeable, say
      so and stop"; `--next` said something takeable, for another role. The role
      token is printed first on the line the prompt tells the run to read a path
      out of.
  - probe: whether the previous run chose wrongly
    result: >-
      no. It queued `sieve-v3-work` when `work` was the answer, and the answer
      changed while the entry waited. Both the queue's order and the item pool
      moved underneath it.

consequences:
  - >
    this session worked no item and left the tree unchanged but for this file.
    The frame-count item is untouched and still unspecified.
  - >
    `sieve-v3-review` was already queued and matches the role `--next` now
    names, so the loop's next link exists; nothing was queued on top of it.

closed:
  - what: >
      that naming the role at queue time is sufficient to keep the split, and
      the hole is only in hand-written entries
    why: >
      the role is as much a claim about the tree as a tailored prompt's premise
      is. `--next` re-derives its answer on every call; a queued *choice of
      prompt* derives it once, and only the path survives into the run. Two
      role-drifts happened inside one three-minute session here — one from the
      queue's own latency, one from a commit landing mid-session.

open_questions:
  - >
    the cheap guard is one paragraph in each loop prompt: read the role token
    `--next` prints, and if it is not this prompt's role, queue the prompt it
    names and stop. That costs a turn, needs no queue change, and is the option
    the tailored-prompt finding already put beside requiring paths in tailored
    entries. Both prompts live in `../agent-orchestrator/prompts/` and are
    Kendrick's edit.
  - >
    `4b59cf4` landed fifty seconds into this session. Whether that was a
    concurrent run or the keyboard is not determined here, and CLAUDE.md already
    forbids the second. Either way the role can go stale *during* a run, not
    only before it, which the prompt-side guard above only half covers — it
    reads the role once, at the start.

files:
  - docs/todo/a-frame-count-does-not-enforce-its-own-int.md
supersedes: []
---

# A role-named queue entry re-derives its path and never its role

The specify/work/review split holds only if the session that starts is the one
whose authority matches the head of the queue. `--next` was given a role token
so the queue would not have to infer it, and
`2026.08.07-a-tailored-prompt-works-an-item-the-selector-would-have-sent-to-specify.md`
ruled that this is sufficient for entries queued *as* a role, leaving the hole
in hand-written paragraphs. It is not sufficient. The role is computed from the
pool at queue time and travels in the queue, where the tree cannot reach it —
the same property that finding named as what makes a tailored premise rot.

What makes this quieter than the tailored case is that the entry looks fresh.
The saved prompt's first instruction is to run `--next`, so the run does get a
current answer, and the path it acts on is genuinely the head of the pool. Only
the half of the answer it was not told to read has changed. A run that reads
"do the item it names" and takes the path off the end of the line is behaving
exactly as instructed and is nonetheless the wrong session for that item.

What would refute this: a work entry served against a `specify` head that
produced a criterion and satisfied it without either being wrong. Nothing about
the drift makes the *work* bad — the frame-count item names a real defect and a
work session could have fixed it in an afternoon. What it makes impossible is
the separation the protocol is for, which is why the failure is silent: the
tree would have gone green.

## Amended after a hand-written entry declined, 2026-08-07

The predicted refutation did not arrive; a third drift did, and it names the
consequence this file left out. A hand-written `continue the loop` entry
(orchestrator run 2026.08.07-231834) started against a tree where the work it
would have done was already committed at `2fd2977` and the review it would have
queued was already at row 1. It read `--next`, saw `review`, saw the matching
entry ahead of it, declined to duplicate, and stopped — which is the behaviour
the guard in the open question above asks for, arrived at without the guard.

What is new is what the queue then said about it: `failed — reported success
without committing`. The orchestrator decides whether a run worked by whether
HEAD moved across it, so a run that correctly does nothing and a run that
silently did nothing are the same row. The two earlier drifts in this file both
committed a findings file and so never exposed this; the moment a run declines
cleanly, the only summary anyone scanning the queue reads calls it a failure.

That cuts against the cheap guard above rather than for it: a prompt clause
telling a run to queue the right role and stop makes correct declines *more*
frequent, and every one of them lands in the log as a failure. The guard and a
non-commit success signal are one edit, not two, and both live outside this
repo.
