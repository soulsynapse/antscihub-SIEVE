---
title: A worker's hand-enumerated mutation sweep held under an independent one
date: 2026-08-07
status: closed
commit: "a855910"
tags: [loop, review, mutation, vacuity]

verdict: >
  03.6's review re-ran the mutation argument from scratch with thirteen mutants
  the worker had not named, and found the same single documented survivor plus
  one more equivalent one — so a worker's own sweep is evidence a review can
  extend rather than a claim it has to replace.

question: >
  `2026.08.07-a-mutation-sweep-enumerated-by-hand-misses-what-it-did-not-look-for.md`
  says a sweep a worker enumerates is bounded by what it thought to try. Does an
  independent sweep over the *same* file find the cases the worker's list left
  out, or does the worker's list turn out to have been the interesting ones?

source:
  build: v3 at a855910, `src/sieve/pipeline/executor.py`
  method: >
    thirteen single-token mutants chosen by reading the file rather than the
    worker's list, each run against
    `tests/unit/test_executor.py tests/unit/test_stateful_execution.py`.
  machine: not a timing

measurements:
  - probe: >
      mutants the worker's seven did not name — window fill `<=` → `<`;
      the arrival guard `step - upstream_lag < first` → `step < first`;
      `if index >= plan.span.start` → `if True`; `_check_format`'s comparison
      inverted; `rate_changing` refusal removed; `upstream_lag` folded over
      parents' `upstream_lag` instead of their `lag`; `answers_for = step - lag`
      → `step - lookahead`; `spec.run is None` refusal removed; `keep.get` at the
      reading index
    result: >
      eight killed (5, 1, 9, 16, 1, 2, 1, 2 failures respectively); one
      survived — `keep.get(key, step)`, the read half of the store call the
      worker's finding already documents.
  - probe: "`_completed`'s `return` at the first gap → `continue`"
    result: >
      20 passed. Equivalent, and the docstring above it already argues why: every
      node's answers advance one frame per step, so the completed set is a
      prefix and a later-ready-while-earlier-not frame is unreachable.
  - probe: "`for index in sorted(pending)` → `for index in pending`"
    result: >
      20 passed. Equivalent: `pending` is only ever inserted into at the frame
      being read, so its insertion order is already ascending.

consequences:
  - the review confirmed 03.6 rather than reopening it; the three survivors are
    all argued in the file or in the worker's finding, and no new case was owed.
  - the store call has two halves and the finding named one. `keep.get` at the
    reading index survives for the same disjointness reason `keep.put` does, and
    the day the windowed frontier becomes keyable both go red together.

closed:
  - what: >
      that a worker-enumerated sweep should be treated as unverified until the
      review re-enumerates from the file
    why: >
      re-enumerating found nothing the worker's list had missed. What it did
      find was two *equivalent* mutants the worker had not claimed, which is the
      opposite failure and costs nothing.

open_questions:
  - >
    Whether a review should sweep at all when the worker has. It cost one batch
    of thirteen runs at under a second each, and the cheap version — pick the
    lines the worker's list does not mention and mutate those — is what produced
    every result here.

files:
  - docs/todo/the-executor-is-rederived-and-honors-lookahead.md
supersedes: []
---

# A worker's hand-enumerated mutation sweep held under an independent one

The prior finding's worry is real in general and did not bite here, and the
reason is worth separating from the result. 03.6's worker enumerated seven
mutants against the *change* it had made — the delay, the lag accumulation, the
window length, the index check. The review's thirteen were chosen the other way,
by reading every branch in the file and mutating the ones the worker's list did
not mention, which is where the inherited v2 lines live: `_check_format`'s
comparison, the `rate_changing` refusal, the lead-in suppression in `_completed`.
Those are exactly the lines a worker re-deriving a module does not think to
mutate, because it did not write them — and all of them died, because the case
table carried v2's cases for them.

So the two sweeps were complementary by construction rather than redundant, and
the re-derivation table is what made the second one land: the inherited lines had
inherited cases. A module written from nothing would have no such table, and this
result would not transfer to it.

The one thing the review's sweep added is that the store call has two halves.
`keep.put(key, answers_for, produced)` is the one the worker's finding named;
`keep.get(key, answers_for)` two lines above it is the same choice of index and
survives for the same reason. The disjointness case asserts the property that
licenses both, so nothing is owed — but a finding that names only the write half
reads as though the read half were covered by a case.
