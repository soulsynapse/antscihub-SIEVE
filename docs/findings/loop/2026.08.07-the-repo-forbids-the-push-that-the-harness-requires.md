---
title: The repo forbids the push that the harness requires
date: 2026-08-07
status: closed
commit: "6ee2b9e"
tags: [loop, harness]

verdict: >
  `CLAUDE.md` says "Nothing is pushed unless asked" and the session harness
  tells every run to push before it ends; each run resolves the contradiction
  privately and the tree has been pushed since at least 01.5 without anyone
  asking.

question: >
  When a repo rule and a harness rule disagree about an outward-facing action,
  does the loop notice, and does the resolution survive the session?

source:
  build: v3 at 83078d3; `CLAUDE.md` "Commits" section vs. the standing session
    instructions given to every queued run
  machine: n/a

measurements:
  - probe: 01.5's work run, which named the conflict in its final message
    result: it followed the harness, pushed `v3` to its upstream, and asked the
      reviewer to settle it — the only reason the resolution is visible at all
  - probe: whether an earlier run recorded the same call
    result: nothing in `docs/findings/` or `docs/todo/` mentions pushing, so
      earlier runs either pushed silently or did not push
  - probe: the CLAUDE.md push line at 6ee2b9e, one day later
    result: >-
      "You may push when given permission, but not otherwise." Kendrick's
      edit, and the answer this file asked for. The prohibition is gone; a
      session told to push has permission, so the harness instruction now
      stands unopposed and this review pushed on it.

consequences:
  - the resolution is per-run and unrecorded, so it is not a decision the repo
    made, it is a coin the harness happens to weight
  - a run that resolved it the other way would leave work only in this
    checkout, and nothing would say so

closed:
  - what: that the two rules would go on disagreeing until someone noticed
    why: >-
      the one-line amendment this file asked for was made. What it does not
      close is *how* it arrived: the edit reached `main` inside 02.1's
      deferral commit, unmentioned, swept up by that run's `git add -A` while
      the same run's final message quoted the sentence it had replaced —
      [[2026.08.07-git-add--a-commits-the-tree-the-run-inherited-not-the-work-it-did]].
      A rule change with no message is the same invisibility this file was
      about, one layer down.

open_questions: []

files:
  - CLAUDE.md
supersedes: []
---

# The repo forbids the push that the harness requires

The interesting part is not which rule is right — it is that the loop has no
place to put the answer. A worker can name the conflict in its final message,
which 01.5 did, and that message is gone the moment the review reads it. The
next run starts against the same two rules with no record that anyone weighed
them, and will weigh them again.

That shape generalises past this instance: any standing instruction that
disagrees with the repo gets re-adjudicated every run, silently, because the
loop's only durable surfaces are the item, the finding, and the code. A
disagreement that is about none of those three has nowhere to land. This file
is the workaround, not the fix; the fix is that the losing rule stops being
written down.
