---
title: The review names its subject by recency and the selector names it by index
date: 2026-08-07
status: open
tags: [loop, review, selection, doc_index]

verdict: >
  The review prompt identifies what it reviews as "the run that finished just
  before this one"; `--next` identifies it as the first unfinished row of the
  index. For this review the two answers were nine commits and one whole run
  apart, and the transcript the prompt hands over — step 3's only source of
  red-before-green evidence — belonged to a different item that had already
  been ruled `done`.

question: >
  When a review opens `read_run_log` with no label, is it reading the run that
  produced the item `--next` served?

source:
  build: v3 at 0a7482f; review of `minting-an-item-cannot-overwrite-one`
  machine: orchestrator logs under ~/.agent-orchestrator/logs, 2026-08-07

measurements:
  - probe: what `--next` served this review
    result: >-
      `review docs/todo/minting-an-item-cannot-overwrite-one.md`, the item's
      only commit being 2c4477e at 13:18.
  - probe: what `read_run_log` with no label returned
    result: >-
      the `start the loop` run of 14:09, which reviewed 06.3 and committed
      0a7482f at 14:15 — a different item, already `done`, nine commits later.
  - probe: whether 2c4477e's session has a transcript at all
    result: >-
      no. The log directory goes 11:43 (`sieve-v3-work (06.3)`) straight to
      13:41 (`put the to-do item index link in the readme`); 2c4477e, 61f17b8,
      e31fbeb and the rest of the 12:27-13:23 loop-machinery run were made by
      an interactive session that never entered the queue.
  - probe: what step 3 had to work with
    result: >-
      nothing. Red-before-green evidence was obtained instead by running an
      independent mutation sweep against the commit — five semantic mutants in
      `mint` and `tracked_drift` killed, two wiring mutants survived, which is
      the finding the transcript could not have supplied either way.

consequences:
  - >-
    step 3 degrades silently rather than loudly. A review that trusts the
    prompt reads a transcript, finds it discusses a different item, and has no
    instruction telling it that this is the failure rather than the run being
    terse
  - >-
    the deferral branch is worse than degraded, it is wrong: "if the run made
    no commit, or deferred its item, its final message is the thing under
    review" would have had this review re-adjudicate 06.3's ruling instead of
    2c4477e's work
  - >-
    an item can reach `awaiting-review` from a session that was never in the
    queue, so "the transcript" is not merely stale in this case, it does not
    exist. Any protocol anchored on it has a hole that no amount of queue
    discipline closes

closed:
  - what: >-
      that the work/review pairing keeps the two identifications in step, so
      naming the subject by run recency and by index order are the same thing
    why: >-
      `--next` became the first unfinished row of the index at 46be9e5, which
      is index order and monotone in step; run recency is wall-clock. They
      agree only while every item is reviewed in the run immediately after the
      one that built it, and an interactive session, a re-take, or any lag at
      all breaks that

open_questions:
  - >-
    whether the review should identify its subject from the item instead — the
    path `--next` gives, its `git log`, and the commit that set it
    `awaiting-review` — with the transcript demoted to corroboration that may
    be absent. That is the identification that cannot come apart, because the
    item is what the ruling is about

files:
  - scripts/doc_index.py
supersedes: []
---

# The review names its subject by recency and the selector names it by index

The prompt and the selector were written against the same picture — a work run
finishes, the review runs next, the transcript on top of the log stack is that
work run's. Nothing enforces it. `--next` orders by the index, so the item it
serves is the oldest unfinished one, and the run that finished most recently is
whatever happened to finish most recently. The two coincide by convention.

The convention broke here in the way that is hardest to notice: both halves
answered confidently. `read_run_log` returned a complete, articulate final
message about `render_window`'s cold first frame and a per-sample budget gate,
and every sentence of it was true. It was simply about a different item. A
review that had read it as its subject would have found no contradiction, no
missing evidence, and nothing to report — it would have re-ruled a ruling.

The prediction that separates this from ordinary lag: if the pairing were only
slow, the transcript would be the right run's and merely old. It was not old,
it was newer than the subject by an hour, because the subject's session never
entered the queue. Recency cannot point backwards to the item, and the item is
what the ruling is about.
