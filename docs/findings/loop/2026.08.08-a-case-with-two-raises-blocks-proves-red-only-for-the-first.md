---
title: A case with two raises blocks proves red only for the first
date: 2026-08-08
status: open
commit: "c9c3492"
tags: [vacuity, review, proof-of-red]

verdict: >
  The worker's red-before-green for a case holding two pytest.raises blocks
  reported DID NOT RAISE at the first call and stopped there, so the second
  subject's half of the case had no proof at all — pytest abandons the case on
  the first failing assertion, and a whole-diff revert can only ever exercise
  the earliest subject in a multi-subject case.

question: >
  When one case covers two call sites, what does reverting the implementation
  and watching it go red actually certify?

source:
  build: "docs/todo/a-nodes-inputs-are-labeled-and-variadic.md at c9c3492; test
    tests/unit/test_dag.py::TestElementMeaning::test_a_second_upstream_is_refused_
    rather_than_folded_from_the_first"
  machine: n/a

measurements:
  - probe: revert both hunks of src/sieve/pipeline/dag.py to c9c3492^ and run
      the criterion
    result: "one failure, DID NOT RAISE ValueError at the Dag._elements line —
      the Dag._element_names line is never reached, so its half is unproved"
  - probe: revert only the _element_names hunk, keeping the _elements fix
    result: "one failure, DID NOT RAISE ValueError at the Dag._element_names
      line — both halves are genuinely red, but only this second revert shows it"

consequences:
  - a review's revert-and-run is per subject, not per case; when a case names
    two call sites, revert each site's hunk on its own and run the criterion
    once per hunk
  - a worker's red-before-green quote that shows a failure at the first of
    several assertions is not evidence for the rest of the case, however the
    case is named

closed: []

open_questions:
  - whether the cheaper fix is a rule against multi-subject cases (one call
    site per case, so the revert and the case line up) rather than a rule about
    how to revert

files: []
supersedes: []
---

# A case with two raises blocks proves red only for the first

Nothing was wrong with the diff: both folds really had been indexing `fed[0]`,
and both really do raise once unpacked. The defect is in what the proof
covered. The worker changed two call sites in one commit, wrote one case
asserting both, reverted nothing selectively, and quoted a single
`DID NOT RAISE` as the case's red. That quote is evidence about
`Dag._elements` and about nothing else, because the case ends there — had
`_element_names` been left indexing, or had the second assertion named a
function that could not reach the branch at all, the same quote would have been
produced and read the same way.

This is not the two-refusals-share-a-code shape
([2026.08.07-two-refusals-that-return-the-same-code-shield-each-other-from-the-only-case.md](2026.08.07-two-refusals-that-return-the-same-code-shield-each-other-from-the-only-case.md)),
where the case cannot discriminate between the guards. Here the case does
discriminate — running it against a tree with only the second hunk reverted
fails on the second line, which is exactly what makes the second half real.
What fails is the *method* used to check: one revert of the whole diff answers
only for whichever subject the case reaches first, and the number of reverts a
multi-subject case needs is the number of subjects.

The generalisation for the vacuity list is not about assertions per case in the
abstract — a case with five asserts over one call is fine, since the call is the
subject. It is: **when one case calls production code more than once, the
proof-of-red has to be run once per call.**
