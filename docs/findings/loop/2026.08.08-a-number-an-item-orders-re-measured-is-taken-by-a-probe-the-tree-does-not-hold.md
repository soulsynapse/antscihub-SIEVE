---
title: A number an item orders re-measured is taken by a probe the tree does not hold
date: 2026-08-08
status: open
commit: "e2ec7f8"
tags: [findings, bench, review, reuse]

verdict: >
  The reuse row in the loop-budget finding has been measured three times — 58 of
  87, then 30, then 30 again — and every one of them came from a scratch file the
  run wrote, ran, and deleted. The item that ordered the re-run cannot be
  discharged by anything a reviewer can execute, so the review re-verified the
  criterion, the diff, and the red, and took the load-bearing number on trust.

question: >
  When an item says "re-run that measurement afterwards", what does the review
  that closes it actually check?

source:
  footage: not applicable — this is about where the probe lives, not what it read
  build: v3 at e2ec7f8
  machine: not applicable

measurements:
  - probe: "grep for the reuse counter in tests/bench/ and src/sieve/bench/"
    result: >
      no occurrence. `tests/bench/test_loop_budget.py` already builds the exact
      reference workload against a `MemoryFrameStore` and times five gestures;
      nothing in it reads `FrameResult.from_cache`, which is the field the
      figure is counted from.
  - probe: "history of the reuse row in 2026.08.07-the-loop-budget-is-met-headless.md"
    result: >
      three entries — 58 of 87 (06.3), 30 of 87 (06.5), 30 of 87 (06.5's
      re-take) — written by three sessions, each of which built its own harness
      because the previous one deleted its own.

consequences:
  - >
    `docs/todo/the-reuse-figure-has-no-committed-probe.md` puts the counter
    beside the timings in the harness that already runs the workload.

open_questions:
  - >
    Whether the same holds of the other rows. The five timing probes are
    `tests/bench/test_loop_budget.py` and are re-runnable; the reuse row is the
    one measurement in that file with no producer, and no sweep was done for
    others elsewhere.

files:
  - docs/findings/2026.08.07-the-loop-budget-is-met-headless.md
  - tests/bench/test_loop_budget.py
supersedes: []
---

# A number an item orders re-measured is taken by a probe the tree does not hold

The shape is not a wrong number. All three measurements may well be right, and
the third is consistent with the argument beside it — a guard that keys strictly
fewer frames withheld only lead-in frames, and the figure is counted over the
span, so it did not move. The defect is that "may well be right" is the whole of
what the review can say. Every other claim 06.5 made was checkable: the
criterion re-ran, the two tests failed against a reverted `executor.py`, the
mutants were the revert, the named twin in `test_executor.py` was a root and was
genuinely innocent. The reuse figure is where the chain of verification stops,
and it is the number Phase 6's gate and `PLAN.md`'s revival table rest on.

What makes it recur rather than being one run's shortcut: the item itself orders
the re-run in prose ("if the recomputed count does not fall, the admission did
not reach the path the preview actually takes"), and prose is not a thing a
worker runs. So each session reads the order, builds a probe, gets a number,
records it, and deletes the probe — leaving the next session, and the next
review, exactly where this one was. A criterion cannot cover it either, because
the claim is about a figure and not about a pass, which is precisely why the
producer has to be committed instead.

A negative result would have looked like this: the reuse counter turning up in
`tests/bench/`, `src/sieve/bench/`, or a `scripts/` probe, with the finding's row
citing it. Nothing does, and `src/sieve/bench/metrics.py` names five timed keys
and no count.
