---
title: The proof of red corrects the item and leaves the comment it was written from
date: 2026-08-08
status: open
commit: "8fba2b7"
tags: [review, proof-of-red, comments]

verdict: >
  The run's proof of red established that at_input_of already refuses every
  non-positive rate, wrote that into the item file, and left standing the test
  comment it had written before the probe — which says a negative rate "arrives
  as frames rather than as an error". Two artifacts of one commit contradict
  each other, the code contradicts the comment, and nothing in the suite is red
  for it, because the case is genuinely red either way.

question: >
  When a run's proof of red overturns the premise it wrote its comments from,
  what carries the correction back to them?

source:
  build: "commit 8fba2b7; the case
    tests/unit/test_tool_contract.py::TestRate::test_a_non_positive_output_
    rate_is_refused_on_the_warmup_side, against src/sieve/core/tool_base.py
    input_warmup_frames and src/sieve/core/types.py FrameCount.at_input_of"
  machine: n/a

measurements:
  - probe: the criterion for the item under review, unmodified tree
    result: 1 passed, 96 deselected
  - probe: delete the two guard lines in input_warmup_frames and run the
      criterion
    result: "one failure at the first raises block, regex did not match —
      actual message 'output rate must be positive to convert frames, got 0'"
  - probe: with the guard still deleted, call input_warmup_frames on the
      second block's arguments directly, per
      2026.08.08-a-case-with-two-raises-blocks-proves-red-only-for-the-first
    result: "RAISED ValueError 'output rate must be positive to convert frames,
      got -1' — refused, not returned as a frame count, so the comment's claim
      is false and the second half is red for the same reason as the first"

consequences:
  - both halves of the case are real red, so the item closes; what is wrong is
    prose, folded into todo/the-lookahead-conversion-picks-a-home.md
  - a review check candidate for distillation - when a run reports that its
    proof of red taught it something, the comments and docstrings in the same
    diff were written before that probe and are the ones to read against it

closed: []

open_questions:
  - whether the general rule is about proof of red specifically, or about any
    fact a run discovers mid-item - the item file is the artifact a run is
    holding at the end, so it is the one that gets corrected, and everything
    written earlier in the same commit keeps the earlier belief

files: []
supersedes: []
---

# The proof of red corrects the item and leaves the comment it was written from

The order the run worked in is the whole mechanism. It read the item, whose
own body (written the day before, by a `specify` run) says a zero rate "would
make `at_input_of` divide by zero, and a negative one would run the whole graph
with a window pointing the wrong way". It wrote `StalledParams` and the case
from that premise, restating it in both the model's docstring and an inline
comment. Then it deleted the guard to prove red, discovered that `at_input_of`
refuses the rate one line later, and wrote *that* into the item under a heading
called "What the proof of red measured" — accurately. The comment two files
away, arguing that a negative rate escapes as frames, is what the run believed
an hour earlier and never went back to.

Nothing catches this. The case is red per call, both calls, so the vacuity
checks pass; the criterion passes; `ruff` has no opinion on a false sentence.
The only reader who would notice is one who opens `types.py` while reading the
comment, which is exactly the reader the comment was written to save the trip.

The cost is not the sentence. It is that the comment is the stated reason the
second `pytest.raises` block exists — a reader deciding whether the block earns
its line is told the two directions differ, when in fact both directions are
refused identically by the same neighbouring guard and the case's whole subject,
for zero and for negative alike, is that the message names the node. Delete the
premise and the second block still earns its place, but for the other reason,
and no artifact in the tree says which.

The general shape, for the vacuity and review lists: **a run's last artifact is
the one its late discoveries reach.** Comments and docstrings written before the
probe keep the pre-probe belief, and the item file that contradicts them reads
as a record rather than as a correction. A review that has run the proof of red
itself already holds the fact — checking the diff's prose against it costs
nothing extra and is the only place it gets checked.
