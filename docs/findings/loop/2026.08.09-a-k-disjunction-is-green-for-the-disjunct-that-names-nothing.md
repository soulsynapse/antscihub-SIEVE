---
title: A -k disjunction is green for the disjunct that names nothing
date: 2026-08-09
status: closed
commit: "b63f43f"
tags: [loop, done-when, review, vacuity]

verdict: >
  `-k "a or b"` exits 0 when `a` exists and `b` matches nothing, so widening a
  criterion by adding a case name is invisible — the widened command certifies
  exactly what the narrow one did.

question: >
  The review prompt's step 4 and the todo template both tell a reviewer to
  widen `done_when` by naming the case the work will add. Does the widened
  command actually go red until that case exists?

source:
  build: pytest as pinned by `uv.lock` on this tree
  machine: the repo's own suite; no timing involved

measurements:
  - probe: >
      `uv run pytest tests/integration/test_crop_serving.py -k
      "the_served_graph_holds_no_crop_node or a_case_that_does_not_exist_anywhere" -q`
    result: "1 passed, 7 deselected — exit 0"
  - probe: the same command with only the nonexistent name
    result: "exit 5, no tests ran — red, and red for the absence"

consequences:
  - >
    A criterion minted with `-k` over names that none of which exist is sound:
    nothing matches, pytest exits 5, and the red is the absence. The template's
    rule is written for that case and holds there.
  - >
    A criterion *widened* with `-k` is not. The moment one disjunct matches,
    the command is green whatever the others name, so a review that adds a case
    name to an existing disjunction has written prose, not a criterion. The
    2026-08-09 review of `crop-serving-and-checkpoint-read-back-become-source-tools`
    split the item rather than widen its criterion for this reason.
  - >
    The same shape makes a `-k` criterion silently survive a rename of the case
    it selects, as long as any other disjunct still matches.

closed:
  - what: >
      that adding a case name to a `-k` criterion makes the criterion cover it
    why: >
      pytest's `-k` is a filter over collected names, not an assertion that the
      names resolve; an unmatched disjunct contributes nothing and reports
      nothing.

open_questions:
  - >
    whether a review that must widen a green criterion should use explicit node
    ids (`file.py::Class::test_name`, which errors on absence) instead of `-k`,
    or split the item. Both were available here and the split was cheaper;
    an item whose halves are not separable would have to take the node-id form.

files:
  - docs/todo/_TEMPLATE.md
  - docs/todo/crop-serving-and-checkpoint-read-back-become-source-tools.md
supersedes: []
---

# A -k disjunction is green for the disjunct that names nothing

Two rules in this repo point the same way and only one of them is safe. The
todo template says to name the test the work will add, "with `-k` or a node id,
even though it does not exist yet", because "red because nothing matches is red
for the right reason". The review prompt says widening `done_when` is the
reviewer's power and to use it when a fold outgrows the criterion. Put together
they read as: append the new case name to the existing `-k` and the criterion
now covers it.

It does not. `-k` selects among the names pytest collected; a term matching
nothing removes nothing and asserts nothing. So a disjunction is exactly as
strong as its weakest satisfied term, and appending to it can only ever be a
no-op or a narrowing that never fires. The failure is invisible in the usual
direction — the widened command still passes, which is what a reviewer expects
to see for the half that already landed, so nothing signals that the other half
was not covered.

This is a different defect from
[2026.08.07-a-k-selector-and-the-prose-name-beside-it-are-two-criteria.md](2026.08.07-a-k-selector-and-the-prose-name-beside-it-are-two-criteria.md),
which is about a selector and a prose name disagreeing and shows up as an exit
5 a worker cannot ignore. This one never shows up at all.

What makes it worth writing down rather than filing as a quirk of pytest is
where it bites: the review is the only session that edits `done_when`, and the
edit it is most often asked to make — widen for a fold, widen for a half that
is not started — is exactly the edit the mechanism cannot carry. A reviewer who
believes the widening landed has certified the untouched half in the same
breath as the finished one.
