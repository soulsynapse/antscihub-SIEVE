---
title: An item's alongside question is answered only in the run log
date: 2026-08-09
status: open
commit: "2f2917e"
tags: [loop, review, items, criteria]

verdict: >
  An item's "worth deciding alongside" clause cannot reach the criterion — the
  criterion witnesses the primary change — so a worker that rules "change
  nothing" leaves the ruling in its final message and the item closes `done`
  with the second question indistinguishable from dropped. The closing review is
  the last reader who still has the run log, which makes landing the ruling the
  review's job and not the worker's.

question: >
  Where does a secondary ruling live when its answer is that the tree does not
  move?

source:
  build: v3 at 2f2917e, worktree clean at session start and after the review
  machine: >-
    orchestrator run 2026.08.09-054719-sieve-v3-work and the review of it; the
    reasoning is a doc read and a signature read, so the machine does not enter

measurements:
  - probe: >
      the item's criterion, run against the delivered tree and against the three
      implementation files restored from 2f2917e^
    result: >
      2 passed at HEAD, 2 failed on the revert — the criterion witnesses the
      primary change and is silent either way on the alongside question
  - probe: >
      a grep for the alongside subject across the commit — dag.py, the commit
      message, and the item file
    result: >
      no hit. dag.py is untouched, the message does not mention it, and the item
      file carried only status. The ruling existed in the run's final message
      alone

consequences:
  - >
    The review wrote the ruling into the item as a dated closing section before
    setting `done`, and recorded the distinction the ruling ran together —
    absence per node, which `cli/materialize_cmd.py` relies on, versus the
    parameter default on `ExecutionPlan.build` and `Dag.node_keys`, which is
    what makes the omission silent

open_questions:
  - >
    Whether the review prompt should require it — "an item with a second
    question closes only when its answer is in the tree" is checkable by a
    reviewer and by nothing else, which is the same class as the pool rulings
    step 5 already carries

files:
  - docs/todo/a-source-root-is-unkeyed-in-every-front-end-but-run.md
  - src/sieve/pipeline/dag.py
  - src/sieve/pipeline/plan.py
supersedes: []
---

# An item's alongside question is answered only in the run log

The item asked for one thing and wondered about a second: make every front end
resolve its source roots, and — worth deciding alongside — whether `node_keys`
should keep silently dropping a source node it was given no identity for. The
first is what `done_when` can witness, because a criterion is written from the
change the item wants. The second cannot be, and the asymmetry is not an
oversight in the drafting: had the answer been "yes, change it", the change
would have arrived in the diff and been visible; the answer was "no, keep it",
and a decision to change nothing produces no artifact at all.

So the ruling was argued at length in the run's final message, where it is as
good as unwritten. The item then reads `status: done` with a paragraph asking a
question, and a later reader cannot tell whether it was weighed and kept, or
read and skipped. This is the same failure the repo's own rule about docstrings
describing a shape the code has left behind is written against, pointed at the
backlog instead of at a comment.

Why it lands on the review rather than the worker: the worker did the reasoning
and stated it, which is all the loop asks of it, and no rule in its prompt says
a ruling has to be committed. The review is the step that sets `done` — the
moment the question stops being open — and it is the last participant holding
both the run log and the tree. After it, the log is one of hundreds and nothing
points at it.

The instance is worth keeping for its own sake. The ruling rested on
`cli/materialize_cmd.py` being a legitimate caller that hands no identities, and
that is true; but it relies on `picked` accepting *absence per node*, whereas
the cliff the item named is the *default value* of the `picked` parameter on
`ExecutionPlan.build` and `Dag.node_keys`. Making the argument required and
letting `materialize_cmd` pass `{}` would keep the deliberate case and remove
the forgotten one. Nothing in the tree is wrong today — every front end
resolves — which is why this is recorded here rather than minted as an item; a
fifth front end is what would make it cost something, and this file is what that
run should find.
