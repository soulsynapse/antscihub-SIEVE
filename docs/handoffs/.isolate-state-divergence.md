This file exists as a summary of divergence between the handoffs from the oracle and the current, present state of the rewrite repository. It isn't meant to be a summary of the state; it is a summary of the divergence that was found when assessing the handoff and review files.

## 2026-07-23 14:41:35 -07:00 — Working-window handoff

Reviewed against current checkout commit `7707968`.

Corrections the oracle should carry into later handoffs:

- The current rewrite has no headless asset-generation contract. The private
  `IsolateSession._generation` value is GUI display lifecycle state and must not
  be placed in reusable source requests, batches, or scientific identity.
- `ActiveAsset` is immutable but shares a module with the PyQt
  `ActiveAssetController`. A genuinely headless contract must not import that
  module; use a headless request/reference plus a thin GUI snapshot adapter.
- The active asset snapshot does not currently contain the active media's
  `content_sha256`, although the asset sidecar does. Reading the recorded
  sidecar hash is not the same as verifying the current media bytes. Ordinary
  resolution may compare cheap current facts such as size, dimensions, and
  rational fps, but explicit content verification remains separate.
- Scientific source requests should require an existing registered sidecar.
  They must not silently initialize a raw video merely to manufacture stable
  identity.
- The current media API exposes only FFmpeg-produced `rgb24` bytes through
  `MediaSession.read_frame_rgb`. It has no general plane ids, plane registry,
  luma/alpha/native-plane delivery, or multi-plane batches.
- Isolate display explicitly requests a width-limited preview. Scientific
  source handoffs must require native decode and must not reuse that preview as
  evidence.
- `MediaSession.frame_count` may come from decoded, packet, container, or
  duration-derived counts. Later handoffs must retain this provenance and must
  not call every admitted extent verified.
- The current `MediaSession` owns one mutable FFmpeg cursor. A scientific
  iterator must use a request-local session rather than sharing the live player
  session.
- For a source with no consumer, the smallest bounded contract is synchronous
  iteration with natural backpressure. Do not require a worker, producer queue,
  GUI latest-only mailbox, or cross-thread cancellation until a real channel
  consumes the source.
- A source stream must make its final outcome accessible after ordinary
  iteration and must close request-local media on normal completion,
  cancellation, failure, explicit close, and early consumer exit.
- Current short RGB reads all become `FRAME_DECODE_FAILED`; the media layer does
  not preserve a structured clean-EOF versus decode-error reason. Do not require
  the working-window layer to guess truncation from message text or stderr.
- Decoded/delivered source span is not examined scientific coverage. Reserve
  `examined`, `processed`, `quiet`, and `detected` for completed scientific
  nodes.
- The current backend returns a complete exact-sized frame or an error. General
  spatial validity masks and coverage artifact types are not yet justified.
- A standalone headless diagnostic cannot observe another running Qt process's
  private current window or generation without an explicitly designed bridge.
  Until a real GUI consumer exists, use explicit diagnostic inputs and a pure
  GUI request-snapshot test.

## 2026-07-23 14:53:33 -07:00 — Working-grid handoff

Reviewed against current checkout commit `4b09232`.

Corrections the rewrite should carry into its assessment of the working-grid
handoff:

- Commit `4b09232` contains the corrected milestone-3 documentation, but no
  working-window implementation. Milestone 4 must remain queued until milestone
  3 is implemented and accepted, then rerun its divergence check against the new
  headless package and GUI snapshot seams.
- `ActiveAsset.width` and `height` are registered native dimensions, but
  `ActiveAsset` still lives in a PyQt-bearing module. Pass primitive native
  dimensions into the headless grid resolver; do not import `ActiveAsset` there.
- `IsolatePlayer.frame_size` is the capped display-preview size, not the native
  source extent. It must not be used to resolve working dimensions or block
  geometry.
- `IsolatePlayer` currently exposes only `image_rect()` for its letterboxed
  preview. It has no native-source-coordinate transform. Map grid boundaries
  directly by normalized working coordinates into `image_rect()` rather than
  creating a general display adapter.
- The normalized current-player mapping is:
  `view_x = left + (working_x/work_width)*drawn_width` and equivalently for
  `y`. The outside grid boundary therefore matches `image_rect()` even when the
  preview dimensions were capped or rounded.
- `IsolatePlayer` has no overlay input, zoom, or overlay-hide shortcut. Add one
  grid-specific optional paint input; do not copy the interactive Replicates
  `FrameView` or introduce a general overlay framework. Do not add zoom or a
  shortcut for this milestone.
- `IsolateSession` currently owns temporal playback, a mutable display
  `MediaSession`, timers, and the display decoder thread. Keep the grid resolver
  Qt-independent and do not make it part of this media-owning session merely for
  convenience. The smallest current GUI owner is `IsolateTab`; milestone 3's
  later request-snapshot method remained temporal and did not replace that
  owner.
- The rewrite currently has no downsample/block settings or persistence policy.
  The original review proposed initializing `1.0` and auto once per Isolate-tab
  session and retaining those intents across asset changes. The post-milestone-3
  refresh below supersedes that proposal as an assumed policy: retain versus
  reset requires a user decision. Do not add sidecars or `QSettings` either way.
- The loaded player already has `IsolateDecodeThread`. “No worker or decode on
  grid change” means no additional grid worker and no additional request to the
  existing display decoder, not that the application has zero threads.
- Current temporal controls use `can_loop` and are disabled on one-frame assets.
  Grid controls are spatial and must instead be enabled from valid active-asset
  geometry so one-frame assets remain supported.
- Dense-grid suppression belongs inside player presentation. It must not change
  the resolved grid, silently increase block size, or feed widget resize back
  into scientific settings.
- The right-hand channel placeholder is still intentionally empty. Grid
  controls should not replace it or introduce a fake channel; use a compact
  separate control/readout area in the existing Isolate layout.

## 2026-07-23 15:14:42 -07:00 — Working-grid post-milestone-3 refresh

Reviewed and updated against current checkout commit `01b256f`.

Corrections the oracle should carry into later handoffs:

- The milestone-4 review's central checkout description is now stale.
  Milestone 3 was implemented in `5bffca7`; the rewrite now has a Qt-free
  `WorkingWindowRequest`, `ResolvedWorkingWindow`, native `rgb24` batches,
  request-local synchronous streaming, explicit extent provenance and final
  outcomes, and a GUI request-snapshot adapter.
- `WorkingWindowRequest` contains registered asset identity and half-open
  temporal bounds. It intentionally contains neither source dimensions nor
  spatial tuning. `ResolvedWorkingWindow` contains probed native width and
  height, but obtaining it opens a request-local media session. Milestone 4
  must not open a working-window source merely to resolve or repaint geometry.
- The implemented GUI adapter is
  `IsolateSession.snapshot_working_window_request()`. It snapshots the current
  asset identity and temporal window without opening scientific media, but it
  does not establish a general Isolate settings controller. Grid intent should
  remain a plain Qt-independent value owned by `IsolateTab`, not be added to
  the playback/media lifecycle of `IsolateSession`.
- `ActiveAsset` now carries registered native `width`, `height`,
  `content_sha256`, and sidecar/media references. It still lives in a
  PyQt-bearing module. Pass primitive width and height from the active snapshot
  into the headless grid resolver; do not import `ActiveAsset` into the
  resolver.
- For immediate UI geometry, registered `ActiveAsset.width,height` are the
  available no-I/O extent. They are recorded facts, not freshly
  content-verified evidence. When a later channel combines a resolved grid with
  pixels, it should require equality with
  `ResolvedWorkingWindow.width,height` and retain that source's recorded
  identity and extent provenance.
- The rewrite still has no working-grid type, downsample setting, block
  setting, spatial-settings persistence, or accepted spatial dimension
  rounding rule. The handoff's Python `round` rule remains available, but must
  be defined once in the grid resolver and pinned at half ties.
- The review's proposed session-local retention across active-asset switches is
  not current rewrite behavior and has not been accepted as a product decision.
  Whether asset switches retain `downsample` and block intent or reset them is
  the only true behavior choice still requiring the user; either policy can
  re-resolve safely against the new primitive native dimensions.
- The current `IsolatePlayer` remains a capped-preview painter with one
  letterboxed `image_rect()` and no zoom or overlay seam. The review's
  normalized mapping directly into `image_rect()` remains the smallest correct
  projection. No milestone-3 implementation created a display adapter that the
  grid should reuse.
- Grid control changes must create no `WorkingWindowStream`, no
  `MediaSession`, no grid worker, and no request to the existing
  `IsolateDecodeThread`. The existing display thread does not violate this
  rule; only additional work caused by a grid change would.
- The review's out-of-scope implementation findings remain separate from
  milestone 4. In particular, milestone 3 did not repair the stale GUI decode
  error path, GUI decoder start/interrupt shutdown race, derivation's
  metadata-only trust of recorded content identity, synchronous first-open
  registration, capped-preview aspect distortion, or incomplete benchmark
  provenance. Grid code must not absorb those responsibilities.
- Focused post-refresh validation passed:
  `tests/test_working_window.py`, `tests/test_isolate_player.py`, and
  `tests/test_domain_and_services.py` report `49 passed in 10.46s` with Qt
  offscreen. This supersedes the review's pre-milestone-3 `34 passed` evidence
  for current-checkout assessment, while leaving the handoff's manual
  acceptance path outstanding.

## 2026-07-23 15:26:23 -07:00 — Working-grid implementation

Reviewed against headless-grid commit `1d09b6f` plus the current GUI change.

Current rewrite state the oracle should carry forward:

- The user accepted milestone 3, accepted the corrected milestone-4 plan, and
  chose to retain requested downsample and block intent across active-asset
  switches. The intents re-resolve against each new registered native extent;
  application restart returns to `1.0` and auto. No `QSettings`, sidecar, or
  per-asset tuning persistence was added.
- `application.working_grid` is the Qt-free spatial contract. It provides
  immutable settings, tagged auto/explicit block intent, deterministic Python
  half-tie rounding, compact resolved geometry, derived half-open block bounds,
  partial area weights, and working-to-source boundary projection.
- `IsolateTab` owns the one plain settings value, controls, resolved readout,
  and active-asset re-resolution. `IsolateSession` remains temporal
  playback/media ownership and was not given grid state.
- `IsolatePlayer` has one grid-specific optional presentation input. It maps
  normalized working boundaries directly into `image_rect()`, draws the true
  outside boundary, and suppresses all internal lines before generating them
  when projected spacing is below four logical pixels.
- Grid changes create no working-window stream, media session, frame request,
  grid worker, timer, cache, result, or coverage artifact. The right-hand
  channel placeholder remains unchanged.
- The controls remain available for valid one-frame active assets even though
  temporal looping is disabled.
- Focused grid/player validation reports `39 passed in 2.37s`; the complete
  offscreen repository suite reports `115 passed in 23.71s`.
- Visible user acceptance remains outstanding. Do not begin the first channel
  merely because the grid implementation and automated tests are complete.

## 2026-07-23 15:43:15 -07:00 — First-channel handoff

Reviewed against current checkout commit `0f4afb2`.

Corrections the oracle should carry into later handoffs:

- Commit `0f4afb2` is titled and described as an implemented first intensity
  channel, but its complete tree change adds only
  `5-First-channel.md` and `5-First-channel-review.md`. The rewrite has no
  intensity computation, channel result, scientific worker, compute/cancel
  controls, panel, or milestone-5 tests. Commit metadata is not implementation
  evidence.
- Milestone 4 is implemented and automatically validated, so the review's
  pre-milestone-4 absence list and “reported” type descriptions are stale.
  Visible user acceptance is still outstanding and remains the milestone-5
  sequencing gate.
- The implemented spatial contract is
  `application.working_grid.WorkingGridSettings` plus
  `ResolvedWorkingGrid`. Concrete resolved fields are `work_width`,
  `work_height`, `resolved_block_size`, `rows`, and `columns`; the handoff's
  illustrative `working_width`, `working_height`, and
  `block_size_working_px` names do not exist.
- `IsolateTab` owns session-local grid intent and its current immutable
  resolution. Requested downsample and block intent survive asset switches and
  re-resolve against each new registered extent; application restart resets to
  `1.0` and auto. `IsolateSession` remains the temporal player/media owner and
  provides `snapshot_working_window_request()`.
- A future compute action should capture the temporal request and current
  resolved grid together. When the source opens, it must compare
  `ResolvedWorkingWindow.width,height` with
  `ResolvedWorkingGrid.source_width,source_height` before consuming pixels.
- `FrameBatch` exposes shape `(batch, height, width, 3)`. Its buffers are
  immutable exact-sized `bytes` from `MediaSession.read_frame_rgb`, tightly
  packed in row-major RGB order with pixel stride 3 and row stride
  `width * 3`. Short frames raise a structured decode failure; no padding or
  approximate reshape exists.
- The source plane is native-resolution decoded `rgb24`, not source-native
  luma. `PlaneDescriptor` records FFmpeg, source pixel format, color range,
  color space, transfer, and primaries, but the current source contract does
  not establish a canonical source encoded-luma plane or fully pin FFmpeg's
  conversion implementation/version.
- The smallest honest milestone-5 representation is therefore the review's
  path B: a distinctly named post-decoder Rec.601-like weighted RGB intensity.
  Its result must retain the accepted plane descriptor and backend provenance
  and must not claim canonical/source-native luma.
- `WorkingWindowStream` already checks cancellation before each frame read and
  owns one final `COMPLETE`, `CANCELLED`, `TRUNCATED`, or `FAILED` outcome.
  Normal completion, cancellation, failure, explicit close, and context exit
  close the request-local session. A channel should embed this exact outcome
  and separately record only channel-stage outcome and actually reduced span.
- `open_working_window(...)` constructs the request-local `MediaSession`
  immediately. Exact retained-result admission therefore has to use the
  captured span and grid to reject over-budget work before calling it.
- Decision 003 now establishes available compute resources and execution policy
  as first-class product concepts. It does not supply the initial numeric
  in-memory result budget; milestone 5 must define that value/default and accept
  it through a portable Qt-free resource-policy seam rather than silently
  inventing an intensity-specific limit.
- There is no scientific worker/supersession seam yet. The existing
  `IsolateDecodeThread` is a display worker around a shared session; its stop
  path interrupts cross-thread, waits up to three seconds without checking the
  return value, then drops the decoder reference. Milestone 5 must not copy
  that lifecycle.
- The safe new-worker boundary is: GUI owns cancellation/token/pending request;
  worker checks cancellation before creating, then exclusively creates,
  consumes, and closes its request-local stream; worker emits one terminal
  outcome only after close; GUI starts a pending replacement only after
  verified thread exit and retains ownership on cleanup failure.
- `IsolateSession.state_changed` exposes current-frame changes, and
  `timeline_seek`/`timeline_scrub` provide seek paths. The right-hand channel
  area is still the explicit `No channels added yet.` placeholder. No channel
  cursor signal, result owner, or panel is implemented.
- There is no current channel behavior that conflicts with the fixed
  conversion/downsample/block/publication rules because no channel exists.
  There is also no NumPy or OpenCV project dependency, so no vectorized or
  backend area reducer may be assumed before implementation.
- The oracle-only files `docs/rewrite-handoff-v2.md`,
  `docs/sieve-scientific-computation-contract.md`, and
  `.isolate-handoff-practices` are absent from this rewrite. Later oracle
  handoffs should carry any indispensable rule directly or cite a file that
  exists in both repositories.
- Post-assessment validation reports `115 passed in 25.23s` for the complete
  repository suite with Qt offscreen. No milestone-5 implementation was added
  by this assessment.

## 2026-07-23 16:12:57 -07:00 — First-channel implementation

Reviewed against the implementation working tree based on `48087a1`.

- The user authorized milestone 5, closing the remaining milestone-4 visible
  acceptance gate, and selected result-memory defaults of 16 GiB for CPU and
  6 GiB for GPU.
- `application.resources.ExecutionResourcePolicy` is the immutable Qt-free
  policy seam. Milestone 5 accepts CPU execution only; the GPU budget does not
  imply that a GPU backend exists.
- `application.intensity` now owns exact pre-source result admission, the named
  `sieve.channel.rgb601_intensity.v1` conversion, deterministic area
  downsampling, block reduction, immutable result values, source-outcome
  composition, processed coverage, and NumPy backend provenance.
- NumPy is now an explicit runtime dependency. OpenCV remains absent. No
  performance claim was made, so no benchmark finding was created.
- `IntensityWorker` owns cancellation and calls the synchronous headless
  computation, which exclusively opens and closes its request-local source.
  GUI publication occurs only after verified thread exit. A newest pending
  request starts only after the obsolete worker exits.
- `IsolateTab` owns the transient job token, at most one worker and pending
  request, the current complete result, and immediate invalidation on asset,
  window, or grid changes. Cursor-only player movement does not recompute.
- The concrete channel raster uses absolute frames on x, row-major blocks on y,
  fixed `[0,1]` grayscale, an exact player cursor, click-to-seek, and
  frame/block/value/bounds/partial-weight hover context.
- Over-budget GUI work reports requested and allowed bytes. Core tests prove
  that rejection and pre-entry cancellation happen before source construction.
- Validation reports `135 passed in 25.46s` for the complete repository suite
  with Qt offscreen. Manual milestone-5 acceptance remains outstanding.

## 2026-07-23 16:22:38 -07:00 — Normalization handoff

Reviewed against current checkout commit `f6af6fe`. The oracle review was
already folded into `6-Normalization.md`; no separate milestone-6 review file
exists.

Corrections the oracle should carry into later handoffs:

- Milestone 5 is implemented and has automated evidence, but it has not been
  manually accepted. The normalization implementation gate therefore remains
  closed. Reviewing and correcting the milestone-6 handoff does not constitute
  acceptance or implementation authorization.
- The concrete Qt-free request is `IntensityRequest`. It captures a
  `WorkingWindowRequest`, `ResolvedWorkingGrid`, `ExecutionResourcePolicy`,
  CPU-only `ExecutionTarget`, and `batch_size`. There is no separate intensity
  settings type. `ChannelStageOutcome` distinguishes only `COMPLETED` and
  `COMPUTATION_FAILED`; the exact `WorkingWindowOutcome` remains separately
  embedded in each `IntensityResult`.
- `IntensityResult` retains the captured request, resolved window and plane,
  immutable float32 block values, exact source and channel outcomes, processed
  half-open span, absolute `frame_indices`, row-major partial-cell weights,
  admitted result bytes, conversion id, normalization id, implementation id,
  NumPy backend provenance, and an optional structured error.
- The exact current per-frame order inside `reduce_rgb_frame(...)` is decoded
  native-resolution `rgb24` validation, float64 RGB601 conversion divided by
  255, deterministic area reduction, conversion of the working plane to finite
  float32 `[0,1]`, float64 mean over each owned block, and storage into a
  float32 block plane. There is no existing pre-block normalization hook.
  Milestone 6 should extract only the smallest pure normalizer seam between
  `area_downsample(...)` and the block loop.
- Current normalization identity is only the default result field
  `normalization_id = "off"`. It is absent from `IntensityRequest`, so current
  requests cannot distinguish normalization intent. The combined
  `sieve.numpy.area_block_mean.v1` implementation id also does not provide
  separate normalization provenance.
- The rewrite has no independent scientific result-key type or property.
  `IsolateTab._intensity_finished(...)` currently publishes only when both the
  worker token equals `_job_token` and a freshly captured `IntensityRequest`
  equals the worker request. Raw request equality also includes resource policy,
  execution target, and batch size even though those are execution inputs, not
  scientific identity. Milestone 6 should add or expose one minimal Qt-free
  scientific key rather than describing request equality as an already
  accepted result key.
- Active-asset, temporal-window, downsample, and block-intent/size changes
  immediately clear a published result and cancel active intensity work.
  Player cursor movement, playback, raster hover, raster click-to-seek, panel
  size, tab presentation, and grid-overlay visibility do not invalidate the
  result. Ordinary input invalidation currently cancels without automatic
  recomputation.
- A repeated explicit **Compute intensity** action does use the accepted
  newest-only supersession handshake: increment `_job_token`, clear the old
  result, store at most one pending `(token, request)`, and cancel the current
  worker. The pending worker starts only after the finished signal and a
  successful verified `wait(3000)`. Normalization-triggered automatic
  replacement is therefore new milestone-6 behavior and should reuse this
  handshake without changing ordinary milestone-5 invalidation semantics.
- `compute_intensity(...)` admits retained result memory and checks
  pre-entry cancellation before `open_working_window(...)`. `IntensityWorker`
  owns its cancellation event and calls that synchronous function. The
  computation closes its request-local stream in `finally`; GUI publication
  occurs only after verified thread exit. `IsolateTab` owns grid settings,
  resolved grid, resource policy, job token, active worker, pending request,
  current result, and the visible controls/panel. `IsolateSession` continues to
  own the display player clock and temporal request snapshot.
- The existing retained-result estimate is exactly
  `T * rows * columns * 4` for the primary float32 values. The accepted
  `ExecutionResourcePolicy` defaults are 16 GiB CPU and 6 GiB GPU, explicitly
  overridable by headless callers; execution remains CPU-only. The oracle's
  proposed 512 MiB default conflicts with current product policy and was
  removed from the corrected handoff. Milestone 6 should add `T` admitted bytes
  for an immutable one-byte-per-frame degeneracy array without replacing the
  existing defaults.
- `IntensityRaster` presents the immutable block values as a fixed `[0,1]`
  grayscale raster. `IsolateTab` owns the visible `0 black ... white 1` legend
  and context label. The raster owns the absolute-frame cursor, exact
  click-to-seek signal, and hover readout of absolute frame, row-major block,
  stored value, working bounds, and partial-cell weight. There is no
  mode-aware palette, normalization label, units field, clipping description,
  or degenerate-frame readout yet.
- The reported automated evidence remains 39 focused intensity/GUI/player tests
  and 135 complete offscreen tests. The current tests cover exact RGB weights,
  area reduction, partial cells, malformed frames, pre-source admission,
  pre-entry cancellation, immutable complete results, distinct source/channel
  failures, explicit-compute supersession, over-budget GUI reporting,
  invalidation, and raster seeking. They do not cover normalization. Manual
  milestone-5 acceptance remains outstanding.
- No current numerical implementation conflicts with population z-score
  because z-score does not exist. The handoff's proposed scientific rules are
  additions: fixed `1e-6` epsilon, float64 population statistics, finite
  complete-frame input, float32 output, exact-zero degeneracy, explicit aligned
  flags, and fixed signed presentation. They must not change the accepted
  milestone-5 `off` values, conversion, area reducer, block ownership, source
  outcomes, worker owner, or player clock.

## 2026-07-23 16:36:24 -07:00 — Normalization implementation

Implemented against the working tree based on commit `6c09ac6` after the user
explicitly authorized milestone 6. That authorization superseded the sequencing
gate; it is not recorded as retroactive milestone-5 manual acceptance.

Current rewrite state the oracle should carry forward:

- `NormalizationMode` has exactly `OFF` and `PER_FRAME_ZSCORE`.
  `NormalizationSpec` is immutable and Qt-free. Off has no epsilon and uses
  `sieve.normalization.off.v1`; z-score uses fixed GUI epsilon `1e-6` and
  `sieve.normalization.per_frame_population_zscore.v1`.
- `IntensityRequest` now captures `NormalizationSpec`.
  `IntensityScientificKey` contains the working-window request, resolved grid,
  normalization specification, RGB601 conversion id, and channel
  implementation id. Resource limits, execution target, batch size, GUI token,
  and presentation mapping remain outside this scientific key.
- GUI publication still requires `_job_token` equality and now compares the
  freshly captured scientific key rather than raw `IntensityRequest` equality.
  Headless results expose the same key.
- The concrete per-frame order is unchanged RGB601 conversion, accepted area
  downsampling to a finite float32 working plane, pure
  `normalize_working_frame(...)`, then the extracted owned-block float64 mean
  reducer and float32 retained block plane.
- The pure normalizer requires a two-dimensional float32 frame and every pixel
  finite. Off returns the existing frame without a statistics pass or identity
  copy. Z-score computes float64 mean, population variance, and sigma. It emits
  exact float32 zeros and `degenerate = true` only when `sigma < epsilon`;
  equality is nondegenerate. Non-finite input/statistics/output is a structured
  normalization failure, never zero-filled data.
- `IntensityResult` now retains the normalization specification through its
  request, scientific units, separate NumPy normalization provenance, and an
  immutable uint8 degeneracy array aligned one-to-one with processed absolute
  frames. Degenerate frames remain valid processed coverage.
- Retained-result admission is now
  `T * rows * columns * 4 + T`. The extra `T` is the exact degeneracy payload.
  The accepted 16 GiB CPU and 6 GiB GPU policy defaults remain unchanged, and
  scientific execution remains CPU-only.
- `IsolateTab` owns one session-local normalization intent. Startup is Off,
  active-asset switches retain the selected intent, and no setting is
  persisted. Selecting a mode before first computation does not start work.
  Changing mode after a result or active/pending job exists immediately clears
  old data and reuses the one-worker/newest-pending verified-exit handshake.
  Other input invalidations retain milestone-5 cancel-without-recompute
  behavior.
- `IntensityRaster` retains real scientific values and now has two stable view
  mappings: fixed `[0,1]` grayscale for Off and fixed `[-3,3]` blue-white-red
  diverging presentation for z-score. Color clipping never mutates stored
  values. Legend, hover, and current-frame context report mode, units, mapping,
  row-major geometry, and degeneracy.
- Focused intensity/normalization and GUI validation reports
  `43 passed in 4.54s`. A first complete run reported 157 passes plus one
  unrelated transient Windows `os.replace` `WinError 5` in the derivation
  cancellation test; that test immediately passed in isolation. The next
  final complete offscreen run reports `158 passed in 28.00s`.
- Milestone-6 visible/manual acceptance remains outstanding. CLAHE, change
  energy, value bands, selection overlays, Morlet processing, detection,
  persistence, caches, and later milestones remain absent and unauthorized.
