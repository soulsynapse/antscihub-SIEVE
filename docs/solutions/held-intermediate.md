---
want: "hold a computed value between the demands that share it"
answer: "the declaration-held pool, whatever the payload is (ADR-0006, experiments/orchestrator-experiments/pool.py). Check which coordinate the two sides key in rather than assuming — that folder and the contract both say position and do not mean the same thing by it, and a narrow cache scoped to one binding, like chain-experiments/bind.py, is keyed correctly by the coordinate it was built beside"
---
