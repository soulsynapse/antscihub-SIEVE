---
want: "hold a computed value between the demands that share it"
answer: "the declaration-held pool, whatever the payload is (ADR-0006, experiments/orchestrator-experiments/pool.py). Both sides key by (row, form) where a row is an ordinal against one listing snapshot, never the contract's position, which is a pts — a narrow cache scoped to one binding, like chain-experiments/bind.py, is keyed correctly by the coordinate it was built beside. Which of the two to reach for is a cost question with a measured answer: the pool for anything whose production dwarfs a scheduler's per-item bookkeeping, a scoped hold for a chained field whose producer is near the crossover, since routing that hold through a dispatcher costs about what the crossover is worth. Both figures are in docs/findings/ (2026.08.30, the chained-field crossover and making a field requestable)"
---
