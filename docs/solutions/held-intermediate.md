---
want: "hold a computed value between the demands that share it"
answer: "the declaration-held pool, whatever the payload is — keyed by what the value is about, position and form, never by the row that asked (ADR-0006, experiments/orchestrator-experiments/pool.py; chain-experiments/bind.py holds fields row-keyed, which is safe only while one producer at one form is the whole graph)"
---
