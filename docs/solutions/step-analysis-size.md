---
want: "bound a step's analysis size so it fits the frame period"
answer: "proxy_form() in src/sieve/proxy.py — whole-frame resolves to the proxy tier it is already served from; anything resampled grades APPROX and may only be shown"
---
