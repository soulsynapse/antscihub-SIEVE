<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Open and deferred work

One file per item. `status: open` is takeable now; `status: deferred` waits on the trigger in `gated_on`. Completion is a move to `completed-todo/` via `tools/complete_item.py`; promotion is a one-line `status:` edit.

| Status | Item | Gated on |
|---|---|---|
| deferred | [Tuning files: export and import the baseline](tuning-files.md) | the first tuning somebody wants to apply to a second video — likely the parity-comparison finding, or the first multi-source experiment |
| deferred | [Surrogate calibration for the detection threshold](surrogate-calibration.md) | the temporal chain settling — concretely, the first parameter set somebody wants to run over a whole video and report |
| deferred | [`slider_to_graph`, which is gated on there being a slider](slider-to-graph.md) | a parameter control bound to a node — VISION step 4's "information on the specific filter applied", the panel beside the operations list |
| deferred | [Sink writers](sink-writers.md) | the first filter that emits a TableSpec (a detector producing coordinates), or materialization landing and needing somewhere for a compacted array |
| deferred | [Right click back to the replicate tab](right-click-back-to-the-replicate-tab.md) | the click-through-navigation decision going the other way — that item explicitly rejected a right-click binding in favour of an output boundary in the chain-stack, so building this now knowingly contradicts it |
| deferred | [Replicate status: crop progress and output existence](replicate-status-columns.md) | the deferred **Sink writers** item (docs/todo/sink-writers.md) landing, or materialization — the same trigger, from either end |
| deferred | [What the viewport keeps of a render, and what it drops](proxy-retention-policy.md) | one recorded tuning session — run the GUI with SIEVE_RETENTION_TRACE set, tune something real for a few minutes, then `compare` the trace at a sweep of capacities (docs/todo/retention-trace.md landed the instrument 2026-07-28). The pending decision is whether distance-from-playhead beats the plain ring by enough to be worth being a second eviction rule; nobody but the person at the keyboard can produce the trace that settles it. |
| deferred | [Profiling as a module](profiling-as-a-module.md) | a budget miss whose cause is not obvious from the span that reported it — concretely, a `full_preview_render` miss over a multi-node graph |
| deferred | [Process isolation for filter execution](process-isolation.md) | a kernel that can take the process down rather than raise — most likely the first OpenCV-heavy filter or the GPU work; cooperative cancellation is the other and more likely trigger |
| deferred | [A pipeline editor, and whether it is a list or a graph](pipeline-editor-list-or-graph.md) | a graph that is not a chain — which means a multi-upstream filter, which means the named-port change to `Edge` in docs/todo/kernel-protocol-beyond-one-frame.md |
| open | [Parity comparison finding](parity-comparison-finding.md) | nothing structurally |
| deferred | [Materialization, and the rule that is waiting on it](materialization.md) | a workload that can say what the general store's chunking is for — the replicate-crop half was split out 2026-07-28 (crop-artifact-writer / -serving / crop-boundary-gesture, takeable in order) and no longer waits here; "filesystem is truth at rest" returns to the rules table with the writer item, not with this one |
| open | [The ledger's three unmeasured numbers](ledger-measurements.md) | nothing structurally — the ledger landed with its reserve and worker arithmetic marked provisional; each measurement below replaces a stated guess with a finding, and none blocks the items consuming the ledger |
| deferred | [A kernel protocol that is not one frame in, one frame out](kernel-protocol-beyond-one-frame.md) | a filter that actually needs one — for `Mode.WINDOWED`, a filter needing a span before it can emit; for `rate_changing`, a decimator |
| deferred | [HPC handoff, and review mode](hpc-handoff-and-review-mode.md) | for HPC, a dataset that does not fit in a local session plus at least one sink — a real user with a real cluster, not a milestone; for review mode, the first durable output worth interpreting |
| open | [Grid overlay seam and border width](grid-overlay-seam-and-border-width.md) | nothing structurally — both defects live in one paint routine |
| deferred | [GPU execution](gpu-execution.md) | a filter whose CPU kernel is measurably the bottleneck of a tuning session — not merely slow, but one where `bench/budgets.py` is being missed because of it and the profile says so |
| deferred | [Downsample or rescale](downsample-or-rescale.md) | the parity-comparison finding landing — rescale is v1's semantic and must exist unchanged until the comparison has run; after it, this item decides whether rescale stays a first-class step or becomes a parity fixture |
| open | [The crop boundary in the chain-stack](crop-boundary-gesture.md) | docs/todo/crop-artifact-writer.md and docs/todo/crop-artifact-serving.md landing first — this is the face they were built to wear; take the three crop items in order |
| open | [The crop artifact writer](crop-artifact-writer.md) | nothing — first of the three-item crop split (writer → serving → gesture); the codec is measured and every design decision below is settled |
| open | [Serving the crop artifact](crop-artifact-serving.md) | docs/todo/crop-artifact-writer.md landing first — same trigger, back half; take the three crop items in order |
| deferred | [Coverage and detection lanes on the timeline](coverage-and-detection-lanes.md) | the executor recording, per replicate and per frame, that it ran and under which resolved params |
| deferred | [Click-through navigation: the output boundary lives in the chain-stack](click-through-navigation.md) | general materialization promoting (docs/todo/materialization.md) — the source-boundary half left this item 2026-07-28 into docs/todo/crop-boundary-gesture.md, and what remains here is descent through *node* output boundaries, which has no writer until the general store exists |
| deferred | [Cache eviction, and spilling to disk](cache-eviction.md) | a tuning session that exhausts memory, or `materialize.py` landing — compaction to Zarr is where spilling belongs |
| open | [Blocks in band autoscale](blocks-in-band-autoscale.md) | nothing structurally — isolated, and no doctrine is disturbed |
| open | [Block signal free measures](block-signal-free-measures.md) | nothing structurally — every candidate below reads from arrays the kernel already forms; the work is choosing which earn a Signal entry, and the screening experiment is the choosing |
| open | [Band power at small block size](band-power-at-small-block-size.md) | nothing structurally — but it is only reachable through the wheel bug, so take `docs/todo/wheel-over-the-panel.md` first or the repro is by accident |
| deferred | [Application config, and where the boundary with Preferences falls](application-config.md) | the first setting the CLI and the GUI both need to read — cache bounds and backend selection policy are the two candidates |
| deferred | [Annotation spans on the timeline](annotation-spans.md) | a detector whose output is worth correcting by hand, or a labelling task that needs ground truth before one exists |
| deferred | [Accuracy feedback in the tuning loop](accuracy-feedback.md) | a marks model plus one hand-labelled window — the same trigger as the deferred **Annotation spans on the timeline** item (docs/todo/annotation-spans.md) |

*30 entries.*
