<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Open and deferred work

One file per item, most urgent first. `status: open` is takeable now; `status: deferred` waits on the trigger in `gated_on`. `priority` is how much it matters, independent of when it can be taken — `unassessed` means nobody has ranked it. Completion is a move to `completed-todo/` via `tools/complete_item.py`; promotion is a one-line `status:` edit.

| Priority | Status | Item | Gated on |
|---|---|---|---|
| high | open | [A filter id spelled outside its module is an enumeration](a-filter-id-spelled-twice.md) | nothing |
| high | open | [A kernel that sees a span — Mode.WINDOWED runs](a-kernel-that-sees-a-span.md) | nothing |
| high | open | [Every declarable shape runs or is refused by name](declarable-but-not-runnable.md) | nothing |
| high | open | [Detection is a filter emitting a per-frame channel](detection-is-a-filter.md) | nothing |
| high | open | [Four kinds of number, four types](four-numbers-four-types.md) | nothing |
| high | open | [A GUI-saved pipeline must run identically in the CLI](gui-cli-execution-parity.md) | nothing — AUTO-GUARDRAILS §2's own trigger ("the next item that touches serialization") fired with schema v3 and the check was never written |
| high | open | [The crop is a filter](the-crop-is-a-filter.md) | nothing |
| high | open | [The graph carries the crop, the span, and the detector](the-graph-carries-the-crop-the-span-and-the-detector.md) | nothing |
| high | open | [The spec has three channels, and every field is in exactly one](the-spec-has-three-channels.md) | nothing |
| normal | open | [A filter names what it emits](a-filter-names-what-it-emits.md) | nothing |
| normal | open | [A number says how it was founded, and for which machine](a-number-says-how-it-was-founded.md) | nothing |
| normal | open | [A ceiling is denominated in the dimension of what it bounds](ceilings-in-the-dimension-they-bound.md) | nothing |
| normal | open | [DetectorState dies, and nothing replaces it](detector-state-dies.md) | nothing |
| normal | open | [filter_tab.py owns six secrets](filter-tab-many-secrets.md) | nothing structurally |
| normal | open | [One definition of edge legality, two consumption modes](one-definition-of-edge-legality.md) | nothing |
| normal | open | [Presentation is a declared channel, not a switch in a widget](presentation-is-a-channel-not-a-switch.md) | nothing |
| normal | open | [Rule 6's frontier moves into the execution contract](rule-sixs-frontier-moves-into-the-contract.md) | nothing |
| normal | open | [sieve detect collapses into sieve run](sieve-detect-collapses-into-sieve-run.md) | nothing |
| normal | open | [temporal_baseline.py's docstring argues four other files' decisions](temporal-baseline-scope-split.md) | nothing |
| normal | open | [The executor stops cutting frames](the-executor-stops-cutting-frames.md) | nothing |
| normal | open | [The mutual tier — shared by dependency, not by agreement](the-mutual-tier.md) | nothing |
| normal | open | [The span is a filter; the decode range is an optimization](the-span-is-a-filter.md) | nothing |
| normal | open | [Warmup is derived, epsilon is a field, cacheable splits fact from policy](warmup-is-derived-not-declared.md) | nothing |
| normal | open | [Work units have one anchor and no coefficient table](work-units-have-one-anchor.md) | nothing |
| normal | deferred | [A kernel that changes the rate](a-kernel-that-changes-the-rate.md) | a decimator somebody needs — the rate arithmetic (output_rate as exact Fraction, input_warmup_frames, the monotone fold) is fully built and reachable by no filter, and building execution for it without a consumer would repeat the GPU machinery's mistake |
| low | open | [Name the fold, and give the combining rule to whoever fixes the strategy](name-the-fold.md) | nothing |
| low | open | [Stage is derived from the spec, or declared once — never both](stage-is-derived-or-declared-once.md) | nothing |
| unassessed | open | [A budget check gets slower the more of the suite pytest imported](budget-checks-under-ambient-load.md) | nothing — but the first step is a measurement of the mechanism, not a choice among fixes; every option written before 2026-07-28 was written against a cause that turned out not to be the cause |
| unassessed | open | [composite_view.py docstring budget](composite-view-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file, or fold it into CONTRACT_MODULES alongside cache_key.py |
| unassessed | open | [dag.py docstring budget](dag-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file, or fold it into CONTRACT_MODULES alongside cache_key.py, the module it says it completes |
| unassessed | open | [document.py docstring budget](document-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file |
| unassessed | open | [filter_base.py exceeds even the contract-module prose budget](filter-base-contract-budget.md) | nothing structurally — a Kendrick decision on whether CONTRACT_FILE_PROSE_WORDS is the wrong number, or whether some of this file's rationale has another owner |
| unassessed | open | [filter_tab.py is eleven jobs in one widget](filter-tab-is-eleven-jobs.md) | nothing structurally — but take it one named responsibility per item, never as one split |
| unassessed | open | [main_window.py docstring budget](main-window-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file |
| unassessed | open | [One edit is three regions in two files](one-edit-is-three-regions-in-two-files.md) | nothing — scoped to option A on 2026-07-28 after 937ac91 refused option B |
| unassessed | open | [pipeline_model.py prose budget](pipeline-model-prose-budget.md) | nothing structurally — a Kendrick decision on whether pipeline_model.py's contract-module budget should rise further, whether the file should split, or whether it stays flagged |
| unassessed | open | [prefetch.py docstring budget](prefetch-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file, or fold it into CONTRACT_MODULES alongside cache_key.py |
| unassessed | open | [pipeline/preview.py docstring budget](preview-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file |
| unassessed | open | [preview_runner.py docstring budget](preview-runner-docstring-budget.md) | nothing structurally — a Kendrick decision on whether the docstring convention's per-symbol ban and 400-word prose cap should exempt this file |
| unassessed | open | [Qt-free logic is stranded under gui/](qt-free-logic-under-gui.md) | nothing structurally — but the rework moves the biggest rows on its own terms (rescoped 2026-07-29: this item is the residue, not the plan) |
| unassessed | open | [`slider_to_graph` names a gesture two other budgets now time](slider-to-graph.md) | nothing — the trigger this item was written against ("a parameter control bound to a node") fired, and what is left is a decision about the budget table rather than a panel to build |
| unassessed | open | [Sessions log how the app was used, and changes are scored against a moving baseline](slow-path-surfacing.md) | nothing — the trigger fired 2026-07-28 when ledger-producers landed (docs/completed-todo/2026.07.28-ledger-producers.md): the resource probe and the pool meters now publish, so there is something to aggregate. |
| unassessed | deferred | [Whether the worker split should be a controller rather than a constant](adaptive-worker-allocation.md) | evidence that the fixed constants are wrong on machines that are not the reference — concretely, pool-utilisation samples from more than one class of machine showing the declared split leaving a pool starved or idle. The sensor landed 2026-07-28 (docs/completed-todo/2026.07.28-ledger-producers.md), so what is missing is no longer instrumentation but a second machine to point it at. A governor tuned on argument would oscillate where a fixed constant merely sits at the wrong value. |
| unassessed | deferred | [Annotation spans, and the accuracy feedback they unlock](annotation-spans.md) | a detector whose output is worth correcting by hand, or a labelling task that needs ground truth before one exists |
| unassessed | deferred | [Application config, and where the boundary with Preferences falls](application-config.md) | the first setting the CLI and the GUI both need to read — backend selection policy is the only live candidate, since cache bounds withdrew 2026-07-27 when the resource ledger made them derived rather than configured |
| unassessed | deferred | [flow_direction, and whether a signal is a plane or a parameter](block-signal-free-measures.md) | two decisions, both Kendrick's. (1) How a *circular* signal bands and renders — flow_direction is orthogonal to everything existing and is the screening's other survivor, but a value band and a heat ramp over an angle are wrap-around objects the GUI has no shape for. (2) Whether block_signal keeps emitting one plane chosen by a parameter or emits several at once — the screening promoted one measure, not the four that would have paid for the protocol change, so the case for it is now weaker than when this item was written and it should be reopened by a *second* survivor, not by this one. Recommendation for both in the body. |
| unassessed | deferred | [Click-through navigation: the output boundary lives in the chain-stack](click-through-navigation.md) | general materialization promoting (docs/todo/materialization.md) — the source-boundary half left this item 2026-07-28 and shipped (docs/completed-todo/2026.07.28-crop-boundary-gesture.md), and what remains here is descent through *node* output boundaries, which has no writer until the general store exists |
| unassessed | deferred | [Coverage and detection lanes on the timeline](coverage-and-detection-lanes.md) | the executor recording, per replicate and per frame, that it ran and under which resolved params |
| unassessed | deferred | [Downsample or rescale](downsample-or-rescale.md) | the parity-comparison finding landing — rescale is v1's semantic and must exist unchanged until the comparison has run; after it, this item decides whether rescale stays a first-class step or becomes a parity fixture |
| unassessed | deferred | [GPU execution](gpu-execution.md) | a filter whose CPU kernel is measurably the bottleneck of a tuning session — not merely slow, but one where `bench/budgets.py` is being missed because of it and the profile says so |
| unassessed | deferred | [HPC handoff, and review mode](hpc-handoff-and-review-mode.md) | for HPC, a dataset that does not fit in a local session plus at least one sink — a real user with a real cluster, not a milestone; for review mode, the first durable output worth interpreting, which is the same trigger the deferred **Coverage and detection lanes** item has |
| unassessed | deferred | [The ledger's two unmeasured numbers](ledger-measurements.md) | a floor reading on a *small* machine. The instrumented session ran 2026-07-28 (docs/findings/2026.07.28-the-session-floor-is-the-window.md) and changed the question: there is no machine-independent floor, session memory tracks the working window, and memory_reserve's fraction term therefore models the wrong variable — least wrongly on the largest machine and worst on the smallest. The load-bearing measurement is now one 30-second reading on ~8 GB hardware, which nobody has. H4 is answered only in the weak sense; the producers it was blocked on landed 2026-07-28 (docs/completed-todo/2026.07.28-ledger-producers.md), so what H4 now needs is a session read through them rather than a sensor. |
| unassessed | deferred | [Materialization, and the rule that is waiting on it](materialization.md) | a workload that can say what the general store's chunking is for — the replicate-crop half was split out 2026-07-28 (crop-artifact-writer / -serving / crop-boundary-gesture) and built the same day, so it no longer waits here; "filesystem is truth at rest" returned to the rules table with the writer, not with this one |
| unassessed | deferred | [Parity comparison finding](parity-comparison-finding.md) | a seated session with the v1 checkout — v1 is a sibling repo no agent can reach and no headless run can drive, so the pending decision is what that session is for: a live two-GUI side-by-side, or one throwaway v1 run that dumps its count/gate series as a fixture a headless harness then compares against forever (recommended) |
| unassessed | deferred | [Process isolation for filter execution](process-isolation.md) | a kernel that can take the process down rather than raise — most likely the first OpenCV-heavy filter or GPU work. Cooperative cancellation is the other trigger and the more likely one in practice: a full-video run the user wants to stop mid-frame cannot be interrupted in-process short of checking a flag between frames, which is fine until a single frame is slow. |
| unassessed | deferred | [What the viewport keeps of a render, and what it drops](proxy-retention-policy.md) | a recorded session that scrubs, which is docs/todo/slow-path-surfacing.md's gesture mix to make visible. The capacity half landed 2026-07-28 in 14ce201: RENDER_RING_SHARE took fraction=0.01 against a 4 GB reserve — ~644 MB and so ~700 gray 1280-wide proxies on the finding's 68.4 GB machine, sized to reach a large machine's own knee rather than to hardcode the ~720 where that session saturated — and below ~26 GB total the 256 MB floor resolves instead, so a small machine pays nothing. What is left is the scrub half, and it has no sample: 16 scrub events, 0.00% hit under every policy. Reopening the eviction rule is a stall-length argument, not a throughput one, so it needs a recorded session that actually scrubs. |
| unassessed | deferred | [Sink writers, and the replicate status that reads them](sink-writers.md) | the first filter that emits a TableSpec (a detector producing coordinates), or materialization landing and needing somewhere for a compacted array |
| unassessed | deferred | [Surrogate calibration for the detection threshold](surrogate-calibration.md) | the temporal chain settling — concretely, the first parameter set somebody wants to run over a whole video and report |
| unassessed | deferred | [Tuning files: export and import the baseline](tuning-files.md) | the first tuning somebody wants to apply to a second video — likely the parity-comparison finding, or the first multi-source experiment |
| unassessed | superseded | [A kernel protocol that is not one frame in, one frame out](kernel-protocol-beyond-one-frame.md) | superseded 2026-07-29 — the trigger fired from inside the tree (REWORK.md R2: detection is the arrived case for windowed execution, and motion_history's own docstring asks for `Mode.WINDOWED`); the two remaining shapes split into one item each, and the decimator's anti-alias record moved with its shape |
| unassessed | superseded | [The GUI holds the enumeration rule 3 forbids](the-gui-holds-the-filter-enumeration.md) | superseded 2026-07-29 — the literal sites this item catalogued are now the exception list of `a-filter-id-spelled-twice`, and the caption/card question is decided in `presentation-is-a-channel-not-a-switch` |

*61 entries.*
