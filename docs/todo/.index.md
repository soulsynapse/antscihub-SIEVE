<!-- Generated by tools/doc_index.py. Do not edit; run `uv run nox -s docs`. -->

# Open and deferred work

One file per item. `status: open` is takeable now; `status: deferred` waits on the trigger in `gated_on`. Completion is a move to `completed-todo/` via `tools/complete_item.py`; promotion is a one-line `status:` edit.

| Status | Item | Gated on |
|---|---|---|
| deferred | [Tuning files: export and import the baseline](tuning-files.md) | the first tuning somebody wants to apply to a second video — likely the parity-comparison finding, or the first multi-source experiment |
| open | [The todo DAG exists, in prose, where nothing can read it](the-todo-dag-is-prose.md) | nothing structurally — the frontmatter and the index tool are already the shape this needs, and the edges are already written, in sentences |
| deferred | [The GUI holds the enumeration rule 3 forbids](the-gui-holds-the-filter-enumeration.md) | the temporal chain becoming real nodes — two of the five enumerated steps have no filter module to be discovered *from*, so the enumeration cannot go before `Mode.WINDOWED` (docs/todo/kernel-protocol-beyond-one-frame.md); the parity comparison (docs/todo/parity-comparison-finding.md) frees the other three |
| open | [The decode format is a key input with six derivations](the-decode-format-has-six-derivations.md) | nothing structurally — every site already has the object that should own the answer, and two of the six are on the render thread inside a timed span |
| deferred | [Surrogate calibration for the detection threshold](surrogate-calibration.md) | the temporal chain settling — concretely, the first parameter set somebody wants to run over a whole video and report |
| open | [Sessions log how the app was used, and changes are scored against a moving baseline](slow-path-surfacing.md) | nothing — the trigger fired 2026-07-28 when ledger-producers landed (docs/completed-todo/2026.07.28-ledger-producers.md): the resource probe and the pool meters now publish, so there is something to aggregate. |
| open | [`slider_to_graph` names a gesture two other budgets now time](slider-to-graph.md) | nothing — the trigger this item was written against ("a parameter control bound to a node") fired, and what is left is a decision about the budget table rather than a panel to build |
| deferred | [Sink writers](sink-writers.md) | the first filter that emits a TableSpec (a detector producing coordinates), or materialization landing and needing somewhere for a compacted array |
| deferred | [Replicate status: crop progress and output existence](replicate-status-columns.md) | the deferred **Sink writers** item (docs/todo/sink-writers.md) landing, or materialization — the same trigger, from either end |
| open | [Qt-free logic is stranded under gui/](qt-free-logic-under-gui.md) | nothing structurally — but do headless-detection.md first, which moves the largest of these on its own terms |
| open | [What the viewport keeps of a render, and what it drops](proxy-retention-policy.md) | nothing — the trigger fired 2026-07-28 and the policy question is answered against the proposal on throughput (docs/findings/2026.07.28-capacity-beats-policy-in-the-render-ring.md). What is left is the change that finding points at: give RENDER_RING_SHARE a fraction so the ring grows with the allocation, since capacity was worth ~60x policy at the operating point. The scrub half is untouched and does not block it. |
| deferred | [Profiling as a module](profiling-as-a-module.md) | a budget miss whose cause is not obvious from the span that reported it — concretely, a `full_preview_render` miss over a multi-node graph |
| deferred | [Process isolation for filter execution](process-isolation.md) | a kernel that can take the process down rather than raise — most likely the first OpenCV-heavy filter or the GPU work; cooperative cancellation is the other and more likely trigger |
| deferred | [A pipeline editor, and whether it is a list or a graph](pipeline-editor-list-or-graph.md) | a graph that is not a chain — which means a multi-upstream filter, which means the named-port change to `Edge` in docs/todo/kernel-protocol-beyond-one-frame.md |
| deferred | [Parity comparison finding](parity-comparison-finding.md) | a seated session with the v1 checkout — v1 is a sibling repo no agent can reach and no headless run can drive, so the pending decision is what that session is for: a live two-GUI side-by-side, or one throwaway v1 run that dumps its count/gate series as a fixture a headless harness then compares against forever (recommended) |
| open | [One edit is three regions in two files](one-edit-is-three-regions-in-two-files.md) | nothing structurally — but the first step is a decision, and taking the mechanical half without making it is churn with no payoff |
| deferred | [Materialization, and the rule that is waiting on it](materialization.md) | a workload that can say what the general store's chunking is for — the replicate-crop half was split out 2026-07-28 (crop-artifact-writer / -serving / crop-boundary-gesture) and built the same day, so it no longer waits here; "filesystem is truth at rest" returned to the rules table with the writer, not with this one |
| open | [Machine-share policy sits above its consumers](machine-share-policy-is-above-its-consumers.md) | nothing structurally |
| deferred | [The ledger's two unmeasured numbers](ledger-measurements.md) | a floor reading on a *small* machine. The instrumented session ran 2026-07-28 (docs/findings/2026.07.28-the-session-floor-is-the-window.md) and changed the question: there is no machine-independent floor, session memory tracks the working window, and memory_reserve's fraction term therefore models the wrong variable — least wrongly on the largest machine and worst on the smallest. The load-bearing measurement is now one 30-second reading on ~8 GB hardware, which nobody has. H4 is answered only in the weak sense; the producers it was blocked on landed 2026-07-28 (docs/completed-todo/2026.07.28-ledger-producers.md), so what H4 now needs is a session read through them rather than a sensor. |
| deferred | [A kernel protocol that is not one frame in, one frame out](kernel-protocol-beyond-one-frame.md) | a filter that actually needs one — for `Mode.WINDOWED`, a filter needing a span before it can emit; for `rate_changing`, a decimator |
| deferred | [HPC handoff, and review mode](hpc-handoff-and-review-mode.md) | for HPC, a dataset that does not fit in a local session plus at least one sink — a real user with a real cluster, not a milestone; for review mode, the first durable output worth interpreting |
| open | [The detector has no headless home](headless-detection.md) | nothing structurally — the primitives are in core/ already and only the glue is misplaced |
| deferred | [GPU execution](gpu-execution.md) | a filter whose CPU kernel is measurably the bottleneck of a tuning session — not merely slow, but one where `bench/budgets.py` is being missed because of it and the profile says so |
| open | [filter_tab.py is eleven jobs in one widget](filter-tab-is-eleven-jobs.md) | nothing structurally — but take it one named responsibility per item, never as one split |
| deferred | [Downsample or rescale](downsample-or-rescale.md) | the parity-comparison finding landing — rescale is v1's semantic and must exist unchanged until the comparison has run; after it, this item decides whether rescale stays a first-class step or becomes a parity fixture |
| deferred | [Coverage and detection lanes on the timeline](coverage-and-detection-lanes.md) | the executor recording, per replicate and per frame, that it ran and under which resolved params |
| deferred | [Click-through navigation: the output boundary lives in the chain-stack](click-through-navigation.md) | general materialization promoting (docs/todo/materialization.md) — the source-boundary half left this item 2026-07-28 and shipped (docs/completed-todo/2026.07.28-crop-boundary-gesture.md), and what remains here is descent through *node* output boundaries, which has no writer until the general store exists |
| deferred | [Cache eviction, and spilling to disk](cache-eviction.md) | a tuning session that exhausts memory, or `materialize.py` landing — compaction to Zarr is where spilling belongs |
| open | [A one-shot wall clock in the correctness gate](budget-checks-under-ambient-load.md) | nothing structurally — but the fix is a decision about how a budget is adjudicated, not a threshold to nudge, so it should not be taken as a one-line tolerance bump |
| deferred | [flow_direction, and whether a signal is a plane or a parameter](block-signal-free-measures.md) | two decisions, both Kendrick's. (1) How a *circular* signal bands and renders — flow_direction is orthogonal to everything existing and is the screening's other survivor, but a value band and a heat ramp over an angle are wrap-around objects the GUI has no shape for. (2) Whether block_signal keeps emitting one plane chosen by a parameter or emits several at once — the screening promoted one measure, not the four that would have paid for the protocol change, so the case for it is now weaker than when this item was written and it should be reopened by a *second* survivor, not by this one. Recommendation for both in the body. |
| deferred | [Application config, and where the boundary with Preferences falls](application-config.md) | the first setting the CLI and the GUI both need to read — cache bounds and backend selection policy are the two candidates |
| deferred | [Annotation spans on the timeline](annotation-spans.md) | a detector whose output is worth correcting by hand, or a labelling task that needs ground truth before one exists |
| deferred | [Whether the worker split should be a controller rather than a constant](adaptive-worker-allocation.md) | evidence that the fixed constants are wrong on machines that are not the reference — concretely, pool-utilisation samples from more than one class of machine showing the declared split leaving a pool starved or idle. The sensor landed 2026-07-28 (docs/completed-todo/2026.07.28-ledger-producers.md), so what is missing is no longer instrumentation but a second machine to point it at. A governor tuned on argument would oscillate where a fixed constant merely sits at the wrong value. |
| deferred | [Accuracy feedback in the tuning loop](accuracy-feedback.md) | a marks model plus one hand-labelled window — the same trigger as the deferred **Annotation spans on the timeline** item (docs/todo/annotation-spans.md) |

*34 entries.*
