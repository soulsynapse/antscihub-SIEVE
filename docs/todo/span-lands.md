---
title: span lands
step: "04.2"
status: awaiting-review
gated_on: nothing
done_when: "uv run pytest tests/unit/test_span.py -q"
opened: 2026-08-06
---

# span lands

`tools/span.py` in the ADR-2 shape; parity per 02.4's golden mechanism; the
span param declares its `span` stereotype (01.4). The kernel ports from v2
(PLAN.md, porting discipline).
