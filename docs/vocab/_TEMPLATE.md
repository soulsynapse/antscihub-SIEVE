---
title: the word, lowercase — plus the domain if another entry spells it alike
group: an existing shelf; a new one needs a reason
position: its order along that shelf
gloss: What the word names, at most 40 words. VOCAB.md quotes it verbatim, so this is the whole entry. Names no code.
origin: emergent | decided
defined: YYYY-MM-DD
---

<!--
Copy this, name it for the word, delete this comment. Nothing goes below the
frontmatter: `uv run python -m checks.vocab` fails an entry with a body.

A definition, not an argument. `origin` carries the provenance in one field —
`emergent` if the tree reached for the word unprompted, `decided` if someone
chose it. Where the word lives is what grep is for.

One sense per entry. A word used two ways gets two files, qualified the way a
dictionary qualifies a homonym: `node (contract)` and `node (orchestrator)` are
different words that happen to be spelled alike, and listing them apart says so
without anybody having to settle it first.
-->
