# CLAUDE.md

Read `proto_sieve/docs/AGENTS.md` for the rules, then `proto_sieve/docs/STATUS.md`
for how far the work has got and what the next action is. `AGENTS.md`
**replaces** the repo root's `AGENTS.md` here — it does not extend it.

Nothing else in this repo governs work under `proto_sieve/`. This file is only
the pointer.
