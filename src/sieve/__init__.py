"""The runtime-importable version string: what the CLI `--version` flag and the
GUI's `QApplication.setApplicationVersion` report, independent of the build
metadata `pyproject.toml` carries under the same name.
"""

__version__ = "0.1.0"
