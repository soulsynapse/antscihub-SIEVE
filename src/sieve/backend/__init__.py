"""Device policy and backend identity. Holds no filter's implementation.

If adding a filter required editing a file in this package, non-negotiable #3
would already be broken. Kernels live beside their spec in `sieve.filters`;
what lives here is the question of *which* kernel runs and what to call the
machine that ran it.
"""
