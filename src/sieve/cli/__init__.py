"""Typer command surface: thin adapters over the pipeline layer (ADR-006).

No pipeline composition or scientific logic in a callback. Runs without Qt
installed, which is the point of the layer rules below it.
"""
