"""Filter implementations, discovered by explicit import rather than by scan.

Each filter is ``<name>.py`` beside a ``<name>.md`` guidance file in a category
directory; the pair is the extension unit. Registration failure is loud on
purpose -- a broken filter does not silently vanish from the registry.
"""
